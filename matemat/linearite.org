
#+STARTUP: showall

#+TITLE: Eclats de vers : Matemat : Linéarité
#+AUTHOR: chimay
#+EMAIL: or du val chez gé courriel commercial
#+LANGUAGE: fr
#+LINK_HOME: file:../index.html
#+LINK_UP: file:index.html
#+HTML_HEAD: <link rel="stylesheet" type="text/css" href="../style/defaut.css" />

#+OPTIONS: H:6
#+OPTIONS: toc:nil

#+TAGS: noexport(n)

[[file:index.org][Index des Grimoires]]

#+INCLUDE: "../include/navigan-1.org"

#+TOC: headlines 1

#+INCLUDE: "../include/latex/latex.org"

\label{chap:lineaire}

* Dépendances

  - Chapitre \ref{chap:fonction} : Les fonctions

* Définition

Soit les ensembles $E$ et $F$, un corps $\corps$ et la fonction $f :
E \mapsto F$. On dit que $f$ est linéaire si, pour tout $x,y \in E$
et $\alpha, \beta \in \corps$, on a :

$$f(\alpha \cdot x + \beta \cdot y) = \alpha \cdot f(x) + \beta \cdot f(y)$$

On note $\lineaire(E,F)$ l'ensemble des fonctions linéaires de $E$ vers $F$.

* Généralisation

Soit les $x_i \in E$ et les $\alpha_i \in \corps$, pour $i \in
\{1,2,...,n\}$. On peut montrer par récurrence que :

$$f\left(\sum_{i=1}^n \alpha_i x_i\right) = \sum_{i=1}^n \alpha_i \cdot f(x_i)$$

* Identité

L'application identité est clairement linéaire.

* Inverse

Soit $u = f(x)$ et $v = f(y)$. Si l'application inverse existe, on a
$x = f^{-1}(u)$ et $y = f^{-1}(v)$. En composant à gauche par $f^{-1}$
la définition de la linéarité :

$$f\left(\alpha \cdot f^{-1}(u) + \beta \cdot f^{-1}(v)\right) = \alpha \cdot u + \beta \cdot v$$

on obtient :

$$\alpha \cdot f^{-1}(u) + \beta \cdot f^{-1}(v) = f^{-1}(\alpha \cdot u + \beta \cdot v)$$

ce qui montre que l'inverse est également linéaire.

* Image de l’élément nul

Choisissons un $x \in E$. on voit que :

$$f(0) = f(0 \cdot x) = 0 \cdot f(x) = 0$$

La valeur d'une fonction linéaire s'annule en l’élément nul.
