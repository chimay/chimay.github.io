% vim: set ft=tex tabstop=4 shiftwidth=4 noexpandtab:

% standard template with header in another file

\input{../../include/latex/tikz/header-light.tex}

% opening %{{{1

\begin{document}
\begin{tikzpicture}[scale=1.0]

% parameters %{{{1

	%\def\len{3}
	%\def\gth{3}

% coordinates %{{{1

	\coordinate (E) at (0,0);
	\coordinate (F) at (7,0);
	\coordinate (G) at (6,3);
	\coordinate (H) at (3,3);

	\coordinate (I) at (3,0);
	\coordinate (J) at (7,3);

	\coordinate (X) at (-2,3);
	\coordinate (Y) at (9,3);

% points, dots, vertices %{{{1

	\fill (E) circle (0.4mm);
	\fill (F) circle (0.4mm);
	\fill (G) circle (0.4mm);
	\fill (H) circle (0.4mm);
	\fill (I) circle (0.4mm);
	\fill (J) circle (0.4mm);

% segments, sides %{{{1

	\draw (E) -- (F) -- (G) -- (H) -- cycle;

	\draw[dashdotdotted] (X) -- (H);
	\draw[dashdotdotted] (G) -- (Y);

	\draw[dashdotted] (F) -- (H);

	\draw[dashed] (H) -- (I);
	\draw[dashed] (F) -- (J);

% points, dots, vertices labels %{{{1

	\node[below left] at (E) {$E$};
	\node[below right] at (F) {$F$};
	\node[above right] at (G) {$G$};
	\node[above left] at (H) {$H$};

	\node[above left] at (I) {$I$};
	\node[above] at (J) {$J$};

% segments, sides, lines labels %{{{1

	\node[below] at ($ (E)!0.5!(F) $) {$B$};
	\node[above] at ($ (G)!0.5!(H) $) {$b$};

	\node[left] at ($ (H)!0.5!(I) $) {$h$};
	\node[right] at ($ (F)!0.5!(J) $) {$h$};

% right angles markers %{{{1

	\pic [draw, angle radius=7pt, angle eccentricity=1]
	{right angle = F--I--H};

	\pic [draw, angle radius=7pt, angle eccentricity=1]
	{right angle = G--J--F};

% closing %{{{1

\end{tikzpicture}
\end{document}
