
% header %{{{1

\documentclass[tikz, border=1mm]{standalone}

\usepackage{amsmath}

\usetikzlibrary{calc,angles,quotes,shapes.geometric}

% document %{{{1

% opening %{{{2

\begin{document}
\begin{tikzpicture}[scale=1.3]

% parameters %{{{2

	% ---- triangle 3 - 4 - 5
	% ---- arctan(4/3) in degrees

	\def\ang{53.13}

% coordinates %{{{2

	\coordinate (A) at (5,0);
	\coordinate (B) at (0,0);
	\coordinate (C) at ({3*cos(\ang)},{3*sin(\ang)});

	% ---- feet of altitude from C to AB

	\coordinate (D) at ({3*cos(\ang)},0);

% triangle %{{{2

	\draw (A) -- (B) -- (C) -- cycle;

	% ---- altitude

	\draw[dashed] (C) -- (D);

% vertices, points, dots %{{{2

	\fill (A) circle (0.3mm);
	\fill (B) circle (0.3mm);
	\fill (C) circle (0.3mm);
	\fill (D) circle (0.3mm);

% vertices labels %{{{1

	\node[below right] at (A) {$A$};
	\node[below left] at (B) {$B$};
	\node[above] at (C) {$C$};
	\node[above right] at (D) {$D$};

% sides labels %{{{2

	\node[above left]  at ($(B)!0.5!(C)$) {$a$};
	\node[above right] at ($(A)!0.5!(C)$) {$b$};
	\node[below] at ($(A)!0.5!(B)$) {$c$};

	% ---- height label

	\node[left] at ($(C)!0.5!(D)$) {$h$};

	% ---- segment labels on altitude projection

	\node[above] at ($(B)!0.5!(D)$) {$u$};
	\node[above] at ($(D)!0.5!(A)$) {$v$};

% right angles markers %{{{2

	\pic [draw, angle radius=7pt, angle eccentricity=1]
	{right angle = C--D--B};

% closing %{{{2

\end{tikzpicture}
\end{document}
