% vim: set ft=tex tabstop=4 shiftwidth=4 noexpandtab:

\input{../../include/latex/tikz/header.tex}

% opening %{{{2

\begin{document}
\begin{tikzpicture}[scale=1.0]

% parameters %{{{1

	\def\ang{0}
	\def\gle{180}
	\def\ngl{-30}

	\def\radius{2.5}
	\def\ext{2.3}

% coordinates %{{{1

	\coordinate (B) at (1,2);

	\coordinate (A) at ($ (B) - 1.5*(\radius,0) $);
	\coordinate (C) at ($ (B) + 1.7*(\radius,0) $);

	\coordinate (Bplus) at ($ (B) - (\radius,0) $);
	\coordinate (Bminus) at ($ (B) + (\radius,0) $);

	\coordinate (X) at ($ (B) - \ext*(\radius,0) $);
	\coordinate (Y) at ($ (B) + \ext*(\radius,0) $);

	\coordinate (labelC) at ($ (B) + (\ngl:{\radius+0.3}) $);

% points, dots, vertices %{{{1

	\fill (A) circle (0.4mm);
	\fill (B) circle (0.4mm);
	\fill (C) circle (0.4mm);

	%\fill (Bplus) circle (0.4mm);
	%\fill (Bminus) circle (0.4mm);

% segments, sides %{{{1

	\draw (X) -- (Y);

% circular arcs %{{{1

	\draw[thick, vector] (B) ++(\ang:\radius) arc (\ang:\gle:\radius);
	\draw[dashed] (B) ++(\gle:\radius) arc (\gle:{\ang+360}:\radius);

% points, dots, vertices labels %{{{1

	\node[below left] at (A) {$A$};
	\node[below] at (B) {$B$};
	\node[below right] at (C) {$C$};

% circle label %{{{1

	\node at (labelC) {$\mathscr{C}$};

% segments, sides, lines labels %{{{1

	\node[above] at ($ (X)!0.05!(Y) $) {$d$};

	\node[below] at ($ (Bminus)!0.5!(B) $) {$1$};
	\node[below] at ($ (B)!0.5!(Bplus) $) {$1$};

% segments, sides, lines marks %{{{1

	\tkzMarkSegments[mark=||, size=2pt](B,Bplus)
	\tkzMarkSegments[mark=||, size=2pt](B,Bminus)

% circular arcs labels %{{{1

	\node at ($ (B) + ({0.5*(\ang+\gle)}:\radius+0.3) $) {$\Gamma$};

% angles labels %{{{1

	\pic[draw, ->, "$\alpha$", angle radius=0.8cm, angle eccentricity=1.3]
	{angle = C--B--A};

% closing %{{{1

\end{tikzpicture}
\end{document}
