
\input{../../include/latex/tikz/header.tex}

% opening %{{{1

\begin{document}
\begin{tikzpicture}[scale=2.5]

% parameters %{{{1

	\def\unit{1.4}
	\def\len{1.7}

	\def\ang{40}

	\def\extratio{1.0}

% coordinates %{{{1

	\coordinate (A) at (0,0);

	\coordinate (U) at (\unit,0);
	\coordinate (V) at (\ang:\unit);

	\coordinate (X) at ({\extratio*\len},0);
	\coordinate (Y) at (\ang:{\extratio*\len});

% coordinates for dimension lines %{{{1

	%\coordinate (dimA) at ($ (A) + (0,-0.4) $);
	%\coordinate (dimU) at ($ (U) + (0,-0.4) $);

% dots, points, vertices %{{{1

	\fill (A) circle (0.2mm);

% radius lines %{{{1

	\draw (A) -- (X);
	\draw (A) -- (Y);

% dimension lines %{{{1

	%\draw[dimension] (dimA) -- (dimU);
	%\draw[dimension extension] (A) -- (dimA);
	%\draw[dimension extension] (U) -- (dimU);

% arc to represent the angle %{{{1

	\draw[thick, vector] (A) ++(0:\unit) arc (0:\ang:\unit);

	%\draw[vector] (D) arc[start angle=0, end angle=40, radius=\unit]
		%node[midway, right=2pt] {$\arcdecercle{DE}$};

% vertices labels %{{{1

	\node[below left] at (A) {$A$};

% segments, sides labels %{{{1

	\node[below] at ($ (A)!0.5!(U) $) {$1$};
	\node[above left] at ($ (A)!0.5!(V) $) {$1$};

	%\node[below, goldenbrown] at ($ (dimA)!0.5!(dimU) $) {$1$};

% circular arcs labels %{{{2

	\node at ($ (A) + ({\ang/2}:\unit + 0.15) $) {$\Gamma$};

% segments, sides, lines marks %{{{1

	\tkzMarkSegments[mark=||, size=2pt](A,U)
	\tkzMarkSegments[mark=||, size=2pt](A,V)

% angle label %{{{1

	\pic[draw, ->, "$\alpha$", angle radius=1.3cm, angle eccentricity=1.2]
	{angle = U--A--V};

% closing %{{{1

\end{tikzpicture}
\end{document}
