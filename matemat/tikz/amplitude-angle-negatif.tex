
% header %{{{1

\documentclass[tikz, border=1mm]{standalone}

\usepackage{amsmath}
\usepackage{tikz}

%\usetikzlibrary{calc,angles,quotes}
\usetikzlibrary{calc,angles,quotes,intersections,shapes.geometric,arrows.meta}

% document %{{{1

% opening %{{{2

\begin{document}
\begin{tikzpicture}[scale=1.0]

% parameters %{{{2

	\def\len{2.7}

% coordinates %{{{2

	\coordinate (A) at (0,0);
	\coordinate (B) at (\len,0);
	\coordinate (C) at ({\len*cos(40)}, {\len*sin(40)});

% radius lines %{{{2

	\draw (A) -- (B);
	\draw (A) -- (C);

% dots, points, vertices %{{{2

	\fill (A) circle (0.4mm);

% vertices labels %{{{2

	\node[left] at (A) {$A$};

% angle label %{{{2

	\pic[draw, {Straight Barb[length=1.2mm]}-, "$\alpha$", angle radius=1.5cm, angle eccentricity=1.2]
	{angle = B--A--C};

% closing %{{{2

\end{tikzpicture}
\end{document}
