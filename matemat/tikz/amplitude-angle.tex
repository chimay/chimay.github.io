
% header %{{{1

\documentclass[tikz, border=1mm]{standalone}

\usepackage{amsmath}
\usepackage{tikz}

\usetikzlibrary{calc,angles,quotes}

% document %{{{1

% opening %{{{2

\begin{document}
\begin{tikzpicture}[scale=1]

% coordinates %{{{2

	\coordinate (A) at (0,0);
	\coordinate (B) at (3,0);
	\coordinate (C) at ({3*cos(40)}, {3*sin(40)});

% radius lines %{{{2

	\draw (0,0) -- (3,0) node[midway, below] {$r$};
	\draw (0,0) -- ({3*cos(40)}, {3*sin(40)});

% arc to represent the angle %{{{2

	\draw (3,0) arc[start angle=0, end angle=40, radius=3]
	node[midway, right=2pt] {$l$};

% angle marking at the center %{{{2

	\pic[draw, ->, "$\alpha$", angle radius=1.0cm, angle eccentricity=1.2]
	{angle = B--A--C};

% thick vertices %{{{2

	\fill (A) circle (0.4mm);
	\fill (B) circle (0.4mm);
	\fill (C) circle (0.4mm);

% vertices labels %{{{2

	\node[left] at (A) {$A$};
	\node[below] at (B) {$B$};
	\node[above] at (C) {$C$};

% closing %{{{2

\end{tikzpicture}
\end{document}
