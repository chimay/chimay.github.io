% vim: set ft=tex tabstop=4 shiftwidth=4 noexpandtab:

\input{../../include/latex/tikz/header.tex}

% opening %{{{1

\begin{document}
\begin{tikzpicture}[scale=1.0]

% parameters %{{{1

	\def\ang{-70}
	\def\gle{80}
	\def\ngl{170}

	\def\radius{4.5}

% coordinates %{{{1

	\coordinate (O) at (0,0);
	\coordinate (A) at (\ang:\radius);
	\coordinate (B) at (\gle:\radius);
	\coordinate (P) at (\ngl:\radius);

% points, dots, vertices %{{{1

	\fill (O) circle (0.4mm);
	\fill (A) circle (0.4mm);
	\fill (B) circle (0.4mm);
	\fill (P) circle (0.4mm);

% segments, sides %{{{1

	\draw (O) -- (A);
	\draw (O) -- (B);
	\draw[dashed] (P) -- (O);
	\draw (P) -- (A);
	\draw (P) -- (B);

% circles %{{{1

	\draw (O) circle (\radius);

% points, dots, vertices labels %{{{1

	\node[anchor=west] at ($ (O) + (0:0.1) $) {$O$};
	\node[below right] at (A) {$A$};
	\node[above] at (B) {$B$};
	\node[left] at (P) {$P$};

% segments, sides labels %{{{1

	\node[above right] at ($ (O)!0.5!(B) $) {$r$};

% segments, sides marks %{{{1

	\tkzMarkSegments[mark=||, size=2pt](O,A)
	\tkzMarkSegments[mark=||, size=2pt](O,B)
	\tkzMarkSegments[mark=||, size=2pt](O,P)

% circle label %{{{1

	\node at (135:{\radius+0.3}) {$\mathscr{C}$};
	\node at (0:{\radius+0.3}) {$\Gamma$};

% angles labels %{{{1

	\pic[draw, ->, "$\alpha$", angle radius=0.8cm, angle eccentricity=1.3]
	{angle = A--O--B};

	\pic[draw, ->, "$\beta$", angle radius=0.9cm, angle eccentricity=1.3]
	{angle = A--P--B};

	\pic[draw, ->, "$\gamma$", angle radius=1.6cm, angle eccentricity=1.2]
	{angle = O--P--B};

	\pic[draw, ->, "$\gamma$", angle radius=1.2cm, angle eccentricity=1.2]
	{angle = P--B--O};

	\pic[draw, ->, "$\lambda$", angle radius=1.8cm, angle eccentricity=1.2]
	{angle = A--P--O};

	\pic[draw, ->, "$\lambda$", angle radius=1.2cm, angle eccentricity=1.2]
	{angle = O--A--P};

	\pic[draw, ->, "$\delta$", angle radius=1.0cm, angle eccentricity=1.3]
	{angle = B--O--P};

	\pic[draw, ->, "$\theta$", angle radius=0.9cm, angle eccentricity=1.3]
	{angle = P--O--A};

% closing %{{{1

\end{tikzpicture}
\end{document}
