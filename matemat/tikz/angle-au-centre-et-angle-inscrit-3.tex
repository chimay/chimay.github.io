% vim: set ft=tex tabstop=4 shiftwidth=4 noexpandtab:

\input{../../include/latex/tikz/header.tex}

% opening %{{{1

\begin{document}
\begin{tikzpicture}[scale=1.0]

% parameters %{{{1

	\def\ang{10}
	\def\gle{60}
	\pgfmathtruncatemacro{\ngl}{\ang+180}

	\def\radius{4.5}

% coordinates %{{{1

	\coordinate (O) at (0,0);
	\coordinate (A) at (\ang:\radius);
	\coordinate (B) at (\gle:\radius);
	\coordinate (P) at (\ngl:\radius);

% points, dots, vertices %{{{1

	\fill (O) circle (0.4mm);
	\fill (A) circle (0.4mm);
	\fill (B) circle (0.4mm);
	\fill (P) circle (0.4mm);

% segments, sides %{{{1

	\draw (O) -- (A);
	\draw (O) -- (B);
	\draw (P) -- (O);
	\draw (P) -- (B);

% circles %{{{1

	\draw (O) circle (\radius);

% points, dots, vertices labels %{{{1

	\node[below] at (O) {$O$};
	\node[below right] at (A) {$A$};
	\node[above] at (B) {$B$};
	\node[below left] at (P) {$P$};

% segments, sides labels %{{{1

	\node[below right] at ($ (O)!0.5!(B) $) {$r$};

% segments, sides marks %{{{1

	\tkzMarkSegments[mark=||, size=2pt](O,A)
	\tkzMarkSegments[mark=||, size=2pt](O,B)
	\tkzMarkSegments[mark=||, size=2pt](O,P)

% circle label %{{{1

	\node at (100:{\radius+0.3}) {$\mathscr{C}$};
	\node at (35:{\radius+0.3}) {$\Gamma$};

% angles labels %{{{1

	\pic[draw, ->, "$\alpha$", angle radius=0.8cm, angle eccentricity=1.3]
	{angle = A--O--B};

	\pic[draw, ->, "$\beta$", angle radius=2.1cm, angle eccentricity=1.1]
	{angle = A--P--B};

	\pic[draw, ->, "$\beta$", angle radius=2.5cm, angle eccentricity=1.1]
	{angle = P--B--O};

	\pic[draw, ->, "$\delta$", angle radius=0.6cm, angle eccentricity=1.4]
	{angle = B--O--P};

% closing %{{{1

\end{tikzpicture}
\end{document}
