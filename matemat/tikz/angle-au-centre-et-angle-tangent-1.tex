% vim: set ft=tex tabstop=4 shiftwidth=4 noexpandtab:

\input{../../include/latex/tikz/header.tex}

% opening %{{{1

\begin{document}
\begin{tikzpicture}[scale=1.0]

% parameters %{{{1

	\def\len{1.1}

	\def\ang{100}
	\def\gle{-10}

	\def\radius{4}

% coordinates %{{{1

	\coordinate (O) at (0,0);
	\coordinate (A) at (\ang:\radius);

	\coordinate (B) at (\gle:\radius);

	\coordinate (X) at ($(A)!-\len!90:(O)$);
	\coordinate (Y) at ($(A)!\len!90:(O)$);

% points, dots, vertices %{{{1

	\fill (O) circle (0.4mm);
	\fill (A) circle (0.4mm);
	\fill (B) circle (0.4mm);

% segments, sides %{{{1

	\draw (A) -- (B);

	\draw (O) -- (A);
	\draw (O) -- (B);

	\draw (X) -- (Y);

% circles %{{{1

	\draw (O) circle (\radius);

% points, dots, vertices labels %{{{1

	\node[below] at (O) {$O$};
	\node[above] at (A) {$A$};
	\node[above right] at (B) {$B$};

% segments, sides, lines labels %{{{1

	\node[above] at ($ (X)!0.9!(Y) $) {$t$};
	\node[below left] at ($ (O)!0.5!(A) $) {$r$};
	\node[below right] at ($ (O)!0.5!(B) $) {$r$};

% segments, sides, lines marks %{{{1

	\tkzMarkSegments[mark=||, size=2pt](O,A)
	\tkzMarkSegments[mark=||, size=2pt](O,B)

% circles labels %{{{1

	\node at ($ (O)+(155:\radius+0.3) $) {$\mathscr{C}$};

% angles labels %{{{1

	\pic[draw, ->, "$\alpha$", angle radius=0.8cm, angle eccentricity=1.3]
	{angle = B--O--A};

	\pic[draw, ->, "$\gamma$", angle radius=1.4cm, angle eccentricity=1.3]
	{angle = O--A--B};

	\pic[draw, ->, "$\gamma$", angle radius=1.4cm, angle eccentricity=1.3]
	{angle = A--B--O};

	\pic[draw, ->, "$\beta$", angle radius=1.2cm, angle eccentricity=1.2]
	{angle = B--A--Y};

% right angles markers %{{{2

	\pic [draw, angle radius=7pt, angle eccentricity=1]
	{right angle = X--A--O};

% closing %{{{1

\end{tikzpicture}
\end{document}
