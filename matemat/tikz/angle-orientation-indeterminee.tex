
\input{../../include/latex/tikz/header.tex}

% opening %{{{1

\begin{document}
\begin{tikzpicture}[scale=1.0]

% parameters %{{{1

	\def\len{2.7}

% coordinates %{{{1

	\coordinate (A) at (0,0);
	\coordinate (B) at (\len,0);
	\coordinate (C) at ({\len*cos(40)}, {\len*sin(40)});

% radius lines %{{{1

	\draw (A) -- (B);
	\draw (A) -- (C);

% dots, points, vertices %{{{1

	\fill (A) circle (0.4mm);

% vertices labels %{{{1

	\node[left] at (A) {$A$};

% angle label %{{{1

	\pic[draw, double arrow, "$\alpha$", angle radius=1.5cm, angle eccentricity=1.2]
	{angle = B--A--C};

% closing %{{{1

\end{tikzpicture}
\end{document}
