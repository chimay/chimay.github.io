% vim: set ft=tex tabstop=4 shiftwidth=4 noexpandtab:

\input{../../include/latex/tikz/header.tex}

% opening %{{{1

\begin{document}
\begin{tikzpicture}[scale=1.0]

% parameters %{{{1

	\def\len{1.5}
	\def\gth{2.3}

	\def\ang{-30}
	\def\gle{70}

	\def\radius{3}

% coordinates %{{{1

	\coordinate (O) at (0,0);
	\coordinate (A) at (\ang:\radius);
	\coordinate (P) at (\gle:\radius);

	\coordinate (B) at ($(P)!\len!90:(O)$);

	\coordinate (X) at ($(P)!{- \gth + \len}!90:(O)$);
	\coordinate (Y) at ($(P)!\gth!90:(O)$);

% points, dots, vertices %{{{1

	\fill (O) circle (0.4mm);
	\fill (A) circle (0.4mm);
	\fill (B) circle (0.4mm);
	\fill (P) circle (0.4mm);

% segments, sides %{{{1

	\draw (P) -- (A);
	\draw (P) -- (B);

	\draw[dashed] (O) -- (P);

	\draw (X) -- (P);
	\draw (B) -- (Y);

% circles %{{{1

	\draw (O) circle (\radius);

% points, dots, vertices labels %{{{1

	\node[above left] at (O) {$O$};
	\node[right] at (A) {$A$};
	\node[above] at (B) {$B$};
	\node[above] at (P) {$P$};

% circles labels %{{{1

	\node at ($ (O)+(135:\radius+0.3) $) {$\mathscr{C}$};

% angles labels %{{{1

	\pic[draw, ->, "$\alpha$", angle radius=1.2cm, angle eccentricity=1.2]
	{angle = A--P--B};

% right angles markers %{{{2

	%\pic [draw, angle radius=7pt, angle eccentricity=1]
	%{right angle = X--P--O};

% closing %{{{1

\end{tikzpicture}
\end{document}
