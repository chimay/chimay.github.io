% vim: set ft=tex tabstop=4 shiftwidth=4 noexpandtab:

\input{../../include/latex/tikz/header.tex}

% opening %{{{1

\begin{document}
\begin{tikzpicture}[scale=1.0]

% parameters %{{{1

	\def\len{3.0cm}
	\def\gth{5.0cm}

	\def\ang{70}
	\def\gle{190}

	\def\radius{3}

% coordinates %{{{1

	\coordinate (O) at (0,0);
	\coordinate (A) at (\ang:\radius);
	\coordinate (B) at (\gle:\radius);

	\coordinate (C) at ($(A)!\len!-90:(O)$);

	\coordinate (X) at ($(A)!\gth!90:(O)$);
	\coordinate (Y) at ($(A)!\gth!-90:(O)$);

% points, dots, vertices %{{{1

	\fill (O) circle (0.4mm);
	\fill (A) circle (0.4mm);
	\fill (B) circle (0.4mm);
	\fill (C) circle (0.4mm);

% segments, sides %{{{1

	\draw (A) -- (B);
	\draw (A) -- (C);

	\draw[dashed] (O) -- (A);

	\draw (X) -- (A);
	\draw (C) -- (Y);

% circles %{{{1

	\draw (O) circle (\radius);

% points, dots, vertices labels %{{{1

	\node[above left] at (O) {$O$};
	\node[above] at (A) {$A$};
	\node[left] at (B) {$B$};
	\node[above] at (C) {$C$};

% segments, sides, lines labels %{{{1

	\node[above] at ($ (X)!0.5!(A) $) {$t$};

% circles labels %{{{1

	\node at ($ (O)+(5:\radius+0.3) $) {$\mathscr{C}$};

% circular arcs labels %{{{2

	\node at ($ (O) + (135:\radius + 0.3) $) {$\Gamma$};

% angles labels %{{{1

	\pic[draw, ->, "$\alpha$", angle radius=1.2cm, angle eccentricity=1.2]
	{angle = C--A--B};

% right angles markers %{{{2

	%\pic [draw, angle radius=7pt, angle eccentricity=1]
	%{right angle = X--C--O};

% closing %{{{1

\end{tikzpicture}
\end{document}
