% vim: set ft=tex :

% documentation %{{{1

% units %{{{2

% default unit for plain numbers is 1 cm
%
% conversion :
%
% mm ≈ pt × 0.35
%
% scaling :
%
% - unitless coordinates scale
% - explicit units do not scale
%
% if you want explicit units to scale, you have to add transform shape :
%
% \begin{tikzpicture}[scale=2, transform shape]
% ...
% \end{tikzpicture}

% styles %{{{2

% ---- defining styles
%
% \tikzset{
%   main line/.style={line width=0.8pt},
% }
%
% -- using styles
%
% \draw[main line] (A)--(B);

% header %{{{1

% opening %{{{2

\documentclass[tikz, border=1mm]{standalone}

% packages and libraries %{{{2

% ---- not necessary since the documentclass[tikz ...] requires it
% \usepackage{tikz}

\usepackage{amsmath}
\usepackage{mathrsfs}
\usepackage[x11names]{xcolor}

\usetikzlibrary{calc,angles,quotes,intersections,shapes.geometric}

\usepackage{tkz-euclide}

% style %{{{2

\tikzset{
	% ---- default
	every path/.style={line width=0.3pt},
	every coordinate/.style={fill=black, circle, inner sep=1pt},
	every node/.style={font=\normalsize},
	every picture/.style={scale=1.0},
	% ---- custom
	construction/.style={line width=0.1pt, dashed},
	dimension/.style={line width=0.2pt, <->, brown},
	dimension extension/.style={line width=0.2pt, dashed, brown},
	vector/.style={->, thick},
}

% document %{{{1

% opening %{{{2

\begin{document}
\begin{tikzpicture}[scale=1.0]

% parameters %{{{2

	\def\len{2}
	\def\extlen{4}
	\def\radius{4}
	\def\arcradius{5}
	\def\ang{60}

% coordinates %{{{2

	\coordinate (O) at (0,0);
	\coordinate (A) at (0:\len);
	\coordinate (B) at (\ang:\len);

% coordinates of points in circles %{{{3

	\coordinate (R) at ($(O) + (\radius, 0)$);

% vectors %{{{2

	\coordinate (v1) at ($ (A) - (O) $);
	\coordinate (v2) at ($ (B) - (O) $);

% unit vectors %{{{3

	\coordinate (u1) at ($ (0,0)!1cm!(v1) $);
	\coordinate (u2) at ($ (0,0)!1cm!(v2) $);

% coordinates from vectors %{{{2

	\coordinate (X) at ($ (A) + \extlen*(u1) $);
	\coordinate (Y) at ($ (B) + \extlen*(u2) $);

% intersections %{{{2

	\tkzInterLC(O,A)(O,R)\tkzGetSecondPoint{C}
	\tkzInterLC(O,B)(O,R)\tkzGetSecondPoint{D}

	\path[name path=circleA] (C) circle (\arcradius);
	\path[name path=circleB] (D) circle (\arcradius);
	\path [name intersections={of=circleA and circleB, by={I,J}}];

% points for dimension lines %{{{2

	%\coordinate (IdJ) at ($ (I) + (0,0.7) $);

% segments %{{{2

	\draw (O) -- (X);
	\draw (O) -- (Y);

	%\foreach \i in {1,...,6} { \draw (O) -- (u\i); }

% circles %{{{2

	%\draw (O) circle (\radius);

% circular arcs %{{{2

	\draw (O) ++(-5:\radius) arc (-5:{\ang + 5}:\radius);

	\draw (C) ++(20:\arcradius) arc (20:70:\arcradius);
	\draw (D) ++(-20:\arcradius) arc (-20:30:\arcradius);

% polygons %{{{2

	%\draw (P1) \foreach \i in {2,...,\numsides} { -- (P\i) } -- cycle;

% radiuses %{{{2

	%\draw (O) -- (P1);

% dimension lines %{{{2

	%\draw[dimension] (IdJ) -- (JdI);
	%\draw[dimension extension] (I) -- (IdJ);
	%\draw[dimension extension] (J) -- (JdI);

% thick vertices %{{{2

	\fill (O) circle (0.5mm);
	\fill (A) circle (0.5mm);
	\fill (B) circle (0.5mm);
	\fill (C) circle (0.5mm);
	\fill (D) circle (0.5mm);
	\fill (I) circle (0.5mm);

% vertices labels %{{{2

	\node[below left] at (O) {$O$};
	\node[below] at (A) {$A$};
	\node[left] at (B) {$B$};

	%\node[label={[label distance=0.5mm]above:$G$}] at (G) {};

% segments labels %{{{2

	%\node[above] at ($ (A)!0.5!(B) $) {$a$};

% angles labels %{{{2

	%\pic[draw, ->, "$60^\circ$", angle radius=0.5cm, angle eccentricity=1.5]
	%{angle = H--O--I};

% right angles labels %{{{2

	%\pic [draw, angle radius=7pt, angle eccentricity=1]
	%{right angle = C--B--A};

% closing %{{{2

\end{tikzpicture}
\end{document}
