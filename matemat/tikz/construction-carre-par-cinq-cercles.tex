% vim: set ft=tex tabstop=4 shiftwidth=4 noexpandtab:

\input{../../include/latex/tikz/header.tex}

% opening %{{{1

\begin{document}
\begin{tikzpicture}[scale=1.0]

% parameters %{{{1

	\tikzmath{
		\radius = 2.4 ;
		\sqrtradius = sqrt(3)/2 * \radius ;
	}

% coordinates %{{{1

	\coordinate (O_1) at ({-\radius/2},\sqrtradius);
	\coordinate (O_2) at ({\radius/2},\sqrtradius);
	\coordinate (O_3) at (0,0);
	\coordinate (O_4) at (-\radius,0);
	\coordinate (O_5) at (\radius,0);

	\coordinate (M) at ({-\radius/2},{\sqrtradius - \radius});
	\coordinate (N) at ({\radius/2},{\sqrtradius - \radius});

	\coordinate (I) at ({-\radius/2},-\sqrtradius);
	\coordinate (J) at ({\radius/2},-\sqrtradius);

% intersections %{{{1

	%\begin{pgfinterruptboundingbox}
		%\path[name path=circleA] (A) circle (\radius);
		%\path[name path=circleB] (B) circle (\radius);
		%\path[name intersections={of=circleA and circleB, by={I,J}}];
	%\end{pgfinterruptboundingbox}

% distance %{{{1

	%\path let \p1 = ($ (B) - (A) $)
	%in \pgfextra{
		%\pgfmathsetmacro{\distAB}{veclen(\x1,\y1)/1cm}
		%\global\let\distAB\distAB
	%};

% points, dots, vertices %{{{1

	\foreach \i in {1,...,5} \fill (O_\i) circle (0.4mm);

	\fill (I) circle (0.4mm);
	\fill (J) circle (0.4mm);

	\fill (M) circle (0.4mm);
	\fill (N) circle (0.4mm);

% segments, sides, lines %{{{1

	\draw (O_1) -- (M) -- (N) -- (O_2) -- cycle ;

	\draw[dashed] (M) -- (I);
	\draw[dashed] (N) -- (J);

% circles %{{{1

	\foreach \i in {1,...,5} \draw[dashed] (O_\i) circle (\radius);

% circular arcs %{{{2

	%\draw (A) ++(\ang:\radius) arc (\ang:\gle:\radius);

% points, dots, vertices labels %{{{1

	\node[left] at ($ (O_1) + (180:4pt) $) {$O_1$};
	\node[right] at ($ (O_2) + (0:4pt) $) {$O_2$};
	\node[left] at ($ (O_3) + (180:4pt) $) {$O_3$};
	\node[below left] at (O_4) {$O_4$};
	\node[below right] at (O_5) {$O_5$};

	\node[below] at (I) {$I$};
	\node[below] at (J) {$J$};

	\node[below left] at (M) {$M$};
	\node[below right] at (N) {$N$};

% segments, sides, lines labels %{{{1

	%\node[above] at ($ (A)!0.5!(B) $) {$d$};

% segments, sides, lines marks %{{{1

	%\tkzMarkSegments[mark=||, size=2pt, pos=0.5](A,B)
	%\tkzMarkSegments[mark=||, size=2pt, pos=0.5](C,D)

% circles labels %{{{1

	\node at ($ (O_1) + (135:\radius + 0.3) $) {$\mathscr{C}_1$};
	\node at ($ (O_2) + (45:\radius + 0.3) $) {$\mathscr{C}_2$};
	\node at ($ (O_3) + (270:\radius + 0.3) $) {$\mathscr{C}_3$};
	\node at ($ (O_4) + (225:\radius + 0.3) $) {$\mathscr{C}_4$};
	\node at ($ (O_5) + (-45:\radius + 0.3) $) {$\mathscr{C}_5$};

% circular arcs labels %{{{2

	%\node at ($ (O) + (45:\radius + 0.3) $) {$\mathscr{C}$};

% angles labels %{{{1

	%\pic[draw, ->, "$\alpha$", angle radius=0.8cm, angle eccentricity=1.5]
		%{angle = B--A--C};

% right angles markers %{{{2

	%\pic [draw, angle radius=7] {right angle = C--B--A};

% closing %{{{1

\end{tikzpicture}
\end{document}
