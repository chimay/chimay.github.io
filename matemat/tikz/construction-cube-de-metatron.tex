% vim: set ft=tex tabstop=4 shiftwidth=4 noexpandtab:

\input{../../include/latex/tikz/header.tex}

% opening %{{{1

\begin{document}
\begin{tikzpicture}[scale=1.0]

% parameters %{{{1

	\tikzmath{
		\radius = 1.0 ;
		\rotation = 90 ;
		\numsidesfloat = 6.0 ;
		\numsides = int(\numsidesfloat) ;
		int \i ;
		for \i in {1,...,\numsides} {
			\ang{\i} = \rotation + 360 / \numsidesfloat * (\i-1) ;
		};
		\extratio = 1.7 ;
		\extradius = \extratio * \radius ;
	}

% coordinates %{{{1

	\coordinate (O) at (0,0);

	\foreach \i in {1,...,\numsides} {
		\coordinate (P_\i) at (\ang{\i}:\radius);
		\coordinate (D_\i) at (\ang{\i}:{2*\radius});
		\coordinate (E_\i) at (\ang{\i}:{3*\radius});
		\coordinate (F_\i) at (\ang{\i}:{4*\radius});
		\coordinate (G_\i) at (\ang{\i}:{5*\radius});
	}

	\coordinate (X) at (0,-\extradius);
	\coordinate (Y) at (0,\extradius);

% points, dots, vertices %{{{1

	\fill (O) circle (0.4mm);

	\foreach \i in {1,...,\numsides} {
		\fill (D_\i) circle (0.4mm);
		\fill (F_\i) circle (0.4mm);
	}

% segments, sides, lines %{{{1

	\draw (F_1) -- (F_4) ;
	\draw (F_2) -- (F_5) ;
	\draw (F_3) -- (F_6) ;

	\draw (F_1) \foreach \i in {2,...,\numsides} { -- (F_\i) } -- cycle;

	\draw (D_1) \foreach \i in {2,...,\numsides} { -- (D_\i) } -- cycle;

	\foreach \i
		[
			evaluate=\i as \j using { int(mod(\i - 1 + 2, \numsides) + 1) }
		]
		in {1,...,\numsides}
	{
		\draw (F_\i) -- (F_\j);
		\draw (D_\i) -- (D_\j);
	}

	\foreach \i
		[
			evaluate=\i as \j using { int(mod(\i - 1 + 2, \numsides) + 1) },
			evaluate=\i as \k using { int(mod(\i - 1 + 4, \numsides) + 1) }
		]
		in {1,...,\numsides}
	{
		\draw (F_\i) -- (D_\j);
		\draw (F_\i) -- (D_\k);
	}

% circles %{{{1

	\draw (O) circle (\radius);

	\foreach \i in {1,...,\numsides} {
		\draw (D_\i) circle (\radius);
		\draw (F_\i) circle (\radius);
	}

% circular arcs %{{{2

	%\draw (P_1) ++(-100:\radius) arc (-100:100:\radius);
	%\draw (P_4) ++(80:\radius) arc (80:280:\radius);

% points, dots, vertices labels %{{{1

	%\node[anchor=south east] at ($ (O) + (180:4pt) $) {$O$};

% segments, sides, lines labels %{{{1

	%\node[left] at ($ (P_1)!0.3!(P_4) $) {$d$};

% segments, sides, lines marks %{{{1

	%\tkzMarkSegments[mark=||, size=2pt](O,P_1);
	%\tkzMarkSegments[mark=||, size=2pt](O,P_4);

% circles labels %{{{1

	%\node at ($ (O) + (60:\radius + 0.3) $) {$\mathscr{C}$};

% circular arcs labels %{{{2

	%\node at ($ (O) + (45:\radius + 0.3) $) {$\mathscr{C}$};

% angles labels %{{{1

% right angles markers %{{{2

	%\pic [draw, angle radius=7pt, angle eccentricity=1]
	%{right angle = C--B--A};

% closing %{{{1

\end{tikzpicture}
\end{document}
