% vim: set ft=tex tabstop=4 shiftwidth=4 noexpandtab:

\input{../../include/latex/tikz/header.tex}

% opening %{{{1

\begin{document}
\begin{tikzpicture}[scale=1.0]

% parameters %{{{1

	\tikzmath {
		\unit = 6.0 ;
		\numunits = int(5);
		\numunitsminus = \numunits - 1;
		\sqrtnumunitsplus = sqrt(\numunits + 1)*\unit ;
		int \i ;
		for \i in {1,...,\numunits} {
			\len{\i} = sqrt(\i)*\unit;
		};
		for \i in {1,...,\numunits} {
			\ang{\i} = atan2(\unit, \len{\i});
		};
	}

% coordinates %{{{1

	\coordinate (A) at (0,0);
	\coordinate (B) at (0,\unit);

	\coordinate (u) at (1,0);
	\coordinate (v) at (0,1);

	\foreach \i in {1,...,\numunits}
		\coordinate (P\i) at ($(A) + {\len{\i}}*(u) $);

	\foreach \i in {1,...,\numunits}
		\coordinate (T\i) at ($(B) + {\len{\i}}*(u) $);

	\coordinate (PX) at (A);
	\coordinate (PY) at ($ (A) + \sqrtnumunitsplus*(u) $);

	\coordinate (TX) at (B);
	\coordinate (TY) at ($ (B) + \sqrtnumunitsplus*(u) $);

	\coordinate (BX) at (A);
	\coordinate (BY) at ($ (A) + 1.1*\unit*(v) $);

% points, dots, vertices %{{{1

	\fill (A) circle (0.4mm);
	\fill (B) circle (0.4mm);

	\foreach \i in {1,...,\numunits}
		\fill (P\i) circle (0.4mm);

	\foreach \i in {1,...,\numunits}
		\fill (T\i) circle (0.4mm);

% segments, sides, lines %{{{1

	\draw (BX) -- (BY);
	\draw (PX) -- (PY);
	\draw (TX) -- (TY);

	\foreach \i in {1,...,\numunits}
		\draw (P\i) -- (T\i);

	\draw[dashed] (A) -- (T1);

	%\foreach \i in {1,...,\numunits}
		%\draw[dashdotted] (A) -- (T\i);

% circles %{{{1

% circular arcs %{{{2

	\draw (P2) arc (0:{\ang{1}}:{\len{2}});
	\draw (P3) arc (0:{\ang{2}}:{\len{3}});
	\draw (P4) arc (0:{\ang{3}}:{\len{4}});
	\draw (P5) arc (0:{\ang{4}}:{\len{5}});

% points, dots, vertices labels %{{{1

	\node[below left] at (A) {$A$};
	\node[left] at (B) {$B$};

	\foreach \i in {1,...,\numunits}
		\node[below] at (P\i) {$P_\i$};

	\foreach \i in {1,...,\numunits}
		\node[above] at (T\i) {$T_\i$};

% segments, sides, lines labels %{{{1

	%\node[above] at ($ (A)!0.5!(B) $) {$d$};

% segments, sides, lines marks %{{{1

	\tkzMarkSegments[mark=||, size=2pt](A,B)
	\tkzMarkSegments[mark=||, size=2pt](A,P1)
	\tkzMarkSegments[mark=||, size=2pt](B,T1)
	\tkzMarkSegments[mark=||, size=2pt](P1,T1)

% circles labels %{{{1

% circular arcs labels %{{{2

	\node at (30:\len{2} + 0.3) {$\mathscr{C}_1$};
	\node at (25:\len{3} + 0.3) {$\mathscr{C}_2$};
	\node at (23:\len{4} + 0.3) {$\mathscr{C}_3$};
	\node at (21:\len{5} + 0.3) {$\mathscr{C}_4$};

% angles labels %{{{1

	%\pic[draw, ->, "$\alpha$", angle radius=0.8cm, angle eccentricity=1.5]
	%{angle = B--A--C};

% right angles markers %{{{2

	\pic [draw, angle radius=7pt, angle eccentricity=1]
	{right angle = PY--A--B};

	\pic [draw, angle radius=7pt, angle eccentricity=1]
	{right angle = A--B--TY};

	\foreach \i in {1,...,\numunits} {
		\pic [draw, angle radius=7pt, angle eccentricity=1]
		{right angle = T\i--P\i--A};
		\pic [draw, angle radius=7pt, angle eccentricity=1]
		{right angle = B--T\i--P\i};
	}

% closing %{{{1

\end{tikzpicture}
\end{document}
