% vim: set ft=tex :

% documentation %{{{1

% units %{{{2

% default unit for plain numbers is 1 cm
%
% conversion :
%
% mm ≈ pt × 0.35
%
% scaling :
%
% - unitless coordinates scale
% - explicit units do not scale
%
% if you want explicit units to scale, you have to add transform shape :
%
% \begin{tikzpicture}[scale=2, transform shape]
% ...
% \end{tikzpicture}

% styles %{{{2

% ---- defining styles
%
% \tikzset{
%   main line/.style={line width=0.8pt},
% }
%
% -- using styles
%
% \draw[main line] (A)--(B);

% header %{{{1

\documentclass[tikz, border=1mm]{standalone}

% packages and libraries %{{{2

% ---- not necessary since the documentclass[tikz ...] requires it
% \usepackage{tikz}

\usepackage{amsmath}

\usetikzlibrary{calc,angles,quotes,intersections,shapes.geometric}

\usepackage{tkz-euclide}

% style %{{{2

\tikzset{
	% ---- default
	every path/.style={line width=0.3pt},
	every coordinate/.style={fill=black, circle, inner sep=1pt},
	every node/.style={font=\normalsize},
	every picture/.style={scale=1.0},
	% ---- custom
	construction/.style={line width=0.1pt, dashed},
	dimension/.style={line width=0.2pt, <->},
	dimension extension/.style={line width=0.2pt, dashed}
}

% document %{{{1

\begin{document}
\begin{tikzpicture}[scale=1.0]

	% ---- parameters

	\def\length{6}
	\def\radius{0.7*\length}

	% ---- coordinates

	\coordinate (A) at (-\length/2,0);
	\coordinate (B) at (\length/2,0);

	\coordinate (Ra) at ($(A) + (\radius, 0)$);
	\coordinate (Rb) at ($(B) + (\radius, 0)$);

	% ---- intersections

	\tkzInterCC(A,Ra)(B,Rb)\tkzGetPoints{D}{E}

	% ---- points for dimension lines

	%\coordinate (IdJ) at ($(I) + (0,0.7)$);

	% ---- segments

	\draw (A) -- (B);
	\draw (D) -- (E);

	% ---- circle

	%\draw (O) circle (\radius);

	% ---- circular arc

	\draw (A) ++(-90:\radius) arc (-90:90:\radius);
	\draw (B) ++(90:\radius) arc (90:270:\radius);

	% ---- polygon

	%\draw (P1) \foreach \i in {2,...,\numsides} { -- (P\i) } -- cycle;

	% ---- radiuses

	%\draw (O) -- (P1);

	% ---- dimension lines

	%\draw[dimension] (IdJ) -- (JdI);
	%\draw[dimension extension] (I) -- (IdJ);
	%\draw[dimension extension] (J) -- (JdI);

	% ---- thick vertices

	\fill (A) circle (0.3mm);
	\fill (B) circle (0.3mm);

	% ---- vertices labels

	\node[below] at (A) {$A$};
	\node[below] at (B) {$B$};

	% ---- segments labels

	%\node[above] at ($(G)!0.5!(I)$) {$b$};

	% ---- angles labels

	%\pic[draw, ->, "$60^\circ$", angle radius=0.5cm, angle eccentricity=1.5]
	%{angle = H--O--I};

	% ---- right angles labels

	%\pic [draw, angle radius=7pt, angle eccentricity=1]
	%{right angle = C--B--A};

\end{tikzpicture}
\end{document}
