% vim: set ft=tex tabstop=4 shiftwidth=4 noexpandtab:

\input{../../include/latex/tikz/header.tex}

% opening %{{{1

\begin{document}
\begin{tikzpicture}[scale=1.0]

% parameters %{{{1

	\def\len{6}
	\def\radius{0.65*\len}

% coordinates %{{{1

	\coordinate (A) at (0,0);
	\coordinate (B) at (\len,0);

	\coordinate (Ra) at ($(A) + (\radius, 0)$);
	\coordinate (Rb) at ($(B) + (\radius, 0)$);

% intersections %{{{1

	\tkzInterCC(A,Ra)(B,Rb)\tkzGetPoints{D}{E}

	\coordinate (X) at ($(E) - (0, 2)$);
	\coordinate (Y) at ($(D) + (0, 2)$);

	\tkzInterLL(A,B)(D,E)\tkzGetPoint{F}

% points, dots, vertices %{{{1

	\fill (A) circle (0.5mm);
	\fill (B) circle (0.5mm);
	\fill (D) circle (0.5mm);
	\fill (E) circle (0.5mm);
	\fill (F) circle (0.5mm);

% segments %{{{1

	\draw (A) -- (B);
	\draw (X) -- (Y);

	\draw[dashed] (A) -- (D);
	\draw[dashed] (B) -- (D);

	\draw[dashed] (A) -- (E);
	\draw[dashed] (B) -- (E);

% circular arcs %{{{1

	\draw (A) ++(-65:\radius) arc (-65:65:\radius);
	\draw (B) ++(115:\radius) arc (115:245:\radius);

% points, dots, vertices labels %{{{1

	\node[below] at (A) {$A$};
	\node[below] at (B) {$B$};
	\node[label={[label distance=0.5]left:$D$}] at (D) {};
	\node[label={[label distance=0.5]left:$E$}] at (E) {};

% segments, sides labels %{{{1

	\node[left] at ($ (X)!0.95!(Y) $) {$m$};

% segments, sides marks %{{{1

	\tkzMarkSegments[mark=||, size=2pt](A,F)
	\tkzMarkSegments[mark=||, size=2pt](F,B)

% circular arcs labels %{{{1

	\node at ($(10:{\radius+0.3}) + (0, 0)$) {$\mathscr{C}_1$};
	\node at ($(170:{\radius+0.3}) + (\len, 0)$) {$\mathscr{C}_2$};

% angles labels %{{{1

% right angles markers %{{{2

	\pic [draw, angle radius=7pt, angle eccentricity=1]
	{right angle = B--F--Y};

% segments, sides marks %{{{1

	\tkzMarkSegments[mark=||, size=2pt](A,D)
	\tkzMarkSegments[mark=||, size=2pt](A,E)
	\tkzMarkSegments[mark=||, size=2pt](B,D)
	\tkzMarkSegments[mark=||, size=2pt](B,E)

% closing %{{{1

\end{tikzpicture}
\end{document}
