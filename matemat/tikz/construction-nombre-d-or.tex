% vim: set ft=tex tabstop=4 shiftwidth=4 noexpandtab:

\input{../../include/latex/tikz/header.tex}

% opening %{{{1

\begin{document}
\begin{tikzpicture}[scale=1.0]

% parameters %{{{1

	\tikzmath{
		\len = 9 ;
		\unit = 4 ;
		\x1 = \unit / 2 ;
		\y1 = 0 ;
		\x2 = 0 ;
		\y2 = \unit ;
		\dx = \x2 - \x1 ;
		\dy = \y2 - \y1 ;
		\radius = veclen(\dx,\dy) ;
		\ang = -10 ;
		\gle = 130 ;
	}

% coordinates %{{{1

	\coordinate (A) at (0,0);
	\coordinate (B) at ({\unit/2},0);
	\coordinate (C) at (0,\unit);

	\coordinate (HX) at (-\len,0);
	\coordinate (HY) at (\len,0);

	%\coordinate (VX) at (0,-\len);
	%\coordinate (VY) at (0,\len);

% intersections %{{{1

	\begin{pgfinterruptboundingbox}
		\path[name path=line] (HX) -- (HY);
		\path[name path=circle] (B) circle (\radius);
		\path[name intersections={of=line and circle, by={I,J}}];
	\end{pgfinterruptboundingbox}

	 %\begin{pgfinterruptboundingbox}
		 %\path[name path=line] (VX) -- (VY);
		 %\path[name path=circle] (B) circle (\radius);
		 %\path[name intersections={of=line and circle, by={K,L}}];
	 %\end{pgfinterruptboundingbox}

% distance %{{{1

	%\path let \p1 = ($ (B) - (A) $)
	%in \pgfextra{
		%\pgfmathsetmacro{\distAB}{veclen(\x1,\y1)/1cm}
		%\global\let\distAB\distAB
	%};

% points, dots, vertices %{{{1

	\fill (A) circle (0.4mm);
	\fill (B) circle (0.4mm);
	\fill (C) circle (0.4mm);
	\fill (I) circle (0.4mm);

% segments, sides, lines %{{{1

	\draw (A) -- (B) -- (C) -- cycle;

	\draw (B) -- (I);
	\draw[dashed] (I) -- (HY);

% circles %{{{1

	%\draw (B) circle (\radius);

	%\path[draw] let \p1 = ($ (A) - (O) $)
	%in (O) circle ({veclen(\x1,\y1)});

% circular arcs %{{{2

	\draw (B) ++(\ang:\radius) arc (\ang:\gle:\radius);

% points, dots, vertices labels %{{{1

	\node[below] at (A) {$A$};
	\node[below] at (B) {$B$};
	\node[above] at (C) {$C$};
	\node[below right] at (I) {$I$};

% segments, sides, lines labels %{{{1

	\node[below] at ($ (A)!0.5!(B) $) {$1/2$};
	\node[left] at ($ (A)!0.5!(C) $) {$1$};
	\node[above right] at ($ (B)!0.5!(C) $) {$\sqrt{5}/2$};
	\node[below] at ($ (B)!0.5!(I) $) {$\sqrt{5}/2$};

% segments, sides, lines marks %{{{1

	\tkzMarkSegments[mark=||, size=2pt](B,C)
	\tkzMarkSegments[mark=||, size=2pt](B,I)

% circles labels %{{{1

	\node at ($ (B) + (45:\radius + 0.3) $) {$\mathscr{C}$};

% circular arcs labels %{{{2

	%\node at ($ (O) + (45:\radius + 0.3) $) {$\mathscr{C}$};

% angles labels %{{{1

	%\pic[draw, ->, "$\alpha$", angle radius=0.8cm, angle eccentricity=1.5]
	%{angle = B--A--C};

% right angles markers %{{{2

	\pic [draw, angle radius=7pt, angle eccentricity=1]
	{right angle = B--A--C};

% closing %{{{1

\end{tikzpicture}
\end{document}
