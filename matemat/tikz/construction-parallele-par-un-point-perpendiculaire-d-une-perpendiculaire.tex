% vim: set ft=tex tabstop=4 shiftwidth=4 noexpandtab:

\input{../../include/latex/tikz/header.tex}

% opening %{{{1

\begin{document}
\begin{tikzpicture}[scale=1.0]

% parameters %{{{1

	\tikzmath{
		\len = 6;
		\height = 6;
		\extratio = 1.5;
		\radiusDE = 0.65*\len;
		\radiusA = 0.35*\len;
		\radiusIJ = 0.6*\len;
		\distPA = sqrt(\len*\len/4 + \height*\height);
	}

% coordinates %{{{1

	\coordinate (D) at (0,0);
	\coordinate (E) at (\len,0);

	\coordinate (U) at ({-(\extratio - 1)*\len},0);
	\coordinate (V) at ({\extratio*\len},0);

	\coordinate (A) at ({\len/2},\height);

% intersections %{{{1

	\begin{pgfinterruptboundingbox}
		\path[name path=circleA] (D) circle (\radiusDE);
		\path[name path=circleB] (E) circle (\radiusDE);
		\path[name intersections={of=circleA and circleB, by={F,G}}];
	\end{pgfinterruptboundingbox}

	\coordinate (X) at ($(G) - (0, 2)$);
	\coordinate (Y) at ($(F) + (0, 6)$);

	\tkzInterLL(D,E)(F,G)\tkzGetPoint{H}

	\begin{pgfinterruptboundingbox}
		\path[name path=line] (X) -- (Y);
		\path[name path=circle] (A) circle (\radiusA);
		\path[name intersections={of=line and circle, by={I,J}}];
	\end{pgfinterruptboundingbox}

	\begin{pgfinterruptboundingbox}
		\path[name path=circleG] (I) circle (\radiusIJ);
		\path[name path=circleH] (J) circle (\radiusIJ);
		\path[name intersections={of=circleG and circleH, by={K,L}}];
	\end{pgfinterruptboundingbox}

	\coordinate (W) at ($ (K) - ({(\extratio - 1)*\len}, 0) $);
	\coordinate (Z) at ($ (L) + ({(\extratio - 1)*\len}, 0) $);

% points, dots, vertices %{{{1

	\fill (D) circle (0.5mm);
	\fill (E) circle (0.5mm);

	\fill (A) circle (0.5mm);

	\fill (F) circle (0.5mm);
	\fill (G) circle (0.5mm);
	\fill (H) circle (0.5mm);

	\fill (I) circle (0.5mm);
	\fill (J) circle (0.5mm);

	\fill (K) circle (0.5mm);
	\fill (L) circle (0.5mm);

% segments %{{{1

	\draw (U) -- (V);

	\draw (X) -- (Y);

	\draw (W) -- (Z);

% circles %{{{1

	\draw (A) circle (\radiusA);

% circular arcs %{{{2

	\draw (A) ++(200:\distPA) arc (200:340:\distPA);

	\draw (D) ++(-65:\radiusDE) arc (-65:65:\radiusDE);
	\draw (E) ++(115:\radiusDE) arc (115:245:\radiusDE);

	\draw (I) ++(200:\radiusIJ) arc (200:340:\radiusIJ);
	\draw (J) ++(20:\radiusIJ) arc (20:160:\radiusIJ);

% points, dots, vertices labels %{{{1

	\node[below] at (D) {$D$};
	\node[below] at (E) {$E$};

	\node[above left] at (A) {$A$};

	\node[label={[label distance=0.5]left:$F$}] at (F) {};
	\node[label={[label distance=0.5]left:$G$}] at (G) {};

	\node[above left] at (H) {$H$};

	\node[above left] at (I) {$I$};
	\node[below left] at (J) {$J$};

	\node[above] at (K) {$K$};
	\node[above] at (L) {$L$};

% segments, sides labels %{{{1

	\node[above] at ($ (U)!0.05!(V) $) {$d$};
	\node[left] at ($ (X)!0.05!(Y) $) {$m$};
	\node[above] at ($ (W)!0.05!(Z) $) {$p$};

% segments, sides marks %{{{1

	\tkzMarkSegments[mark=||, size=2pt](D,H)
	\tkzMarkSegments[mark=||, size=2pt](H,E)

	\tkzMarkSegments[mark=|, size=2pt](I,A)
	\tkzMarkSegments[mark=|, size=2pt](A,J)

% circular arcs labels %{{{1

	\node at ($ (A) + (320:{\distPA+0.3})$) {$\mathscr{C}_1$};
	\node at ($ (D) + (20:{\radiusDE+0.3}) $) {$\mathscr{C}_2$};
	\node at ($ (E) + (160:{\radiusDE+0.3}) $) {$\mathscr{C}_3$};
	\node at ($ (A) + (45:{\radiusA+0.3}) $) {$\mathscr{C}_4$};
	\node at ($ (I) + (-50:{\radiusIJ+0.3}) $) {$\mathscr{C}_5$};
	\node at ($ (J) + (50:{\radiusIJ+0.3}) $) {$\mathscr{C}_6$};

% circles labels %{{{1

	%\node at ($ (O) + (45:\radius+0.3) $) {$\mathscr{C}$};

% circular arcs labels %{{{2

	%\node at ($ (D) + {\radiusA + 0.2}*(u) $) {$\mathscr{C}$};

% angles labels %{{{1

% right angles markers %{{{2

	\pic [draw, angle radius=7pt, angle eccentricity=1]
	{right angle = E--F--Y};

	\pic [draw, angle radius=7pt, angle eccentricity=1]
	{right angle = Z--A--I};

% closing %{{{1

\end{tikzpicture}
\end{document}
