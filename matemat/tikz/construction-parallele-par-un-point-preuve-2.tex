% vim: set ft=tex tabstop=4 shiftwidth=4 noexpandtab:

\input{../../include/latex/tikz/header.tex}

% opening %{{{1

\begin{document}
\begin{tikzpicture}[scale=1.0]

% parameters %{{{1

	\def\angA{60}
	\def\gleA{150}

	\def\angB{30}
	\def\gleB{120}

	\tikzmath {
		\x1 = 0 ;
		\y1 = 0 ;
		\x2 = 2 ;
		\y2 = -2 ;
		\dx = \x2 - \x1 ;
		\dy = \y2 - \y1 ;
		\dist = veclen(\dx,\dy) ;
	}

% coordinates %{{{1

	\coordinate (A) at (\x1,\y1);

	\coordinate (O) at (\x2,\y2);

	\coordinate (X) at (-3,-3);
	\coordinate (Y) at (7,-3);

% intersections %{{{1

	\begin{pgfinterruptboundingbox}
		\path[name path=line] (X) -- (Y);
		\path[name path=circle] let \p1 = ($(A)-(O)$)
		in (O) circle ({veclen(\x1,\y1)});
		\path[name intersections={of=line and circle, by={I,J}}];
	\end{pgfinterruptboundingbox}

	\begin{pgfinterruptboundingbox}
		\path[name path=circleA] let \p1 = ($(A)-(O)$)
		in (O) circle ({veclen(\x1,\y1)});
		\path[name path=circleB] let \p1 = ($(A)-(I)$)
		in (J) circle ({veclen(\x1,\y1)});
		\path[name intersections={of=circleA and circleB, by={K,L}}];
	\end{pgfinterruptboundingbox}

% extensions %{{{1

	\coordinate (PX) at ($ (A) - (3,0) $);
	\coordinate (PY) at ($ (K) + (3,0) $);

% distance %{{{1

	\path let \p1 = ($(A)-(O)$)
	in
	\pgfextra{
		\pgfmathsetmacro{\distAO}{veclen(\x1,\y1)/28.45}
		\global\let\distAO\distAO
	};

	\path let \p1 = ($(A)-(I)$)
	in
	\pgfextra{
		\pgfmathsetmacro{\distAI}{veclen(\x1,\y1)/28.45}
		\global\let\distAI\distAI
	};

% points, dots, vertices %{{{1

	\fill (A) circle (0.4mm);
	\fill (O) circle (0.4mm);
	\fill (I) circle (0.4mm);
	\fill (J) circle (0.4mm);
	\fill (K) circle (0.4mm);

% segments, sides, lines %{{{1

	\draw (X) -- (Y);

	\draw (PX) -- (PY);

	\draw[dashed] (O) -- (A);
	\draw[dashed] (O) -- (I);
	\draw[dashed] (O) -- (J);
	\draw[dashed] (O) -- (K);

	\draw[dashed] (A) -- (I);
	\draw[dashed] (J) -- (K);

% circles %{{{1

	\path[draw] let \p1 = ($(A)-(O)$)
	in (O) circle ({veclen(\x1,\y1)});

% circular arcs %{{{2

	%\path[draw] let
		%\p1 = ($(A)-(I)$),
		%\n1 = {veclen(\x1,\y1)}
	%in (I) ++(\angA:\n1) arc (\angA:\gleA:\n1);

	\path[draw] let
		\p1 = ($(A)-(I)$),
		\n1 = {veclen(\x1,\y1)}
	in (J) ++(\angB:\n1) arc (\angB:\gleB:\n1);

% points, dots, vertices labels %{{{1

	\node[above left] at (A) {$A$};
	\node[above] at (O) {$O$};
	\node[below left] at (I) {$I$};
	\node[below right] at (J) {$J$};
	\node[above right] at (K) {$K$};

% segments, sides, lines labels %{{{1

	\node[above] at ($ (X)!0.1!(Y) $) {$d$};
	\node[above] at ($ (PX)!0.1!(PY) $) {$p$};

% segments, sides, lines marks %{{{1

	\tkzMarkSegments[mark=|, size=2pt](A,I)
	\tkzMarkSegments[mark=|, size=2pt](J,K)

	\tkzMarkSegments[mark=||, size=2pt](O,A)
	\tkzMarkSegments[mark=||, size=2pt](O,I)
	\tkzMarkSegments[mark=||, size=2pt](O,J)
	\tkzMarkSegments[mark=||, size=2pt](O,K)

% circles labels %{{{1

	%\node at ($ (O) + (15:{\dist + 0.3}) $) {$\mathscr{C}_1$};

	\node at ($ (O) + (15:{\distAO + 0.3}) $) {$\mathscr{C}_1$};

% circular arcs labels %{{{2

	\node at ($ (J) + {\distAI + 0.3}*(45:1) $) {$\mathscr{C}_2$};

% angles labels %{{{1

	%\pic[draw, ->, "$\alpha$", angle radius=0.8cm, angle eccentricity=1.5]
	%{angle = B--A--C};

% right angles markers %{{{2

	%\pic [draw, angle radius=7pt, angle eccentricity=1]
	%{right angle = C--B--A};

% closing %{{{1

\end{tikzpicture}
\end{document}
