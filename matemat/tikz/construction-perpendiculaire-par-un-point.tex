% vim: set ft=tex tabstop=4 shiftwidth=4 noexpandtab:

\input{../../include/latex/tikz/header.tex}

% opening %{{{1

\begin{document}
\begin{tikzpicture}[scale=1.0]

% parameters %{{{1

	\tikzmath{
		\len = 6;
		\height = 4;
		\extratio = 1.5;
		\radius = 0.65*\len ;
		\distTA = sqrt(\len*\len/4 + \height*\height);
	}

% coordinates %{{{1

	\coordinate (A) at (0,0);
	\coordinate (B) at (\len,0);

	\coordinate (U) at ({-(\extratio - 1)*\len},0);
	\coordinate (V) at ({\extratio*\len},0);

	\coordinate (T) at ({\len/2},\height);

% intersections %{{{1

	\begin{pgfinterruptboundingbox}
		\path[name path=circleA] (A) circle (\radius);
		\path[name path=circleB] (B) circle (\radius);
		\path[name intersections={of=circleA and circleB, by={D,E}}];
	\end{pgfinterruptboundingbox}

	\coordinate (X) at ($(E) - (0, 2)$);
	\coordinate (Y) at ($(D) + (0, 2)$);

	\tkzInterLL(A,B)(D,E)\tkzGetPoint{F}

% points, dots, vertices %{{{1

	\fill (A) circle (0.5mm);
	\fill (B) circle (0.5mm);

	\fill (T) circle (0.5mm);

	\fill (D) circle (0.5mm);
	\fill (E) circle (0.5mm);
	\fill (F) circle (0.5mm);

% segments %{{{1

	\draw (U) -- (V);

	\draw (X) -- (Y);

	%\draw[dashed] (A) -- (D);
	%\draw[dashed] (B) -- (D);

	\draw[dashed] (A) -- (E);
	\draw[dashed] (B) -- (E);

	\draw[dashdotdotted] (A) -- (T);
	\draw[dashdotdotted] (B) -- (T);

% circular arcs %{{{1

	\draw (T) ++(200:\distTA) arc (200:340:\distTA);
	\draw (A) ++(-65:\radius) arc (-65:65:\radius);
	\draw (B) ++(115:\radius) arc (115:245:\radius);

% points, dots, vertices labels %{{{1

	\node[below] at (A) {$A$};
	\node[below] at (B) {$B$};

	\node[above left] at (T) {$T$};

	\node[label={[label distance=0.5]left:$D$}] at (D) {};
	\node[label={[label distance=0.5]left:$E$}] at (E) {};

	\node[above left] at (F) {$F$};

% segments, sides labels %{{{1

	\node[above] at ($ (U)!0.05!(V) $) {$d$};
	\node[left] at ($ (X)!0.05!(Y) $) {$p$};

% segments, sides marks %{{{1

	\tkzMarkSegments[mark=||, size=2pt](A,E)
	\tkzMarkSegments[mark=||, size=2pt](B,E)

	\tkzMarkSegments[mark=|, size=2pt](T,A)
	\tkzMarkSegments[mark=|, size=2pt](T,B)

% circular arcs labels %{{{1

	\node at ($ (T) + (320:{\distTA+0.3})$) {$\mathscr{C}_1$};
	\node at ($ (A) + (10:{\radius+0.3}) $) {$\mathscr{C}_2$};
	\node at ($ (B) + (170:{\radius+0.3}) $) {$\mathscr{C}_3$};

% angles labels %{{{1

% right angles markers %{{{2

	\pic [draw, angle radius=7pt, angle eccentricity=1]
	{right angle = B--F--Y};

% closing %{{{1

\end{tikzpicture}
\end{document}
