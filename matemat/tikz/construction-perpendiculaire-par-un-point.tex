% vim: set ft=tex tabstop=4 shiftwidth=4 noexpandtab:

\input{../../include/latex/tikz/header.tex}

% opening %{{{1

\begin{document}
\begin{tikzpicture}[scale=1.0]

% parameters %{{{1

	\tikzmath{
		\len = 6;
		\height = 4;
		\extratio = 1.5;
		\radius = 0.65*\len ;
		\distAI = sqrt(\len*\len/4 + \height*\height);
	}

% coordinates %{{{1

	\coordinate (I) at (0,0);
	\coordinate (J) at (\len,0);

	\coordinate (U) at ({-(\extratio - 1)*\len},0);
	\coordinate (V) at ({\extratio*\len},0);

	\coordinate (A) at ({\len/2},\height);

% intersections %{{{1

	\begin{pgfinterruptboundingbox}
		\path[name path=circleA] (I) circle (\radius);
		\path[name path=circleB] (J) circle (\radius);
		\path[name intersections={of=circleA and circleB, by={K,L}}];
	\end{pgfinterruptboundingbox}

	\coordinate (X) at ($(L) - (0, 2)$);
	\coordinate (Y) at ($(K) + (0, 2)$);

	\tkzInterLL(I,J)(K,L)\tkzGetPoint{M}

% points, dots, vertices %{{{1

	\fill (I) circle (0.5mm);
	\fill (J) circle (0.5mm);

	\fill (A) circle (0.5mm);

	\fill (K) circle (0.5mm);
	\fill (L) circle (0.5mm);
	\fill (M) circle (0.5mm);

% segments %{{{1

	\draw (U) -- (V);

	\draw (X) -- (Y);

	%\draw[dashed] (I) -- (K);
	%\draw[dashed] (J) -- (K);

	\draw[dashed] (I) -- (L);
	\draw[dashed] (J) -- (L);

	\draw[dashdotdotted] (I) -- (A);
	\draw[dashdotdotted] (J) -- (A);

% circular arcs %{{{1

	\draw (A) ++(200:\distAI) arc (200:340:\distAI);
	\draw (I) ++(-65:\radius) arc (-65:65:\radius);
	\draw (J) ++(115:\radius) arc (115:245:\radius);

% points, dots, vertices labels %{{{1

	\node[below] at (I) {$I$};
	\node[below] at (J) {$J$};

	\node[above left] at (A) {$A$};

	\node[label={[label distance=0.5]left:$K$}] at (K) {};
	\node[label={[label distance=0.5]left:$L$}] at (L) {};

	\node[above left] at (M) {$M$};

% segments, sides labels %{{{1

	\node[above] at ($ (U)!0.05!(V) $) {$d$};
	\node[left] at ($ (X)!0.05!(Y) $) {$p$};

% segments, sides marks %{{{1

	\tkzMarkSegments[mark=||, size=2pt](I,L)
	\tkzMarkSegments[mark=||, size=2pt](J,L)

	\tkzMarkSegments[mark=|, size=2pt](A,I)
	\tkzMarkSegments[mark=|, size=2pt](A,J)

% circular arcs labels %{{{1

	\node at ($ (A) + (320:{\distAI+0.3})$) {$\mathscr{C}_1$};
	\node at ($ (I) + (10:{\radius+0.3}) $) {$\mathscr{C}_2$};
	\node at ($ (J) + (170:{\radius+0.3}) $) {$\mathscr{C}_3$};

% angles labels %{{{1

% right angles markers %{{{2

	\pic [draw, angle radius=7pt, angle eccentricity=1]
	{right angle = J--M--Y};

% closing %{{{1

\end{tikzpicture}
\end{document}
