% vim: set ft=tex tabstop=4 shiftwidth=4 noexpandtab:

\input{../../include/latex/tikz/header.tex}

% opening %{{{1

\begin{document}
\begin{tikzpicture}[scale=1.0]

% parameters %{{{1

	\def\radius{3}
	\def\extratio{0.5}

% coordinates %{{{1

	\coordinate (O) at (0,0);
	\coordinate (A) at (0:6.5);
	\coordinate (M) at ($ (O)!0.5!(A) $);

% intersections %{{{1

	\begin{pgfinterruptboundingbox}
		\path[name path=circleO] (O) circle (\radius);
		\path[name path=circleM]
		let
			\p1 = ($ (A) - (M) $)
		in (M) circle ({veclen(\x1,\y1)});
		\path[name intersections={of=circleO and circleM, by={I,J}}];
	\end{pgfinterruptboundingbox}

	\coordinate (IA) at ($ (A) - (I) $);
	\coordinate (JA) at ($ (A) - (J) $);

	\coordinate (XI) at ($ (I) - \extratio*(IA) $);
	\coordinate (YI) at ($ (A) + \extratio*(IA) $);

	\coordinate (XJ) at ($ (J) - \extratio*(JA) $);
	\coordinate (YJ) at ($ (A) + \extratio*(JA) $);

% distance %{{{1

	\path let \p1 = ($ (A) - (M) $)
	in \pgfextra{
		\pgfmathsetmacro{\distAM}{veclen(\x1,\y1)/1cm}
		\global\let\distAM\distAM
	};

% points, dots, vertices %{{{1

	\fill (O) circle (0.4mm);
	\fill (A) circle (0.4mm);
	\fill (M) circle (0.4mm);
	\fill (I) circle (0.4mm);
	\fill (J) circle (0.4mm);

% segments, sides, lines %{{{1

	\draw (O) -- (A);
	\draw[dashed] (O) -- (I);
	\draw[dashed] (O) -- (J);
	\draw (XI) -- (YI);
	\draw (XJ) -- (YJ);

% circles %{{{1

	\draw (O) circle (\radius);

	\path[draw] let \p1 = ($(A)-(M)$)
	in (M) circle ({veclen(\x1,\y1)});

% circular arcs %{{{2

	%\draw (A) ++(\ang:\radius) arc (\ang:\gle:\radius);

% points, dots, vertices labels %{{{1

	\node[above left] at (O) {$O$};
	\node[right] at (A) {$A$};
	\node[below] at (M) {$M$};
	\node[above] at (I) {$I$};
	\node[below] at (J) {$J$};

% segments, sides, lines labels %{{{1

	\node[above] at ($ (XI)!0.9!(YI) $) {$t$};
	\node[above] at ($ (XJ)!0.9!(YJ) $) {$u$};

% segments, sides, lines marks %{{{1

	\tkzMarkSegments[mark=|, size=2pt](O,I)
	\tkzMarkSegments[mark=|, size=2pt](O,J)

	\tkzMarkSegments[mark=||, size=2pt](A,M)
	\tkzMarkSegments[mark=||, size=2pt](O,M)

% circles labels %{{{1

	\node at ($ (O) + (135:\radius + 0.3) $) {$\mathscr{C}$};

	\node at ($ (M) + (35:\distAM + 0.3) $) {$\mathscr{L}$};

% circular arcs labels %{{{2

	%\node at ($ (O) + (45:\radius+0.3) $) {$\mathscr{C}$};

% angles labels %{{{1

	%\pic[draw, ->, "$\alpha$", angle radius=0.8cm, angle eccentricity=1.5]
	%{angle = B--A--C};

% right angles markers %{{{2

	\pic [draw, angle radius=7pt, angle eccentricity=1]
	{right angle = O--I--A};

	\pic [draw, angle radius=7pt, angle eccentricity=1]
	{right angle = A--J--O};

% closing %{{{1

\end{tikzpicture}
\end{document}
