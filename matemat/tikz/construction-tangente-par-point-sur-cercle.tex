% vim: set ft=tex tabstop=4 shiftwidth=4 noexpandtab:

\input{../../include/latex/tikz/header.tex}

% opening %{{{1

\begin{document}
\begin{tikzpicture}[scale=1.0]

% parameters %{{{1

	\tikzmath{
		\radius = 3.0 ;
		\rad = 1.7 ;
		\dius = 1.7 * \rad ;
		\extratioA = 1.7 ;
		\extratioKL = 0.8 ;
		\ang = 90 ;
		\ngl = \ang - 100 ;
		\gle = \ang + 100 ;
	}

% coordinates %{{{1

	\coordinate (O) at (0,0);
	\coordinate (A) at (\ang:\radius);
	\coordinate (XA) at ($ \extratioA*(A) $);

% intersections %{{{1

	\begin{pgfinterruptboundingbox}
		\path[name path=line] (O) -- (XA);
		\path[name path=circle] (A) circle (\rad);
		\path[name intersections={of=line and circle, by={J,I}}];
	\end{pgfinterruptboundingbox}

	\begin{pgfinterruptboundingbox}
		\path[name path=circleA] (I) circle (\dius);
		\path[name path=circleB] (J) circle (\dius);
		\path[name intersections={of=circleA and circleB, by={L,K}}];
	\end{pgfinterruptboundingbox}

	\coordinate (KL) at ($ (L) - (K) $);

	\coordinate (XKL) at ($ (K) - \extratioKL*(KL) $);
	\coordinate (YKL) at ($ (L) + \extratioKL*(KL) $);

% points, dots, vertices %{{{1

	\fill (O) circle (0.4mm);
	\fill (A) circle (0.4mm);

	\fill (I) circle (0.4mm);
	\fill (J) circle (0.4mm);

	\fill (K) circle (0.4mm);
	\fill (L) circle (0.4mm);

% segments, sides, lines %{{{1

	\draw (O) -- (XA);
	\draw (XKL) -- (YKL);

% circles %{{{1

	\draw (O) circle (\radius);
	\draw (A) circle ({\rad});

% circular arcs %{{{2

	\draw (I) ++(\ngl:\dius) arc (\ngl:\gle:\dius);
	\draw (J) ++({\ngl + 180}:\dius) arc ({\ngl + 180}:{\gle + 180}:\dius);

% points, dots, vertices labels %{{{1

	\node[above left] at (O) {$O$};
	\node[below left] at (A) {$A$};

	\node[below left] at (I) {$I$};
	\node[above right] at (J) {$J$};

	\node[below left] at (K) {$K$};
	\node[below right] at (L) {$L$};

% segments, sides, lines labels %{{{1

	 \node[above] at ($ (XKL)!0.9!(YKL) $) {$t$};

% segments, sides, lines marks %{{{1

	%\tkzMarkSegments[mark=||, size=2pt](O,I)
	%\tkzMarkSegments[mark=||, size=2pt](O,J)

% circles labels %{{{1

	\node at ($ (O) + (195:\radius + 0.3) $) {$\mathscr{C}$};
	\node at ($ (A) + (105:\rad + 0.3) $) {$\mathscr{C}_1$};
	\node at ($ (I) + (15:\dius + 0.3) $) {$\mathscr{C}_2$};
	\node at ($ (J) + (-15:\dius + 0.3) $) {$\mathscr{C}_3$};

% circular arcs labels %{{{2

	%\node at ($ (O) + (45:\radius+0.3) $) {$\mathscr{C}$};

% angles labels %{{{1

	%\pic[draw, ->, "$\alpha$", angle radius=0.8cm, angle eccentricity=1.5]
	%{angle = B--A--C};

% right angles markers %{{{2

	\pic [draw, angle radius=7pt, angle eccentricity=1]
	{right angle = O--A--L};

% closing %{{{1

\end{tikzpicture}
\end{document}
