% vim: set ft=tex tabstop=4 shiftwidth=4 noexpandtab:

\input{../../include/latex/tikz/header.tex}

% opening %{{{1

\begin{document}
\begin{tikzpicture}[scale=1.2]

% parameters %{{{1

	\def\lenAB{4}
	\def\lenAC{5}
	\def\ext{1.3}

% coordinates %{{{1

	\coordinate (A) at (0,0);
	\coordinate (B) at (\lenAB,0);
	\coordinate (X) at (\lenAB,{\ext*\lenAB});

% intersections %{{{1

	\begin{pgfinterruptboundingbox}
		\path[name path=line] (B) -- (X);
		\path[name path=circle] (A) circle (\lenAC);
		\path[name intersections={of=line and circle, by=C}];
	\end{pgfinterruptboundingbox}

% points, dots, vertices %{{{1

	\fill (A) circle (0.4mm);
	\fill (B) circle (0.4mm);
	\fill (C) circle (0.4mm);

% segments, sides %{{{1

	\draw (A) -- (B) -- (C) -- cycle;
	\draw[dashed] (C) -- (X);

% circles %{{{1

% circular arcs %{{{2

	\draw[dashdotted] (A) ++(10:\lenAC) arc (10:70:\lenAC);

% points, dots, vertices labels %{{{1

	\node[below left] at (A) {$A$};
	\node[below right] at (B) {$B$};
	\node[above right] at (C) {$C$};

% segments, sides labels %{{{1

	\node[left] at ($ (C)!0.7!(X) $) {$d$};

% circular arcs labels %{{{2

	\node at (25:\lenAC+0.3) {$\mathscr{C}$};

% right angles markers %{{{1

	\pic [draw, angle radius=7pt, angle eccentricity=1]
	{right angle = C--B--A};

% closing %{{{1

\end{tikzpicture}
\end{document}
