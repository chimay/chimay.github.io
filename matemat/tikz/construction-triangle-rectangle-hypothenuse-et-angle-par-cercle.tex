% vim: set ft=tex tabstop=4 shiftwidth=4 noexpandtab:

\input{../../include/latex/tikz/header.tex}

% opening %{{{1

\begin{document}
\begin{tikzpicture}[scale=1.2]

% parameters %{{{1

	\def\lenAC{6.5}
	\def\angBAC{35}
	\def\ext{1.2}
	\def\ang{10}
	\def\gle{60}

	\tikzmath{
		\radius = \lenAC/2;
	}

% coordinates %{{{1

	\coordinate (A) at (0,0);
	\coordinate (X) at (\angBAC:{\ext*\lenAC});
	\coordinate (C) at (\lenAC,0);
	\coordinate (O) at (\radius,0);

% intersections %{{{1

	\begin{pgfinterruptboundingbox}
		\path[name path=line] (A) -- (X);
		\path[name path=circle] (O) circle (\radius);
		\path[name intersections={of=line and circle, by=B}];
	\end{pgfinterruptboundingbox}

% points, dots, vertices %{{{1

	\fill (A) circle (0.4mm);
	\fill (C) circle (0.4mm);
	\fill (O) circle (0.4mm);
	\fill (B) circle (0.4mm);

% segments, sides %{{{1

	\draw (A) -- (B) -- (C) -- cycle;
	\draw[dashed] (B) -- (X);

% circles %{{{1

	\draw (O) circle (\radius);

% points, dots, vertices labels %{{{1

	\node[below left] at (A) {$A$};
	\node[below right] at (C) {$C$};
	\node[above right] at (O) {$O$};
	\node[above] at (B) {$B$};

% segments, sides labels %{{{1

	\node[above] at ($ (B)!0.5!(X) $) {$d$};

% circular arcs labels %{{{1

	\node at ($ (O) + (45:\radius+0.3) $) {$\mathscr{C}$};

% angles labels %{{{1

	\pic[draw, ->, "$\alpha$", angle radius=1.4cm, angle eccentricity=1.2]
	{angle = C--A--B};

% right angles markers %{{{1

	\pic [draw, angle radius=7pt, angle eccentricity=1]
	{right angle = C--B--A};

% closing %{{{1

\end{tikzpicture}
\end{document}
