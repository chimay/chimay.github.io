% vim: set ft=tex :

\input{../../include/latex/tikz/header.tex}

% opening %{{{1

\begin{document}
\begin{tikzpicture}[scale=1.0]

% parameters %{{{1

	\def\len{2.3}

	\def\ang{40}

% coordinates %{{{1

	\coordinate (A) at (0,0);
	\coordinate (B) at (\len,0);
	\coordinate (C) at (\ang:\len);
	\coordinate (X) at (3*\len/2,0);
	\coordinate (Y) at (\ang:3*\len/2);

% segments %{{{1

	\draw (A) -- (X);
	\draw (A) -- (Y);

% points, dots, vertices %{{{1

	\fill (A) circle (0.4mm);

% vertices labels %{{{1

	\node[below left] at (A) {$A$};

% segments, sides, lines labels %{{{1

	\node[above] at ($ (A)!0.7!(X) $) {$d$};
	\node[above] at ($ (A)!0.7!(Y) $) {$f$};

% angles labels %{{{1

	\pic[draw, vector, "$\alpha$", angle radius=1.2cm, angle eccentricity=1.2]
	{angle = B--A--C};

% closing %{{{1

\end{tikzpicture}
\end{document}
