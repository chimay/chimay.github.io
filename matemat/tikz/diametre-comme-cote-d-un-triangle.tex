% vim: set ft=tex tabstop=4 shiftwidth=4 noexpandtab:

\input{../../include/latex/tikz/header.tex}

% opening %{{{1

\begin{document}
\begin{tikzpicture}[scale=1.0]

% parameters %{{{1

	\def\ang{10}
	\def\gle{60}

	\def\radius{4.5}

% coordinates %{{{1

	\coordinate (O) at (0,0);
	\coordinate (A) at (\ang:-\radius);
	\coordinate (B) at (\ang:\radius);
	\coordinate (C) at (\gle:\radius);

% points, dots, vertices %{{{1

	\fill (O) circle (0.4mm);
	\fill (A) circle (0.4mm);
	\fill (B) circle (0.4mm);
	\fill (C) circle (0.4mm);

% segments, sides %{{{1

	\draw (A) -- (B) -- (C) -- cycle;
	%\draw (O) -- (C);

% circles %{{{1

	\draw (O) circle (\radius);

% points, dots, vertices labels %{{{1

	\node[below] at (O) {$O$};
	\node[left] at (A) {$A$};
	\node[right] at (B) {$B$};
	\node[above] at (C) {$C$};

% segments, sides labels %{{{1

	\node[below] at ($ (O)!0.5!(A) $) {$r$};
	\node[below] at ($ (O)!0.5!(B) $) {$r$};

% segments, sides marks %{{{1

	\tkzMarkSegments[mark=||, size=2pt](O,A)
	\tkzMarkSegments[mark=||, size=2pt](O,B)
	%\tkzMarkSegments[mark=||, size=2pt](O,C)

% circle label %{{{1

	\node at (100:{\radius+0.3}) {$\mathscr{C}$};
	\node at (-80:{\radius+0.3}) {$\Gamma$};

% angles labels %{{{1

	%\pic[draw, ->, "$\alpha$", angle radius=0.5cm, angle eccentricity=1.4]
	%{angle = C--O--A};

	%\pic[draw, ->, "$\delta$", angle radius=1.0cm, angle eccentricity=1.2]
	%{angle = B--O--C};

	%\pic[draw, ->, "$\beta$", angle radius=1.5cm, angle eccentricity=1.2]
	%{angle = O--A--C};

	%\pic[draw, ->, "$\beta$", angle radius=1.5cm, angle eccentricity=1.2]
	%{angle = A--C--O};

	%\pic[draw, ->, "$\gamma$", angle radius=0.8cm, angle eccentricity=1.3]
	%{angle = C--B--O};

	%\pic[draw, ->, "$\gamma$", angle radius=1.3cm, angle eccentricity=1.2]
	%{angle = O--C--B};

	%\pic[draw, ->, "$\theta$", angle radius=0.7cm, angle eccentricity=1.3]
	%{angle = A--C--B};

	\pic[draw, ->, "$\alpha$", angle radius=0.7cm, angle eccentricity=1.3]
	{angle = A--O--B};

	\pic[draw, ->, "$\beta$", angle radius=0.7cm, angle eccentricity=1.3]
	{angle = A--C--B};

% closing %{{{1

\end{tikzpicture}
\end{document}
