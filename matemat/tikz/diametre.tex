
\input{../../include/latex/tikz/header.tex}

% opening %{{{1

\begin{document}
\begin{tikzpicture}[scale=0.9]

% parameters %{{{1

	\def\radius{3}

% coordinates %{{{1

	\coordinate (O) at (0,0);
	\coordinate (A) at (0:-\radius);
	\coordinate (B) at (0:\radius);

% points for dimension lines %{{{1

	\coordinate (Ad) at ($(A) + (0,3.5)$);
	\coordinate (Bd) at ($(B) + (0,3.5)$);

% circle %{{{1

	\draw (O) circle (\radius);

% diameter %{{{1

	\draw (A) -- (B);

% dimension lines %{{{1

	\draw[dimension] (Ad) -- (Bd);
	\draw[dimension extension] (Ad) -- (A);
	\draw[dimension extension] (Bd) -- (B);

% points, dots, vertices %{{{1

	\fill (O) circle (0.5mm);
	\fill (A) circle (0.5mm);
	\fill (B) circle (0.5mm);

% points, dots, vertices labels %{{{1

	\node[below] at (O) {$O$};
	\node[below left] at (A) {$A$};
	\node[below right] at (B) {$B$};

% segments labels %{{{1

	\node[above] at ($(O)!0.5!(A)$) {$r$};
	\node[above] at ($(O)!0.5!(B)$) {$r$};
	\node[above, goldenbrown] at ($(Ad)!0.5!(Bd)$) {$d$};

% segments, sides, lines marks %{{{1

	\tkzMarkSegments[mark=||, size=2pt](O,A)
	\tkzMarkSegments[mark=||, size=2pt](O,B)

% circle label %{{{1

	\node at (45:{\radius+0.3}) {$\mathscr{C}$};

% closing %{{{1

\end{tikzpicture}
\end{document}
