
\input{../../include/latex/tikz/header.tex}

% opening %{{{1

\begin{document}
\begin{tikzpicture}[scale=0.9]

% parameters %{{{1

	\def\radius{3}

% coordinates %{{{1

	\coordinate (O) at (0,0);
	\coordinate (P) at (10:\radius);

% circle %{{{1

	\draw (O) circle (\radius);

% radius %{{{1

	\draw (O) -- (P);

% points, dots, vertices %{{{1

	\fill (O) circle (0.5mm);
	\fill (P) circle (0.5mm);

% points, dots, vertices labels %{{{1

	\node[left] at (O) {$O$};
	\node[right] at (P) {$P$};

% segments labels %{{{1

	\node[above] at ($(O)!0.5!(P)$) {$r$};

% circle label %{{{1

	\node at (75:{0.7*\radius}) {$\mathscr{D}$};

	\node at (45:{\radius+0.3}) {$\mathscr{C}$};

% closing %{{{1

\end{tikzpicture}
\end{document}
