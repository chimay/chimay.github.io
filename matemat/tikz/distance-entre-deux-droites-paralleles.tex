% vim: set ft=tex tabstop=4 shiftwidth=4 noexpandtab:

\input{../../include/latex/tikz/header.tex}

% opening %{{{1

\begin{document}
\begin{tikzpicture}[scale=1.0]

% parameters %{{{1

	\def\len{3}
	\def\height{3}

	\def\ext{1.7}

% coordinates %{{{1

	\coordinate (A) at (0,0);
	\coordinate (B) at (\len,0);

	\coordinate (C) at (0,-\height);
	\coordinate (D) at (\len,-\height);

	\coordinate (X1) at ({-(\ext - 1)*\len},0);
	\coordinate (X2) at ({\ext*\len},0);

	\coordinate (Y1) at ({-(\ext - 1)*\len},-\height);
	\coordinate (Y2) at ({\ext*\len},-\height);

	\coordinate (U1) at (0,{(\ext - 1)*\height});
	\coordinate (U2) at (0,{-\ext*\height});

	\coordinate (V1) at (\len,{(\ext - 1)*\height});
	\coordinate (V2) at (\len,{-\ext*\height});

% points, dots, vertices %{{{1

	\fill (A) circle (0.4mm);
	\fill (B) circle (0.4mm);
	\fill (C) circle (0.4mm);
	\fill (D) circle (0.4mm);

% segments, sides %{{{1

	\draw (X1) -- (X2);
	\draw (Y1) -- (Y2);

	\draw (U1) -- (U2);
	\draw (V1) -- (V2);

% points, dots, vertices labels %{{{1

	\node[above left] at (A) {$A$};
	\node[above right] at (B) {$B$};
	\node[below left] at (C) {$C$};
	\node[below right] at (D) {$D$};

% closing %{{{1

\end{tikzpicture}
\end{document}
