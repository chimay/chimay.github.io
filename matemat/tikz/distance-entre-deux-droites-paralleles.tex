% vim: set ft=tex tabstop=4 shiftwidth=4 noexpandtab:

\input{../../include/latex/tikz/header.tex}

% opening %{{{1

\begin{document}
\begin{tikzpicture}[scale=1.0]

% parameters %{{{1

	\def\len{5}
	\def\height{3}

	\def\ext{1.5}
	\def\ens{1.5}

% coordinates %{{{1

	\coordinate (A) at (0,0);
	\coordinate (B) at (\len,0);

	\coordinate (C) at (0,-\height);
	\coordinate (D) at (\len,-\height);

	\coordinate (X1) at ({-(\ext - 1)*\len},0);
	\coordinate (X2) at ({\ext*\len},0);

	\coordinate (Y1) at ({-(\ext - 1)*\len},-\height);
	\coordinate (Y2) at ({\ext*\len},-\height);

	\coordinate (U1) at (0,{(\ens - 1)*\height});
	\coordinate (U2) at (0,{-\ens*\height});

	\coordinate (V1) at (\len,{(\ens - 1)*\height});
	\coordinate (V2) at (\len,{-\ens*\height});

% points, dots, vertices %{{{1

	\fill (A) circle (0.4mm);
	\fill (B) circle (0.4mm);
	\fill (C) circle (0.4mm);
	\fill (D) circle (0.4mm);

% segments, sides %{{{1

	\draw (X1) -- (X2);
	\draw (Y1) -- (Y2);

	\draw (U1) -- (U2);
	\draw (V1) -- (V2);

	\draw[dashed] (A) -- (D);

% points, dots, vertices labels %{{{1

	\node[above left] at (A) {$A$};
	\node[above right] at (B) {$B$};
	\node[below left] at (C) {$C$};
	\node[below right] at (D) {$D$};

% segments, sides labels %{{{1

	\node[above] at ($ (B)!0.7!(X2) $) {$a$};
	\node[above] at ($ (D)!0.7!(Y2) $) {$b$};
	\node[left] at ($ (A)!0.8!(U1) $) {$c$};
	\node[left] at ($ (B)!0.8!(V1) $) {$d$};

% angles labels %{{{1

	\pic[draw, ->, "$\alpha$", angle radius=0.8cm, angle eccentricity=1.5]
	{angle = D--A--B};

	\pic[draw, ->, "$\alpha$", angle radius=0.8cm, angle eccentricity=1.5]
	{angle = A--D--C};

% right angles markers %{{{2

	\pic [draw, angle radius=7pt, angle eccentricity=1]
	{right angle = C--A--B};

	\pic [draw, angle radius=7pt, angle eccentricity=1]
	{right angle = A--B--D};

	\pic [draw, angle radius=7pt, angle eccentricity=1]
	{right angle = D--C--A};

	\pic [draw, angle radius=7pt, angle eccentricity=1]
	{right angle = B--D--C};

% closing %{{{1

\end{tikzpicture}
\end{document}
