% vim: set ft=tex tabstop=4 shiftwidth=4 noexpandtab:

\input{../../include/latex/tikz/header-light.tex}

% opening %{{{1

\begin{document}
\begin{tikzpicture}[scale=1.0]

% parameters %{{{1

	\def\len{5}
	\def\height{3}

% coordinates %{{{1

	\coordinate (A) at (0,0);
	\coordinate (B) at (0,-\height);

	\coordinate (O) at (-4,-\height);
	\coordinate (P) at (-2,-\height);

	\coordinate (X) at (-6,-\height);
	\coordinate (Y) at (5,-\height);

% points, dots, vertices %{{{1

	\fill (A) circle (0.4mm);
	\fill (B) circle (0.4mm);

	\fill (O) circle (0.4mm);
	\fill (P) circle (0.4mm);

% segments, sides, lines %{{{1

	\draw[dashed] (X) -- (O);
	\draw (O) -- (Y);
	\draw (A) -- (B);

% points, dots, vertices labels %{{{1

	\node[above] at (A) {$A$};
	\node[below] at (B) {$B$};

	\node[below] at (O) {$O$};
	\node[below] at (P) {$P$};

% segments, sides, lines labels %{{{1

	%\node[above] at ($ (X)!0.9!(Y) $) {$f$};

% right angles markers %{{{1

	\pic [draw, angle radius=7pt, angle eccentricity=1]
	{right angle = Y--B--A};

% closing %{{{1

\end{tikzpicture}
\end{document}
