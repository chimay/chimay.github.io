% vim: set ft=tex tabstop=4 shiftwidth=4 noexpandtab:

\input{../../include/latex/tikz/header-light.tex}

% opening %{{{1

\begin{document}
\begin{tikzpicture}[scale=1.0]

% parameters %{{{1

	\def\len{5}
	\def\height{3}

% coordinates %{{{1

	\coordinate (A) at (0,0);
	\coordinate (B) at (0,-\height);
	\coordinate (X) at (-\len,-\height);
	\coordinate (Y) at (\len,-\height);

% points, dots, vertices %{{{1

	\fill (A) circle (0.4mm);
	\fill (B) circle (0.4mm);

% segments, sides, lines %{{{1

	\draw (X) -- (Y);
	\draw (A) -- (B);

% points, dots, vertices labels %{{{1

	\node[above] at (A) {$A$};
	\node[below] at (B) {$B$};

% segments, sides, lines labels %{{{1

	\node[above] at ($ (X)!0.9!(Y) $) {$d$};

% right angles markers %{{{1

	\pic [draw, angle radius=7pt, angle eccentricity=1]
	{right angle = Y--B--A};

% closing %{{{1

\end{tikzpicture}
\end{document}
