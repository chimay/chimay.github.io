% vim: set ft=tex tabstop=4 shiftwidth=4 noexpandtab:

\input{../../include/latex/tikz/header-light.tex}

% opening %{{{1

\begin{document}
\begin{tikzpicture}[scale=1.0]

% parameters %{{{1

	\def\len{7}
	\def\gth{4}

	\def\ang{20}

% coordinates %{{{1

	\coordinate (A) at (0,0);
	\coordinate (B) at (\ang:\len);
	\coordinate (C) at (\ang:\gth);

% points, dots, vertices %{{{1

	\fill (A) circle (0.4mm);
	\fill (B) circle (0.4mm);
	\fill (C) circle (0.4mm);

% segments, sides %{{{1

	\draw (A) -- (B);

% points, dots, vertices labels %{{{1

	\node[above] at (A) {$A$};
	\node[above] at (B) {$B$};
	\node[above] at (C) {$C$};

% segments, sides labels %{{{1

	%\node[above] at ($ (A)!0.5!(B) $) {$c$};

% closing %{{{1

\end{tikzpicture}
\end{document}
