% vim: set ft=tex tabstop=4 shiftwidth=4 noexpandtab:

\input{../../include/latex/tikz/header-light.tex}

% opening %{{{1

\begin{document}
\begin{tikzpicture}[scale=1.0]

% parameters %{{{1

	\def\len{5}
	\def\height{3}

% coordinates %{{{1

	\coordinate (A) at (3.5,0);
	\coordinate (I) at (3.5,-\height);

	\coordinate (O) at (0,-\height);

	\coordinate (X) at (-3,-\height);
	\coordinate (Y) at (7,-\height);

% points, dots, vertices %{{{1

	\fill (A) circle (0.4mm);
	\fill (I) circle (0.4mm);

	\fill (O) circle (0.4mm);

% segments, sides, lines %{{{1

	\draw[dashed] (X) -- (O);
	\draw (O) -- (Y);
	\draw (A) -- (I);
	\draw (O) -- (A);

% points, dots, vertices labels %{{{1

	\node[above] at (A) {$A$};
	\node[below] at (I) {$I$};

	\node[below] at (O) {$O$};

% segments, sides, lines labels %{{{1

	\node[above] at ($ (X)!0.9!(Y) $) {$f$};
	\node[above] at ($ (X)!0.1!(Y) $) {$d$};

% angles labels %{{{1

	\pic[draw, ->, "$\alpha$", angle radius=1.2cm, angle eccentricity=1.3]
	{angle = I--O--A};

% right angles markers %{{{2

	\pic [draw, angle radius=7pt, angle eccentricity=1]
	{right angle = A--I--O};

% closing %{{{1

\end{tikzpicture}
\end{document}
