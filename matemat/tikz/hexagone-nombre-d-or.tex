
\input{../../include/latex/tikz/header.tex}

% opening %{{{1

\begin{document}
\begin{tikzpicture}[scale=2.5]

% parameters %{{{1

	\tikzmath{
		\radius = 2.0 ;
		\rotation = 90 ;
		\numsidesfloat = 6.0 ;
		\numsides = int(\numsidesfloat) ;
		\numsidesminus = \numsides - 1 ;
		int \i ;
		for \i in {1,...,\numsides} {
			\ang{\i} = \rotation + 360 / \numsidesfloat * (\i-1) ;
		};
	}

% coordinates %{{{1

	\coordinate (O) at (0,0);

	\foreach \i in {1,...,\numsides}
		\coordinate (P_\i) at (\ang{\i}:\radius);

% intersections %{{{1

	\begin{pgfinterruptboundingbox}
		\path[name path=line1] (O) -- (P_2);
		\path[name path=line2] (P_1) -- (P_3);
		\path[name intersections={of=line1 and line2, by=G}];
	\end{pgfinterruptboundingbox}

	\begin{pgfinterruptboundingbox}
		\path[name path=line1] (O) -- (P_6);
		\path[name path=line2] (P_1) -- (P_5);
		\path[name intersections={of=line1 and line2, by=H}];
	\end{pgfinterruptboundingbox}

	\begin{pgfinterruptboundingbox}
		\path[name path=line1] (O) -- (P_1);
		\path[name path=line2] (G) -- (H);
		\path[name intersections={of=line1 and line2, by=I}];
	\end{pgfinterruptboundingbox}

	\coordinate (GH) at ($ (H) - (G) $);
	\coordinate (X) at ($ (G) - 2*(GH) $);
	\coordinate (Y) at ($ (H) + 2*(GH) $);

	\begin{pgfinterruptboundingbox}
		\path[name path=line] (X) -- (Y);
		\path[name path=circle] (O) circle (\radius);
		\path[name intersections={of=line and circle, by={J,K}}];
	\end{pgfinterruptboundingbox}

% points, dots, vertices %{{{1

	\fill (O) circle (0.2mm);

	\fill (G) circle (0.2mm);
	\fill (H) circle (0.2mm);
	\fill (I) circle (0.2mm);
	\fill (J) circle (0.2mm);

	\foreach \i in {1,...,\numsides}
		\fill (P_\i) circle (0.2mm);

% segments, sides, lines %{{{1

	\draw (P_1) \foreach \i in {2,...,\numsides} { -- (P_\i) } -- cycle;

	\draw (P_1) -- (P_3) -- (P_5) -- cycle;

	\draw (P_2) -- (P_5);
	\draw (P_3) -- (P_6);

	\draw (O) -- (P_1);

	\draw (O) -- (J);
	\draw (G) -- (J);

% circle %{{{1

	\draw (O) circle (\radius);

% points, dots, vertices labels %{{{1

	\node[anchor=east] at ($ (O) + (180:2pt) $) {$O$};

	\node[above] at (P_1) {$P_1$};
	\node[left] at (P_2) {$P_2$};
	\node[left] at (P_3) {$P_3$};
	\node[below] at (P_4) {$P_4$};
	\node[right] at (P_5) {$P_5$};
	\node[right] at (P_6) {$P_6$};

	\node[anchor=south] at ($ (G) + (90:1pt) $) {$G$};
	\node[anchor=south] at ($ (H) + (90:1pt) $) {$H$};
	\node[above left] at (I) {$I$};
	\node[right] at (J) {$J$};

% segments labels %{{{1

	\node[below right] at ($ (O)!0.4!(J) $) {$r$};

	\node[right] at ($(O)!0.5!(I)$) {$a$};

	\node[above] at ($(G)!0.5!(I)$) {$b$};
	\node[above] at ($(I)!0.5!(H)$) {$b$};

	\node[below left] at ($(O)!0.5!(G)$) {$r / 2$};

	\node[above left] at ($(P_1)!0.5!(G)$) {$r \ \sqrt{3}$};

% segments, sides, lines marks %{{{1

	\tkzMarkSegments[mark=||, size=2pt](P_1,G);
	\tkzMarkSegments[mark=||, size=2pt](G,P_3);
	\tkzMarkSegments[mark=||, size=2pt](P_1,H);
	\tkzMarkSegments[mark=||, size=2pt](H,P_5);

	\tkzMarkSegments[mark=|, size=2pt](O,G);
	\tkzMarkSegments[mark=|, size=2pt](O,H);

	\tkzMarkSegments[mark=|, size=2pt](G,P_2);
	\tkzMarkSegments[mark=|, size=2pt](H,P_6);

% circles labels %{{{1

	\node at ($ (O) + (60:\radius + 0.2) $) {$\mathscr{C}$};

% circular arcs labels %{{{2

	%\node at ($ (O) + (45:\radius + 0.3) $) {$\mathscr{C}$};

% angles labels %{{{1

	\pic[draw, ->, "$60^\circ$", angle radius=0.5cm, angle eccentricity=1.5]
		{angle = I--O--G};

% right angles markers %{{{2

	\pic [draw, angle radius=7pt, angle eccentricity=1]
		{right angle = P_3--G--O};

	\pic [draw, angle radius=7pt, angle eccentricity=1]
		{right angle = O--H--P_5};

	\pic [draw, angle radius=7pt, angle eccentricity=1]
		{right angle = H--I--P_1};

% closing %{{{1

\end{tikzpicture}
\end{document}
