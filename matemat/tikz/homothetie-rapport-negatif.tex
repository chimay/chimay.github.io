% vim: set ft=tex tabstop=4 shiftwidth=4 noexpandtab:

% header %{{{1

% opening %{{{2

\documentclass[tikz, border=1mm]{standalone}

% packages and libraries %{{{2

\usepackage{../../include/latex/custom}

\usetikzlibrary{calc,intersections,angles,quotes}
%\usetikzlibrary{calc,intersections,angles,quotes,shapes.geometric,arrows.meta}

\usepackage{tkz-euclide}

% colors %{{{2

\definecolor{goldenbrown}{HTML}{5b3c11}

% style %{{{2

\tikzset{
	% ------- every something
	every picture/.style={
		scale=1.0,
	},
	every coordinate/.style={
		fill=black, circle, inner sep=1pt,
	},
	every path/.style={
		line width=0.3pt,
	},
	every node/.style={
		font=\normalsize,
	},
	every angle/.style={
	},
	every pic/.style={
		% ---- does not worl
		%draw,
		%-{Straight Barb[length=1.2mm]},
	},
	% ------- custom
	vector/.style={
		-{Straight Barb[length=1.2mm]},
		%thick,
	},
	construction/.style={
		line width=0.1pt,
		dashed,
	},
	dimension/.style={
		line width=0.2pt,
		<->,
		goldenbrown,
	},
	dimension extension/.style={
		line width=0.2pt,
		dashed,
		goldenbrown,
	},
}

% document %{{{1

% opening %{{{2

\begin{document}
\begin{tikzpicture}[scale=1.0]

% parameters %{{{2

	\tikzmath{
		\len = 3.0;
		\homothety = -5/2;
		\extratio = 1.2;
		\ext = \extratio * max(1, \homothety);
	}

	\def\ang{20}

% coordinates %{{{2

	\coordinate (O) at (0,0);
	\coordinate (A) at (\ang:\len);
	\coordinate (B) at (\ang:{\homothety*\len});

	\coordinate (X) at (\ang:{\extratio*\homothety*\len});
	\coordinate (Y) at (\ang:{\ext*\len});

% points, dots, vertices %{{{2

	\fill (O) circle (0.4mm);
	\fill (A) circle (0.4mm);
	\fill (B) circle (0.4mm);

% segments, sides %{{{2

	\draw (X) -- (Y);

% points, dots, vertices labels %{{{2

	\node[below] at (O) {$O$};
	\node[below] at (A) {$A$};
	\node[below] at (B) {$B$};

% segments, sides labels %{{{2

	%\node[above left] at ($ (O)!0.5!(Y) $) {$d$};

% closing %{{{2

\end{tikzpicture}
\end{document}
