% vim: set ft=tex tabstop=4 shiftwidth=4 noexpandtab:

% header %{{{1

% opening %{{{2

\documentclass[tikz, border=1mm]{standalone}

% packages and libraries %{{{2

\usepackage{../../include/latex/custom}

\usetikzlibrary{calc,intersections,angles,quotes}

\usepackage{tkz-euclide}

% colors %{{{2

\definecolor{goldenbrown}{HTML}{5b3c11}

% style %{{{2

\tikzset{
	% ------- every something
	every picture/.style={
		scale=1.0,
	},
	every coordinate/.style={
		fill=black, circle, inner sep=1pt,
	},
	every path/.style={
		line width=0.3pt,
	},
	every node/.style={
		font=\normalsize,
	},
	every angle/.style={
	},
	every pic/.style={
		% ---- does not worl
		%draw,
		%-{Straight Barb[length=1.2mm]},
	},
	% ------- custom
	vector/.style={
		-{Straight Barb[length=1.2mm]},
		%thick,
	},
	construction/.style={
		line width=0.1pt,
		dashed,
	},
	dimension/.style={
		line width=0.2pt,
		<->,
		goldenbrown,
	},
	dimension extension/.style={
		line width=0.2pt,
		dashed,
		goldenbrown,
	},
}

% document %{{{1

% opening %{{{2

\begin{document}
\begin{tikzpicture}[scale=1.0]

% parameters %{{{2

	\tikzmath{
		\lenA = 3.0;
		\lenB = 2.0;
		\angA = -20;
		\angB = 30;
		\homothety = 6/2;
		\extratio = 1.3;
		\ext = \extratio * max(1, \homothety);
	}


% coordinates %{{{2

	\coordinate (O) at (0,0);
	\coordinate (A) at (\angA:\lenA);
	\coordinate (A2) at (\angA:{2*\lenA});
	\coordinate (B) at (\angB:\lenB);
	\coordinate (B2) at (\angB:{2*\lenB});
	\coordinate (hA) at (\angA:{\homothety*\lenA});
	\coordinate (hB) at (\angB:{\homothety*\lenB});

% points, dots, vertices %{{{2

	\fill (O) circle (0.4mm);
	\fill (A) circle (0.4mm);
	\fill (B) circle (0.4mm);
	\fill (hA) circle (0.4mm);
	\fill (hB) circle (0.4mm);

	\draw (A) -- ($ (A)  + ({90+\angA}:2mm) $);
	\draw (A2) -- ($ (A2)  + ({90+\angA}:2mm) $);
	\draw (B) -- ($ (B)  + ({90+\angB}:2mm) $);
	\draw (B2) -- ($ (B2)  + ({90+\angB}:2mm) $);
	\draw (hA) -- ($ (hA)  + ({90+\angA}:2mm) $);
	\draw (hB) -- ($ (hB)  + ({90+\angB}:2mm) $);

% segments, sides %{{{2

	\draw (A) -- (B);
	\draw (hA) -- (hB);

	\draw[dashdotted] (O) -- (hA);
	\draw[dashdotted] (O) -- (hB);

% points, dots, vertices labels %{{{2

	\node[below] at (O) {$O$};
	\node[below] at (A) {$A$};
	\node[above] at (B) {$B$};
	\node[below] at (hA) {$C = \mathcal{H}(A)$};
	\node[above] at (hB) {$D = \mathcal{H}(B)$};

% segments, sides labels %{{{2

	%\node[above left] at ($ (O)!0.9!(Y) $) {$d$};

% closing %{{{2

\end{tikzpicture}
\end{document}
