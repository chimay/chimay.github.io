% vim: set ft=tex tabstop=4 shiftwidth=4 noexpandtab:

\input{../../include/latex/tikz/header.tex}

% opening %{{{1

\begin{document}
\begin{tikzpicture}[scale=1.0]

% parameters %{{{1

	 \tikzmath{
		\radius1 = 2.5;
		\radius2 = 4.5;
		\ang1 = -20;
		\ang2 = 20;
		\angrot = 180;
	 }

% coordinates %{{{1

	\coordinate (O) at (0,0);
	\coordinate (A) at (\ang1:\radius1);
	\coordinate (sA) at ({\ang1+\angrot}:\radius1);
	\coordinate (B) at (\ang2:\radius2);
	\coordinate (sB) at ({\ang2+\angrot}:\radius2);

% points, dots, vertices %{{{1

	\fill (O) circle (0.4mm);
	\fill (A) circle (0.4mm);
	\fill (sA) circle (0.4mm);
	\fill (B) circle (0.4mm);
	\fill (sB) circle (0.4mm);

% segments, sides %{{{1

	\draw[dashdotted] (O) -- (A);
	\draw[dashdotted] (O) -- (sA);
	\draw[dashdotted] (O) -- (B);
	\draw[dashdotted] (O) -- (sB);

	\draw (A) -- (B);
	\draw (sA) -- (sB);

% points, dots, vertices labels %{{{1

	\node[above] at (O) {$O$};
	\node[below right] at (A) {$A$};
	\node[right] at (B) {$B$};
	\node[above left] at (sA) {$C = \mathcal{S}(A)$};
	\node[below left] at (sB) {$D = \mathcal{S}(B)$};

% segments, sides marks %{{{1

	\tkzMarkSegments[mark=|, size=2pt](O,A)
	\tkzMarkSegments[mark=|, size=2pt](O,sA)

	\tkzMarkSegments[mark=||, size=2pt](O,B)
	\tkzMarkSegments[mark=||, size=2pt](O,sB)

% angles labels %{{{1

	\pic[draw, ->, "$\alpha$", angle radius=1.0cm, angle eccentricity=1.3]
	{angle = A--O--B};

	\pic[draw, ->, "$\alpha$", angle radius=1.2cm, angle eccentricity=1.3]
	{angle = sA--O--sB};

% closing %{{{1

\end{tikzpicture}
\end{document}
