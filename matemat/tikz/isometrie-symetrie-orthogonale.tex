% vim: set ft=tex tabstop=4 shiftwidth=4 noexpandtab:

\input{../../include/latex/tikz/header.tex}

% opening %{{{1

\begin{document}
\begin{tikzpicture}[scale=1.0]

% parameters %{{{1

	\def\lenA{4}
	\def\lenB{2.5}
	\def\lenLine{4}

% coordinates %{{{1

	\coordinate (X) at (0,{-\lenLine});
	\coordinate (Y) at (0,{\lenLine});

	\coordinate (A) at (-\lenA,-2);
	\coordinate (B) at (-\lenB,2);
	\coordinate (sA) at (\lenA,-2);
	\coordinate (sB) at (\lenB,2);

% intersections %{{{1

	\path[name path=line2] (X) -- (Y);
	\path[name path=line1] (A) -- (sA);
	\path[name intersections={of=line1 and line2, by=I}];

	\path[name path=line2] (X) -- (Y);
	\path[name path=line1] (B) -- (sB);
	\path[name intersections={of=line1 and line2, by=J}];

% points, dots, vertices %{{{1

	\fill (A) circle (0.4mm);
	\fill (B) circle (0.4mm);
	\fill (I) circle (0.4mm);

	\fill (sA) circle (0.4mm);
	\fill (sB) circle (0.4mm);
	\fill (J) circle (0.4mm);

% segments, sides %{{{1

	\draw (X) -- (Y);
	\draw (A) -- (B);
	\draw (sA) -- (sB);
	\draw[dashdotted] (A) -- (sA);
	\draw[dashdotted] (B) -- (sB);
	\draw[dashed] (B) -- (I);
	\draw[dashed] (sB) -- (I);

% points, dots, vertices labels %{{{1

	\node[above left] at (A) {$A$};
	\node[above left] at (B) {$B$};
	\node[above right] at (sA) {$C = \mathcal{S}(A)$};
	\node[above right] at (sB) {$D = \mathcal{S}(B)$};
	\node[below left] at (I) {$I$};
	\node[below left] at (J) {$J$};

% segments, sides labels %{{{1

	\node[left] at ($ (X)!0.9!(Y) $) {$d$};

% segments, sides marks %{{{1

	\tkzMarkSegments[mark=||, size=2pt](A,I)
	\tkzMarkSegments[mark=||, size=2pt](I,sA)

	\tkzMarkSegments[mark=|, size=2pt](B,J)
	\tkzMarkSegments[mark=|, size=2pt](J,sB)

% angles labels %{{{1

	\pic[draw, ->, "$\alpha$", angle radius=1.0cm, angle eccentricity=1.3]
	{angle = I--B--J};

	\pic[draw, ->, "$\beta$", angle radius=1.0cm, angle eccentricity=1.2]
	{angle = J--sB--I};

	\pic[draw, ->, "$\gamma$", angle radius=1.0cm, angle eccentricity=1.2]
	{angle = B--I--A};

	\pic[draw, ->, "$\delta$", angle radius=1.0cm, angle eccentricity=1.2]
	{angle = sA--I--sB};

% right angles markers %{{{2

	\pic [draw, angle radius=7pt, angle eccentricity=1]
	{right angle = sA--I--X};

	\pic [draw, angle radius=7pt, angle eccentricity=1]
	{right angle = sB--J--X};

% closing %{{{1

\end{tikzpicture}
\end{document}
