% vim: set ft=tex tabstop=4 shiftwidth=4 noexpandtab:

\input{../../include/latex/tikz/header.tex}

% opening %{{{1

\begin{document}
\begin{tikzpicture}[scale=1.3]

% parameters %{{{1

	\def\len{4}

	\def\ang{20}

% vectors %{{{1

	\coordinate (v) at (\ang:\len);

% unit vectors %{{{2

	\coordinate (u) at ($ (0,0)!1cm!(v) $);

% coordinates %{{{1

	\coordinate (A) at (0,0);
	\coordinate (tA) at ($ (A) + (v) $);

	\coordinate (B) at (2,-2);
	\coordinate (tB) at ($ (B) + (v) $);

% points, dots, vertices %{{{1

	\fill (A) circle (0.4mm);
	\fill (B) circle (0.4mm);
	\fill (tA) circle (0.4mm);
	\fill (tB) circle (0.4mm);

% segments, sides %{{{1

	\draw (A) -- (B);
	\draw (tA) -- (tB);

% vectors %{{{2

	\draw[vector, dashed] (A) -- (tA);
	\draw[vector, dashed] (B) -- (tB);
	\draw[dashed] (B) -- (tA);

% points, dots, vertices labels %{{{1

	\node[below left] at (A) {$A$};
	\node[below left] at (B) {$B$};
	\node[above right] at (tA) {$C = \mathcal{T}(A)$};
	\node[right] at (tB) {$D = \mathcal{T}(B)$};

% segments, sides labels %{{{1

	\node[above] at ($ (A)!0.5!(tA) $) {$\vecteur{v}$};
	\node[below] at ($ (B)!0.5!(tB) $) {$\vecteur{v}$};

% angles labels %{{{1

	\pic[draw, ->, "$\alpha$", angle radius=1.1cm, angle eccentricity=1.3]
	{angle = A--tA--B};

	\pic[draw, ->, "$\beta$", angle radius=1.1cm, angle eccentricity=1.3]
	{angle = tB--B--tA};

% closing %{{{1

\end{tikzpicture}
\end{document}
