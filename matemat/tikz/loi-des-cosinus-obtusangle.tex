
\input{../../include/latex/tikz/header-light.tex}

% opening %{{{1

\begin{document}

\begin{tikzpicture}[scale=1.3]

% coordinates %{{{1

	\coordinate (A) at (0,0);
	\coordinate (B) at (3,0);
	\coordinate (C) at (5,3);

	\coordinate (D) at ($ (A)!(C)!(B) $);

	\coordinate (X) at (7,0);

% vertices, points, dots %{{{1

	\fill (A) circle (0.4mm);
	\fill (B) circle (0.4mm);
	\fill (C) circle (0.4mm);
	\fill (D) circle (0.4mm);

% triangle %{{{1

	\draw (A) -- (B) -- (C) -- cycle;

	\draw[dashed] (C) -- (D);

	\draw[dashdotdotted] (B) -- (X);

% vertices labels %{{{1

	\node[below left] at (A) {$A$};
	\node[below] at (B) {$B$};
	\node[above] at (C) {$C$};
	\node[below] at (D) {$D$};

% segments labels %{{{1

	\node[below right] at ($ (B)!0.45!(C) $) {$a$};
	\node[above left] at ($ (C)!0.45!(A) $) {$b$};
	\node[below] at ($ (A)!0.55!(B) $) {$c$};

	\node[left] at ($ (C)!0.5!(D) $) {$h$};
	\node[below] at ($ (B)!0.5!(D) $) {$u$};

% angles labels %{{{1

	\pic[draw, ->, "$\alpha$", angle radius=1.6cm, angle eccentricity=1.2]
	{angle = B--A--C};

	\pic[draw, ->, "$\beta$", angle radius=0.7cm, angle eccentricity=1.4]
	{angle = C--B--A};

	\pic[draw, ->, "$\gamma$", angle radius=1.6cm, angle eccentricity=1.2]
	{angle = A--C--B};

	\pic[draw, ->, "$\theta$", angle radius=1.0cm, angle eccentricity=1.3]
	{angle = D--B--C};

% right angles labels %{{{1

	\pic [draw, angle radius=7pt, angle eccentricity=1]
	{right angle = C--D--A};

% closing %{{{1

\end{tikzpicture}

\end{document}
