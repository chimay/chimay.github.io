% Minimal TikZ standalone example
\documentclass[tikz, border=1mm]{standalone}

\usepackage{amsmath}
\usepackage{tikz}

\usetikzlibrary{calc,angles,quotes}

% document %{{{1

% opening %{{{2

\begin{document}

\begin{tikzpicture}[scale=1.3]

% coordinates %{{{2

	\coordinate (A) at (5,3);
	\coordinate (B) at (0,0);
	\coordinate (C) at (3,0);

	\coordinate (D) at (5,0);

	\coordinate (X) at (7,0);

% vertices, points, dots %{{{2

	\fill (A) circle (0.4mm);
	\fill (B) circle (0.4mm);
	\fill (C) circle (0.4mm);
	\fill (D) circle (0.4mm);

% triangle %{{{2

	\draw (A) -- (B) -- (C) -- cycle;

% altitude %{{{2

	\draw[dashed] (A) -- (D);

	% ---- prolongated side

	\draw[dashdotdotted] (B) -- (X);

% vertices labels %{{{2

	\node[above] at (A) {$A$};
	\node[below right] at (B) {$B$};
	\node[below] at (C) {$C$};
	\node[below right] at (D) {$D$};

% segments labels %{{{2

	\node[above left] at ($ (A)!0.55!(B) $) {$c$};
	\node[below right] at ($ (B)!0.45!(C) $) {$a$};
	\node[below right] at ($ (C)!0.45!(A) $) {$b$};

	\node[left] at ($ (A)!0.5!(D) $) {$h$};
	\node[below] at ($ (C)!0.5!(D) $) {$u$};

% angles labels %{{{2

	\pic[draw, ->, "$\alpha$", angle eccentricity=1.2, angle radius=1.7cm]
	{angle = B--A--C};

	\pic[draw, ->, "$\beta$", angle eccentricity=1.2, angle radius=1.3cm]
	{angle = C--B--A};

	\pic[draw, ->, "$\gamma$", angle eccentricity=1.2, angle radius=0.7cm]
	{angle = A--C--B};

	\pic[draw, ->, "$\theta$", angle eccentricity=1.3, angle radius=0.9cm]
	{angle = D--C--A};

% right angles labels %{{{2

	\pic [draw, angle radius=7pt, angle eccentricity=1]
	{right angle = C--D--A};

% closing %{{{2

\end{tikzpicture}

\end{document}
