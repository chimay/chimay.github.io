
\input{../../include/latex/tikz/header.tex}

% opening %{{{1

\begin{document}
\begin{tikzpicture}[scale=1.0]

% parameters %{{{1

	\tikzmath{
		\ang = 210 ;
		\ngl = -30 ;
		\gle = 80 ;
		\radius = 3.2 ;
	}

% coordinates %{{{1

	\coordinate (O) at (0,0);
	\coordinate (A) at (\ang:\radius);
	\coordinate (B) at (\ngl:\radius);
	\coordinate (C) at (\gle:\radius);

	%\coordinate (D) at ($(A)!(C)!(B)$);

% points, dots, vertices %{{{1

	\fill (O) circle (0.4mm);
	\fill (A) circle (0.4mm);
	\fill (B) circle (0.4mm);
	\fill (C) circle (0.4mm);

	%\fill (D) circle (0.3mm);

% segments, sides, lines %{{{1

	\draw (A) -- (B) -- (C) -- cycle;

	%\draw[dashed] (C) -- (D);

	\draw[dashed] (O) -- (A);
	\draw[dashed] (O) -- (B);
	\draw[dashed] (O) -- (C);

% circles %{{{1

	\draw (O) circle (\radius);

% points, dots, vertices labels %{{{1

	\node[left] at ($ (O) + (130:3pt) $) {$O$};
	\node[below left] at (A) {$A$};
	\node[below right] at (B) {$B$};
	\node[above] at (C) {$C$};

	%\node[above left] at (D) {$D$};

% segments, sides, lines labels %{{{1

	\node[above right] at ($(B)!0.5!(C)$) {$a$};
	\node[above left] at ($(A)!0.5!(C)$) {$b$};
	\node[below]  at ($(A)!0.5!(B)$) {$c$};

	\node[above left] at ($(O)!0.4!(A)$) {$r$};
	\node[below left] at ($(O)!0.4!(B)$) {$r$};
	\node[left] at ($(O)!0.4!(C)$) {$r$};

	%\node[left] at ($(C)!0.6!(D)$) {$h$};

% segments, sides, lines marks %{{{1

	\tkzMarkSegments[mark=||, size=2pt, pos=0.4](O,A)
	\tkzMarkSegments[mark=||, size=2pt, pos=0.4](O,B)
	\tkzMarkSegments[mark=||, size=2pt, pos=0.4](O,C)

% circle label %{{{1

	\node at (40:{\radius+0.3}) {$\mathscr{C}$};

% angles labels %{{{1

	\pic[draw, ->, "$\alpha$" {xshift=1, yshift=-5}, angle radius=1cm, angle eccentricity=1.3]
		{angle = B--A--C};

	\pic[draw, ->, "$\beta$" {xshift=-1, yshift=-9}, angle radius=1cm, angle eccentricity=1.3]
		{angle = C--B--A};

	\pic[draw, ->, "$\gamma$" {xshift=-9, yshift=2}, angle radius=1cm, angle eccentricity=1.3]
		{angle = A--C--B};

		\pic[draw, ->, "$\delta$", angle radius=0.5cm, angle eccentricity=1.5]
		{angle = B--O--C};

% right angle markers %{{{2

	%\pic [draw, angle radius=7pt] {right angle = B--D--C};

% closing %{{{1

\end{tikzpicture}
\end{document}
