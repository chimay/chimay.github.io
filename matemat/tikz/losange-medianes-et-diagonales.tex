% vim: set ft=tex tabstop=4 shiftwidth=4 noexpandtab:

% standard template with header in another file

\input{../../include/latex/tikz/header.tex}

% opening %{{{1

\begin{document}
\begin{tikzpicture}[scale=1.0]

% parameters %{{{1

	\def\len{3.5}
	\def\gth{2.0}

% coordinates %{{{1

	\coordinate (A) at (-\len,0);
	\coordinate (B) at (0,-\gth);
	\coordinate (C) at (\len,0);
	\coordinate (D) at (0,\gth);

% vectors from coordinates %{{{1

	\coordinate (AB) at ($ (B) - (A) $);
	\coordinate (BC) at ($ (C) - (B) $);
	\coordinate (CD) at ($ (D) - (C) $);
	\coordinate (DA) at ($ (A) - (D) $);

% coordinates from vectors %{{{1

	\coordinate (E) at ($ (A) + 0.5*(AB) $);
	\coordinate (F) at ($ (B) + 0.5*(BC) $);
	\coordinate (G) at ($ (C) + 0.5*(CD) $);
	\coordinate (H) at ($ (D) + 0.5*(DA) $);

% vectors from coordinates %{{{1

	\coordinate (AB) at ($ (B) - (A) $);
	\coordinate (BC) at ($ (C) - (B) $);
	\coordinate (CD) at ($ (D) - (C) $);
	\coordinate (DA) at ($ (A) - (D) $);

% intersections %{{{1

	\begin{pgfinterruptboundingbox}
		\path[name path=line1] (A) -- (C);
		\path[name path=line2] (B) -- (D);
		\path[name intersections={of=line1 and line2, by=I}];
	\end{pgfinterruptboundingbox}

% points, dots, vertices %{{{1

	\fill (A) circle (0.4mm);
	\fill (B) circle (0.4mm);
	\fill (C) circle (0.4mm);
	\fill (D) circle (0.4mm);

	\fill (E) circle (0.4mm);
	\fill (F) circle (0.4mm);
	\fill (G) circle (0.4mm);
	\fill (H) circle (0.4mm);

	\fill (I) circle (0.4mm);

% segments, sides %{{{1

	\draw (A) -- (B) -- (C) -- (D) -- cycle;
	\draw[dashed] (E) -- (G);
	\draw[dashed] (F) -- (H);
	\draw[dashed] (A) -- (C);
	\draw[dashed] (B) -- (D);

% points, dots, vertices labels %{{{1

	\node[left] at (A) {$A$};
	\node[below] at (B) {$B$};
	\node[right] at (C) {$C$};
	\node[above] at (D) {$D$};

	\node[below left] at (E) {$E$};
	\node[below right] at (F) {$F$};
	\node[above right] at (G) {$G$};
	\node[above left] at (H) {$H$};

	\node[above left] at (I) {$I$};

% segments, sides marks %{{{1

	\tkzMarkSegments[mark=||, size=2pt](A,E)
	\tkzMarkSegments[mark=||, size=2pt](E,B)

	\tkzMarkSegments[mark=||, size=2pt](D,G)
	\tkzMarkSegments[mark=||, size=2pt](G,C)

	\tkzMarkSegments[mark=||, size=2pt](H,I)
	\tkzMarkSegments[mark=||, size=2pt](I,F)

	\tkzMarkSegments[mark=|, size=2pt](A,H)
	\tkzMarkSegments[mark=|, size=2pt](H,D)

	\tkzMarkSegments[mark=|, size=2pt](B,F)
	\tkzMarkSegments[mark=|, size=2pt](F,C)

	\tkzMarkSegments[mark=|, size=2pt](E,I)
	\tkzMarkSegments[mark=|, size=2pt](I,G)

% angles labels %{{{1

% right angles markers %{{{2

	%\pic [draw, angle radius=7pt, angle eccentricity=1]
	%{right angle = C--I--D};

% closing %{{{1

\end{tikzpicture}
\end{document}
