% vim: set ft=tex tabstop=4 shiftwidth=4 noexpandtab:

% standard template with header in another file

\input{../../include/latex/tikz/header.tex}

% opening %{{{1

\begin{document}
\begin{tikzpicture}[scale=1.0]

% parameters %{{{1

	\def\len{3.5}
	\def\gth{2.0}

% coordinates %{{{1

	\coordinate (A) at (-\len,0);
	\coordinate (B) at (0,-\gth);
	\coordinate (C) at (\len,0);
	\coordinate (D) at (0,\gth);

% points, dots, vertices %{{{1

	\fill (A) circle (0.4mm);
	\fill (B) circle (0.4mm);
	\fill (C) circle (0.4mm);
	\fill (D) circle (0.4mm);

% segments, sides %{{{1

	\draw (A) -- (B) -- (C) -- (D) -- cycle;
	\draw[dashed] (B) -- (D);

% points, dots, vertices labels %{{{1

	\node[left] at (A) {$A$};
	\node[below] at (B) {$B$};
	\node[right] at (C) {$C$};
	\node[above] at (D) {$D$};

% segments, sides marks %{{{1

	\tkzMarkSegments[mark=||, size=2pt](A,B)
	\tkzMarkSegments[mark=||, size=2pt](B,C)
	\tkzMarkSegments[mark=||, size=2pt](C,D)
	\tkzMarkSegments[mark=||, size=2pt](D,A)

% angles labels %{{{1

	\pic[draw, ->, "$\alpha$", angle radius=0.9cm, angle eccentricity=1.3]
	{angle = A--D--B};

	\pic[draw, ->, "$\alpha$", angle radius=0.9cm, angle eccentricity=1.3]
	{angle = D--B--A};

	\pic[draw, ->, "$\alpha$", angle radius=0.8cm, angle eccentricity=1.3]
	{angle = B--D--C};

	\pic[draw, ->, "$\alpha$", angle radius=0.8cm, angle eccentricity=1.3]
	{angle = C--B--D};

	\pic[draw, ->, "$\gamma$", angle radius=0.9cm, angle eccentricity=1.3]
	{angle = B--A--D};

	\pic[draw, ->, "$\gamma$", angle radius=0.9cm, angle eccentricity=1.3]
	{angle = D--C--B};

% closing %{{{1

\end{tikzpicture}
\end{document}
