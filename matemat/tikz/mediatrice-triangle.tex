% vim: set ft=tex tabstop=4 shiftwidth=4 noexpandtab:

\input{../../include/latex/tikz/header.tex}

% opening %{{{1

\begin{document}
\begin{tikzpicture}[scale=1.0]

% parameters %{{{1

	% ---- length is reserved
	%\def\len{3}
	%\def\gth{3}

	% ---- angle can cause trouble
	%\def\ang{60}
	%\def\gle{60}

	%\def\radius{2}

	%\def\numsides{6}
	%\def\rotation{90}

	%\pgfmathtruncatemacro{\last}{\numsides}
	%\pgfmathtruncatemacro{\prevlast}{\numsides-1}

% vectors %{{{1

	% ---- vectors defined independantly of points coordinates

	%\coordinate (v) at (2,3);
	%\coordinate (w) at (30:4);

% unit vectors %{{{2

	%\coordinate (u) at ($ (0,0)!1cm!(v) $);

% coordinates %{{{1

	\coordinate (A) at (0,0);
	\coordinate (B) at (5,0);
	\coordinate (C) at (3,3);
	\coordinate (D) at (2.5,0);
	\coordinate (X) at (2.5,-2);
	\coordinate (Y) at (2.5,3.5);

% points, dots, vertices %{{{1

	\fill (A) circle (0.4mm);
	\fill (B) circle (0.4mm);
	\fill (C) circle (0.4mm);
	\fill (D) circle (0.4mm);

% segments, sides %{{{1

	\draw (A) -- (B) -- (C) -- cycle;
	\draw (X) -- (Y);

% points, dots, vertices labels %{{{1

	\node[below left] at (A) {$A$};
	\node[below right] at (B) {$B$};
	\node[above] at (C) {$C$};
	\node[below left] at (D) {$D$};

	%\node[label={[label distance=0.5]above:$G$}] at (G) {};

% segments, sides labels %{{{1

	\node[left] at ($ (X)!0.5!(Y) $) {$m$};

% segments, sides marks %{{{1

	\tkzMarkSegments[mark=||, size=2pt](A,D)
	\tkzMarkSegments[mark=||, size=2pt](D,B)

% closing %{{{1

\end{tikzpicture}
\end{document}
