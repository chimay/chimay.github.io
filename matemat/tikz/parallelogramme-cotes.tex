% vim: set ft=tex tabstop=4 shiftwidth=4 noexpandtab:

% standard template with header in another file

\input{../../include/latex/tikz/header.tex}

% opening %{{{1

\begin{document}
\begin{tikzpicture}[scale=1.0]

% parameters %{{{1

	%\def\len{3}
	%\def\gth{3}

% coordinates %{{{1

	\coordinate (A) at (0,0);
	\coordinate (B) at (6,0);
	\coordinate (C) at (8,3);
	\coordinate (D) at (2,3);

% points, dots, vertices %{{{1

	\fill (A) circle (0.4mm);
	\fill (B) circle (0.4mm);
	\fill (C) circle (0.4mm);
	\fill (D) circle (0.4mm);

% segments, sides %{{{1

	\draw (A) -- (B) -- (C) -- (D) -- cycle;
	\draw[dashed] (A) -- (C);

% points, dots, vertices labels %{{{1

	\node[below left] at (A) {$A$};
	\node[below right] at (B) {$B$};
	\node[above right] at (C) {$C$};
	\node[above left] at (D) {$D$};

% angles labels %{{{1

	\pic[draw, ->, "$\alpha$", angle radius=1.8cm, angle eccentricity=1.2]
	{angle = B--A--C};

	\pic[draw, ->, "$\beta$", angle radius=1.8cm, angle eccentricity=1.2]
	{angle = D--C--A};

	\pic[draw, ->, "$\gamma$", angle radius=1.4cm, angle eccentricity=1.2]
	{angle = C--A--D};

	\pic[draw, ->, "$\delta$", angle radius=1.4cm, angle eccentricity=1.2]
	{angle = A--C--B};

% closing %{{{1

\end{tikzpicture}
\end{document}
