% vim: set ft=tex tabstop=4 shiftwidth=4 noexpandtab:

\input{../../include/latex/tikz/header.tex}

% opening %{{{1

\begin{document}
\begin{tikzpicture}[scale=1.0]

% parameters %{{{1

	\tikzmath{
		\radius = 4.7 ;
		\rotation = 90 ;
		\numsidesfloat = 5.0 ;
		\numsides = int(\numsidesfloat) ;
		int \i ;
		for \i in {1,...,\numsides} {
			\ang{\i} = \rotation + 360 / \numsidesfloat * (\i-1) ;
		};
	}

% coordinates %{{{1

	\coordinate (O) at (0,0);

	\foreach \i in {1,...,\numsides}
		\coordinate (P_\i) at (\ang{\i}:\radius);

% intersections %{{{1

	\begin{pgfinterruptboundingbox}
		\foreach \i
			[
				evaluate=\i as \j using { int(mod(\i, \numsides) + 1) },
				evaluate=\i as \k using { int(mod(\i + 1, \numsides) + 1) },
				evaluate=\i as \l using { int(mod(\i + 2, \numsides) + 1) }
			]
			in {1,...,\numsides}
		{
			\tkzInterLL(P_\i,P_\k)(P_\j,P_\l)\tkzGetPoint{S_\j}
		}
	\end{pgfinterruptboundingbox}

% points, dots, vertices %{{{1

	\fill (O) circle (0.4mm);

	\foreach \i in {1,...,\numsides}
		\fill (P_\i) circle (0.4mm);

	\foreach \i in {1,...,\numsides}
		\fill (S_\i) circle (0.4mm);

% segments, sides, lines %{{{1

	\draw (P_1) \foreach \i in {2,...,\numsides} { -- (P_\i) } -- cycle;

	\draw (P_1) -- (P_3) -- (P_5) -- (P_2) -- (P_4) -- cycle;

% circles %{{{1

	\draw (O) circle (\radius);

% points, dots, vertices labels %{{{1

	\node[anchor=east] at ($ (O) + (220:0pt) $) {$O$};

	\node[above] at (P_1) {$P_1$};
	\node[left] at (P_2) {$P_2$};
	\node[below left] at (P_3) {$P_3$};
	\node[below right] at (P_4) {$P_4$};
	\node[right] at (P_5) {$P_5$};

	\node[above left] at (S_1) {$S_1$};
	\node[left] at (S_2) {$S_2$};
	\node[below] at (S_3) {$S_3$};
	\node[right] at (S_4) {$S_4$};
	\node[above right] at (S_5) {$S_5$};

	\node[left] at ($ (P_1)!0.6!(S_1) $) {$a_1$};
	\node[below left] at ($ (P_2)!0.6!(S_2) $) {$a_2$};
	\node[below right] at ($ (P_3)!0.6!(S_3) $) {$a_3$};
	\node[above right] at ($ (P_4)!0.6!(S_4) $) {$a_4$};
	\node[above] at ($ (P_5)!0.6!(S_5) $) {$a_5$};

	\node[right] at ($ (P_1)!0.6!(S_5) $) {$b_1$};
	\node[above] at ($ (P_2)!0.6!(S_1) $) {$b_2$};
	\node[above left] at ($ (P_3)!0.6!(S_2) $) {$b_3$};
	\node[below left] at ($ (P_4)!0.6!(S_3) $) {$b_4$};
	\node[below right] at ($ (P_5)!0.6!(S_4) $) {$b_5$};

	\node[below] at ($ (S_1)!0.5!(S_5) $) {$c_1$};
	\node[below right] at ($ (S_2)!0.5!(S_1) $) {$c_2$};
	\node[above right] at ($ (S_3)!0.5!(S_2) $) {$c_3$};
	\node[above left] at ($ (S_4)!0.5!(S_3) $) {$c_4$};
	\node[below left] at ($ (S_5)!0.5!(S_4) $) {$c_5$};

% segments, sides, lines labels %{{{1

	%\node[above left] at ($ (P_1)!0.5!(P_2) $) {$c$};

% segments, sides, lines marks %{{{1

	%\foreach \i in {1,...,\numsides}
		%\tkzMarkSegments[mark=||, size=2pt, pos=0.6](P_\i,S_\i);

	%\foreach \i
		%[
			%evaluate=\i as \j using { int(mod(\i,\numsides) + 1) }
		%]
		%in {1,...,\numsides}
	%{
		%\tkzMarkSegments[mark=||, size=2pt, pos=0.6](P_\j,S_\i);
	%}

% circles labels %{{{1

	\node at ($ (O) + (60:\radius + 0.3) $) {$\mathscr{C}$};

% angles labels %{{{1

	% ------- between diagonals

	\foreach \i
		[
			evaluate=\i as \j using { int(mod(\i - 1 + 2, \numsides) + 1) },
			evaluate=\i as \k using { int(mod(\i - 1 + 3, \numsides) + 1) }
		]
		in {1,...,\numsides}
	{
		\pic[draw, ->, "$36^\circ$", angle radius=1.5cm, angle eccentricity=1.2]
			{angle = P_\j--P_\i--P_\k};
	}

	% ------- iscosceles triangles outside the pentagram

	\foreach \i
		[
			evaluate=\i as \j using { int(mod(\i - 1 + 1, \numsides) + 1) },
			evaluate=\i as \k using { int(mod(\i - 1 + 2, \numsides) + 1) }
		]
		in {1,...,\numsides}
	{
		\pic[draw, ->, "$36^\circ$", angle radius=1.1cm, angle eccentricity=1.3]
			{angle = P_\i--P_\k--P_\j};
		\pic[draw, ->, "$36^\circ$", angle radius=1.1cm, angle eccentricity=1.3]
			{angle = P_\j--P_\i--P_\k};
	}

	% ------- pentagram tips

	\pic[draw, ->, "$\alpha$", angle radius=0.6cm, angle eccentricity=1.5]
		{angle = P_5--S_1--P_1};

	\pic[draw, ->, "$\beta$", angle radius=0.6cm, angle eccentricity=1.5]
		{angle = P_1--S_5--P_2};

	\pic[draw, ->, "$\lambda$", angle radius=0.8cm, angle eccentricity=1.5]
		{angle = P_1--S_1--P_2};

	\pic[draw, ->, "$\lambda$", angle radius=0.5cm, angle eccentricity=1.5]
		{angle = P_3--S_1--P_5};

	\pic[draw, ->, "$\mu$", angle radius=0.8cm, angle eccentricity=1.5]
		{angle = P_5--S_5--P_1};

	\pic[draw, ->, "$\mu$", angle radius=0.5cm, angle eccentricity=1.5]
		{angle = P_2--S_5--P_4};

% closing %{{{1

\end{tikzpicture}
\end{document}
