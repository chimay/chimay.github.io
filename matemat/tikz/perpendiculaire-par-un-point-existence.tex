% vim: set ft=tex tabstop=4 shiftwidth=4 noexpandtab:

\input{../../include/latex/tikz/header.tex}

% opening %{{{1

\begin{document}
\begin{tikzpicture}[scale=1.0]

% parameters %{{{1

	\tikzmath{
		\len = 6;
		\height = 4;
		\extratio = 1.5;
		\radius = 0.65*\len ;
		\distPA = sqrt(\len*\len/4 + \height*\height);
	}

% coordinates %{{{1

	\coordinate (P) at ({\len/2},\height);

	\coordinate (A) at (0,0);
	\coordinate (B) at (\len,0);

	\coordinate (F) at ({\len/2},0);

	\coordinate (U) at ({-(\extratio - 1)*\len},0);
	\coordinate (V) at ({\extratio*\len},0);

	\coordinate (X) at ($(F) - (0, 2)$);
	\coordinate (Y) at ($(P) + (0, 2)$);

% points, dots, vertices %{{{1

	\fill (A) circle (0.5mm);
	\fill (B) circle (0.5mm);

	\fill (P) circle (0.5mm);

	\fill (F) circle (0.5mm);

% segments %{{{1

	\draw (U) -- (V);
	\draw (X) -- (Y);
	\draw[dashed] (P) -- (A);
	\draw[dashed] (P) -- (B);

% circular arcs %{{{1

	\draw (P) ++(200:\distPA) arc (200:340:\distPA);

% points, dots, vertices labels %{{{1

	\node[below] at (A) {$A$};
	\node[below] at (B) {$B$};

	\node[above left] at (P) {$P$};

	\node[above left] at (F) {$F$};

% segments, sides labels %{{{1

	\node[above] at ($ (U)!0.05!(V) $) {$d$};
	\node[left] at ($ (X)!0.5!(Y) $) {$p$};

	\node[above left] at ($ (P)!0.5!(A) $) {$r$};
	\node[above right] at ($ (P)!0.5!(B) $) {$r$};

% segments, sides marks %{{{1

	\tkzMarkSegments[mark=|, size=2pt](P,A)
	\tkzMarkSegments[mark=|, size=2pt](P,B)

	\tkzMarkSegments[mark=||, size=2pt](A,F)
	\tkzMarkSegments[mark=||, size=2pt](F,B)

% circular arcs labels %{{{1

	\node at ($ (P) + (320:{\distPA+0.3})$) {$\mathscr{C}$};

% angles labels %{{{1

	\pic[draw, ->, "$\alpha$", angle radius=0.8cm, angle eccentricity=1.5]
	{angle = B--F--P};

	\pic[draw, ->, "$\beta$", angle radius=0.7cm, angle eccentricity=1.5]
	{angle = P--F--A};

% right angles markers %{{{2

	%\pic [draw, angle radius=7pt, angle eccentricity=1]
	%{right angle = B--F--Y};

% closing %{{{1

\end{tikzpicture}
\end{document}
