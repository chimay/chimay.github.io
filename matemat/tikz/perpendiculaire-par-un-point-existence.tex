% vim: set ft=tex tabstop=4 shiftwidth=4 noexpandtab:

\input{../../include/latex/tikz/header.tex}

% opening %{{{1

\begin{document}
\begin{tikzpicture}[scale=1.0]

% parameters %{{{1

	\tikzmath{
		\len = 6;
		\height = 4;
		\extratio = 1.5;
		\radius = 0.65*\len ;
		\distAB = sqrt(\len*\len/4 + \height*\height);
	}

% coordinates %{{{1

	\coordinate (A) at ({\len/2},\height);

	\coordinate (B) at (0,0);
	\coordinate (D) at (\len,0);

	\coordinate (E) at ({\len/2},0);

	\coordinate (U) at ({-(\extratio - 1)*\len},0);
	\coordinate (V) at ({\extratio*\len},0);

	\coordinate (X) at ($(E) - (0, 3)$);
	\coordinate (Y) at ($(A) + (0, 2)$);

% points, dots, vertices %{{{1

	\fill (A) circle (0.5mm);
	\fill (B) circle (0.5mm);
	\fill (D) circle (0.5mm);
	\fill (E) circle (0.5mm);

% segments %{{{1

	\draw (U) -- (V);
	\draw (X) -- (Y);

	\draw[dashed] (A) -- (B);
	\draw[dashed] (A) -- (D);

% circular arcs %{{{1

	\draw (A) ++(200:\distAB) arc (200:340:\distAB);

% points, dots, vertices labels %{{{1

	\node[above left] at (A) {$A$};

	\node[below] at (B) {$B$};
	\node[below] at (D) {$D$};
	\node[below left] at (E) {$E$};

% segments, sides labels %{{{1

	\node[above] at ($ (U)!0.05!(V) $) {$d$};
	\node[left] at ($ (X)!0.1!(Y) $) {$p$};

	\node[above left] at ($ (A)!0.5!(B) $) {$r$};
	\node[above right] at ($ (A)!0.5!(D) $) {$r$};

% segments, sides marks %{{{1

	\tkzMarkSegments[mark=|, size=2pt](A,B)
	\tkzMarkSegments[mark=|, size=2pt](A,D)

	\tkzMarkSegments[mark=||, size=2pt](B,E)
	\tkzMarkSegments[mark=||, size=2pt](E,D)

% circular arcs labels %{{{1

	\node at ($ (A) + (320:{\distAB+0.3})$) {$\mathscr{C}$};

% angles labels %{{{1

	\pic[draw, ->, "$\alpha$", angle radius=0.8cm, angle eccentricity=1.5]
	{angle = D--E--A};

	\pic[draw, ->, "$\beta$", angle radius=0.7cm, angle eccentricity=1.5]
	{angle = A--E--B};

% closing %{{{1

\end{tikzpicture}
\end{document}
