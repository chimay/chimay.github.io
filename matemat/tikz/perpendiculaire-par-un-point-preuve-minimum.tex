% vim: set ft=tex tabstop=4 shiftwidth=4 noexpandtab:

\input{../../include/latex/tikz/header-light.tex}

% opening %{{{1

\begin{document}
\begin{tikzpicture}[scale=1.0]

% parameters %{{{1

	\def\len{2.0}
	\def\gth{4.0}

	\def\extratio{0.9}
	\def\triangleratio{1.5}

	\def\ang{0}
	\def\gle{90}

% vectors %{{{1

	\coordinate (u) at (\ang:1);
	\coordinate (v) at (\gle:1);

% coordinates %{{{1

	\coordinate (I) at (0,0);
	\coordinate (A) at ($ (I) + \len*(v) $);
	\coordinate (B) at ($ (I) - \len*(v) $);
	\coordinate (C) at ($ (I) + \triangleratio*\len*(u) $);

	\coordinate (X1) at ($ (I) - \gth*(u) $);
	\coordinate (X2) at ($ (I) + \gth*(u) $);

	\coordinate (Y1) at ($ (I) - \extratio*\gth*(v) $);
	\coordinate (Y2) at ($ (I) + \extratio*\gth*(v) $);

% vertices, points, dots %{{{1

	\fill (I) circle (0.4mm);
	\fill (A) circle (0.4mm);
	\fill (B) circle (0.4mm);
	\fill (C) circle (0.4mm);

% segments %{{{1

	\draw (X1) -- (X2);
	\draw (Y1) -- (Y2);

	\draw[very thin] (A) -- (C);
	\draw[very thin] (B) -- (C);

% points, dots, vertices labels %{{{1

	\node[above left] at (I) {$I$};
	\node[left] at (A) {$A$};
	\node[left] at (B) {$B$};
	\node[above right] at (C) {$C$};

% segments, sides labels %{{{1

	\node[above] at ($ (X1)!0.1!(X2) $) {$d$};
	\node[above left] at ($ (Y1)!0.05!(Y2) $) {$p$};

% segments, sides marks %{{{1

	\tkzMarkSegments[mark=||, size=2pt](A,I)
	\tkzMarkSegments[mark=||, size=2pt](B,I)

% angles labels %{{{1

% right angles markers %{{{2

	\pic[draw, angle radius=0.3cm, angle eccentricity=1.3]
	{right angle = X2--I--Y2};

	%\pic[draw, angle radius=0.3cm, angle eccentricity=1.3]
	%{right angle = B--I--C};

% closing %{{{1

\end{tikzpicture}
\end{document}
