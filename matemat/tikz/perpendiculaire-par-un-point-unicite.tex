% vim: set ft=tex tabstop=4 shiftwidth=4 noexpandtab:

\input{../../include/latex/tikz/header-light.tex}

% opening %{{{1

\begin{document}
\begin{tikzpicture}[scale=1.0]

% parameters %{{{1

	\def\len{2.9}
	\def\gth{2.6}

	\def\ang{-10}
	\def\gle{80}

% vectors %{{{1

	\coordinate (u) at (\ang:1);
	\coordinate (v) at (\gle:1);

% coordinates %{{{1

	\coordinate (I) at (0,0);
	\coordinate (P) at ($ (I) + \len*(v) $);
	\coordinate (B) at ($ (I) - 0.5*(u) $);
	\coordinate (C) at ($ (I) + 0.7*(u) $);

	\coordinate (X1) at ($ (I) - \gth*(u) $);
	\coordinate (X2) at ($ (I) + \gth*(u) $);

	%\coordinate (Y1) at ($ (I) - \gth*(v) $);
	%\coordinate (Y2) at ($ (I) + \gth*(v) $);

% vertices, points, dots %{{{1

	\fill (P) circle (0.4mm);
	\fill (B) circle (0.4mm);
	\fill (C) circle (0.4mm);

% segments %{{{1

	\draw (X1) -- (X2);
	%\draw (Y1) -- (Y2);

	\draw (P) -- (B);
	\draw (P) -- (C);

% points, dots, vertices labels %{{{1

	\node[above] at (P) {$P$};
	\node[below left] at (B) {$B$};
	\node[below] at (C) {$C$};

% segments, sides labels %{{{1

	\node[above] at ($ (X1)!0.9!(X2) $) {$d$};

	\node[above left] at ($ (P)!0.5!(B) $) {$f$};
	\node[above right] at ($ (P)!0.5!(C) $) {$g$};

% angles labels %{{{1

	\pic[draw, angle radius=0.3cm, angle eccentricity=1.3]
	{right angle = X2--B--P};

	\pic[draw, angle radius=0.3cm, angle eccentricity=1.3]
	{right angle = P--C--X1};

% closing %{{{1

\end{tikzpicture}
\end{document}
