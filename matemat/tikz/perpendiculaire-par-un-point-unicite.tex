% vim: set ft=tex tabstop=4 shiftwidth=4 noexpandtab:

\input{../../include/latex/tikz/header-light.tex}

% opening %{{{1

\begin{document}
\begin{tikzpicture}[scale=1.0]

% parameters %{{{1

	\def\len{3.1}
	\def\gth{3.2}

	\def\ang{0}
	\def\gle{90}

% vectors %{{{1

	\coordinate (u) at (\ang:1);
	\coordinate (v) at (\gle:1);

% coordinates %{{{1

	\coordinate (I) at (0,0);
	\coordinate (A) at ($ (I) + \len*(v) $);
	\coordinate (B) at ($ (I) - 0.7*(u) $);
	\coordinate (C) at ($ (I) + 0.9*(u) $);

	\coordinate (X1) at ($ (I) - \gth*(u) $);
	\coordinate (X2) at ($ (I) + \gth*(u) $);

	\coordinate (Y) at ($ (B) + 0.7*(B) - 0.7*(A) $);
	\coordinate (Z) at ($ (C) + 0.7*(C) - 0.7*(A) $);

% vertices, points, dots %{{{1

	\fill (A) circle (0.4mm);
	\fill (B) circle (0.4mm);
	\fill (C) circle (0.4mm);

% segments %{{{1

	\draw (X1) -- (X2);

	\draw (A) -- (B);
	\draw (A) -- (C);

	\draw (B) -- (Y);
	\draw (C) -- (Z);

% points, dots, vertices labels %{{{1

	\node[above] at (A) {$A$};
	\node[above left] at (B) {$B$};
	\node[above right] at (C) {$C$};

% segments, sides labels %{{{1

	\node[above] at ($ (X1)!0.9!(X2) $) {$d$};

	\node[above left] at ($ (B)!0.7!(Y) $) {$f$};
	\node[above right] at ($ (C)!0.7!(Z) $) {$g$};

% angles labels %{{{1

	\pic[draw, angle radius=0.3cm, angle eccentricity=1.3]
	{right angle = X2--B--A};

	\pic[draw, angle radius=0.3cm, angle eccentricity=1.3]
	{right angle = A--C--X1};

% closing %{{{1

\end{tikzpicture}
\end{document}
