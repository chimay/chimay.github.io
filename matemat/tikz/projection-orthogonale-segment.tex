% vim: set ft=tex tabstop=4 shiftwidth=4 noexpandtab:

\input{../../include/latex/tikz/header-light.tex}

% opening %{{{1

\begin{document}
\begin{tikzpicture}[scale=1.0]

% parameters %{{{1

	\def\len{5}
	\def\height{3}

% coordinates %{{{1

	\coordinate (A) at (4,0);
	\coordinate (P) at (4,-\height);

	\coordinate (B) at (0,-\height);
	\coordinate (C) at (7,-\height);

	\coordinate (X) at (-2,-\height);
	\coordinate (Y) at (9,-\height);

% points, dots, vertices %{{{1

	\fill (A) circle (0.4mm);
	\fill (B) circle (0.4mm);
	\fill (C) circle (0.4mm);

	\fill (P) circle (0.4mm);

% segments, sides, lines %{{{1

	\draw (A) -- (P);
	\draw (B) -- (C);

	\draw[dashed] (X) -- (B);
	\draw[dashed] (C) -- (Y);

% points, dots, vertices labels %{{{1

	\node[above] at (A) {$A$};
	\node[below] at (B) {$B$};
	\node[below] at (C) {$C$};

	\node[below] at (P) {$P$};

% segments, sides, lines labels %{{{1

	\node[above] at ($ (X)!0.2!(B) $) {$d$};

% angles labels %{{{1

	%\pic[draw, ->, "$\alpha$", angle radius=1.2cm, angle eccentricity=1.3]
	%{angle = P--O--A};

% right angles markers %{{{2

	\pic [draw, angle radius=7pt, angle eccentricity=1]
	{right angle = A--P--B};

% closing %{{{1

\end{tikzpicture}
\end{document}
