% vim: set ft=tex tabstop=4 shiftwidth=4 noexpandtab:

\input{../../include/latex/tikz/header-light.tex}

% opening %{{{1

\begin{document}
\begin{tikzpicture}[scale=1.0]

% parameters %{{{1

	\def\len{1.9}
	\def\gth{2.9}

	\def\ang{0}
	\def\gle{90}

% vectors %{{{1

	\coordinate (u) at (\ang:1);
	\coordinate (v) at (\gle:1);

% coordinates %{{{1

	\coordinate (P) at (0,0);
	\coordinate (A) at ($ (P) + \len*(v) $);

	\coordinate (X1) at ($ (P) - \gth*(u) $);
	\coordinate (X2) at ($ (P) + \gth*(u) $);

	\coordinate (Y1) at ($ (P) - {\gth/2}*(v) $);
	\coordinate (Y2) at ($ (P) + \gth*(v) $);

% vertices, points, dots %{{{1

	\fill (P) circle (0.4mm);
	\fill (A) circle (0.4mm);

% segments %{{{1

	\draw (X1) -- (X2);
	\draw (Y1) -- (Y2);

% points, dots, vertices labels %{{{1

	\node[above left] at (P) {$P$};
	\node[above left] at (A) {$A$};

% segments, sides labels %{{{1

	\node[above] at ($ (X1)!0.9!(X2) $) {$d$};
	\node[above left] at ($ (Y1)!0.1!(Y2) $) {$p$};

% angles labels %{{{1

	\pic[draw, angle radius=0.3cm, angle eccentricity=1.3]
	{right angle = X2--P--Y2};

% closing %{{{1

\end{tikzpicture}
\end{document}
