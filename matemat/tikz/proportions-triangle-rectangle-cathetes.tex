
\input{../../include/latex/tikz/header-light.tex}

% opening %{{{1

\begin{document}
\begin{tikzpicture}[scale=1.0]

% parameters %{{{1

	\def\ratio{1.7}

% coordinates %{{{1

	\coordinate (A) at (0,0);
	\coordinate (B) at (4,0);
	\coordinate (C) at (4,3);
	\coordinate (D) at ({\ratio*4},0);
	\coordinate (E) at ({\ratio*4},{\ratio*3});

% points, dots, vertices %{{{1

	\fill (A) circle (0.4mm);
	\fill (B) circle (0.4mm);
	\fill (C) circle (0.4mm);
	\fill (D) circle (0.4mm);
	\fill (E) circle (0.4mm);

% lines %{{{1

	% triangle

	\draw (A) -- (D) -- (E) -- cycle;
	\draw (B) -- (C);

% points, dots, vertices labels %{{{1

	\node[below left] at (A) {$A$};
	\node[below] at (B) {$B$};
	\node[above left] at (C) {$C$};
	\node[below right] at (D) {$D$};
	\node[above right] at (E) {$E$};

% side labels %{{{1

	% ---- a opposite A, etc.

	\node[below] at ($(A)!0.5!(B)$) {$a$};
	\node[above left]  at ($(A)!0.5!(C)$) {$c$};
	\node[right] at ($(B)!0.5!(C)$) {$b$};

	\node[below] at ($(B)!0.5!(D)$) {$d$};
	\node[right]  at ($(D)!0.5!(E)$) {$f$};
	\node[above left] at ($(E)!0.5!(C)$) {$g$};

% right angle marks %{{{1

	\tkzMarkRightAngle[size=0.2](C,B,A)
	\tkzMarkRightAngle[size=0.2](E,D,B)

% closing %{{{1

\end{tikzpicture}
\end{document}
