% vim: set ft=tex tabstop=4 shiftwidth=4 noexpandtab:

% standard template with header in another file

\input{../../include/latex/tikz/header.tex}

% opening %{{{1

\begin{document}
\begin{tikzpicture}[scale=1.0]

% parameters %{{{1

	\def\len{7}
	\def\gth{4}

% coordinates %{{{1

	\coordinate (A) at (0,0);
	\coordinate (B) at (\len,0);
	\coordinate (C) at (\len,\gth);
	\coordinate (D) at (0,\gth);

% intersections %{{{1

	\path[name path=diagonal1] (A) -- (C);
	\path[name path=diagonal2] (B) -- (D);
	\path[name intersections={of=diagonal1 and diagonal2, by=I}];

% points, dots, vertices %{{{1

	\fill (A) circle (0.4mm);
	\fill (B) circle (0.4mm);
	\fill (C) circle (0.4mm);
	\fill (D) circle (0.4mm);
	\fill (I) circle (0.4mm);

% segments, sides %{{{1

	\draw (A) -- (B) -- (C) -- (D) -- cycle;
	\draw[dashed] (A) -- (C);
	\draw[dashed] (B) -- (D);

% points, dots, vertices labels %{{{1

	\node[below left] at (A) {$A$};
	\node[below right] at (B) {$B$};
	\node[above right] at (C) {$C$};
	\node[above left] at (D) {$D$};
	\node[above] at (I) {$I$};

% segments, sides labels %{{{1

	\node[below] at ($ (A)!0.5!(B) $) {$L$};
	\node[above] at ($ (C)!0.5!(D) $) {$L$};

	\node[left] at ($ (A)!0.5!(D) $) {$l$};
	\node[right] at ($ (B)!0.5!(C) $) {$l$};

% segments, sides marks %{{{1

	\tkzMarkSegments[mark=||, size=2pt](A,I)
	\tkzMarkSegments[mark=||, size=2pt](B,I)
	\tkzMarkSegments[mark=||, size=2pt](C,I)
	\tkzMarkSegments[mark=||, size=2pt](D,I)

% angles labels %{{{1

% right angles markers %{{{2

	\pic [draw, angle radius=7pt, angle eccentricity=1]
	{right angle = B--A--D};

	\pic [draw, angle radius=7pt, angle eccentricity=1]
	{right angle = C--B--A};

	\pic [draw, angle radius=7pt, angle eccentricity=1]
	{right angle = D--C--B};

	\pic [draw, angle radius=7pt, angle eccentricity=1]
	{right angle = A--D--C};

% closing %{{{1

\end{tikzpicture}
\end{document}
