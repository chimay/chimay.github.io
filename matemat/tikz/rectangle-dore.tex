
% header %{{{1

\documentclass[tikz, border=1mm]{standalone}

\usepackage{amsmath}

\usetikzlibrary{calc,angles,quotes}

\usepackage{tkz-euclide}

% document %{{{1

% opening %{{{2

\begin{document}
\begin{tikzpicture}[scale=1.0]

% parameters %{{{2

	\def\len{4}

% coordinates %{{{2

	\coordinate (A) at (0,0);
	\coordinate (B) at (0,\len);
	\coordinate (C) at (1.618*\len,\len);
	\coordinate (D) at (1.618*\len,0);
	\coordinate (E) at (\len,0);
	\coordinate (F) at (\len,\len);

% vertices %{{{2

	\fill (A) circle (0.4mm);
	\fill (B) circle (0.4mm);
	\fill (C) circle (0.4mm);
	\fill (D) circle (0.4mm);
	\fill (E) circle (0.4mm);
	\fill (F) circle (0.4mm);

% big rectangle %{{{2

	\draw (A) -- (B) -- (C) -- (D) -- cycle;

	% partition

	\draw (E) -- (F);

% vertices labels %{{{2

	\node[below] at (A) {$A$};
	\node[above] at (B) {$B$};
	\node[above] at (C) {$C$};
	\node[below] at (D) {$D$};
	\node[below] at (E) {$E$};
	\node[above] at (F) {$F$};

	\node[left]  at ($(A)!0.5!(B)$) {$a$};
	\node[left]  at ($(E)!0.5!(F)$) {$a$};
	\node[left]  at ($(D)!0.5!(C)$) {$a$};
	\node[above] at ($(B)!0.5!(F)$) {$a$};
	\node[below] at ($(A)!0.5!(E)$) {$a$};
	\node[above]  at ($(F)!0.5!(C)$) {$b$};
	\node[below]  at ($(E)!0.5!(D)$) {$b$};

\end{tikzpicture}
\end{document}
