% vim: set ft=tex tabstop=4 shiftwidth=4 noexpandtab:

% header %{{{1

% opening %{{{2

\documentclass[tikz, border=1mm]{standalone}

% packages and libraries %{{{2

\usepackage{../../include/latex/custom}

\usetikzlibrary{calc,intersections,angles,quotes}
%\usetikzlibrary{calc,intersections,angles,quotes,shapes.geometric,arrows.meta}

\usepackage{tkz-euclide}

% colors %{{{2

\definecolor{goldenbrown}{HTML}{5b3c11}

% style %{{{2

\tikzset{
	% ------- every something
	every picture/.style={
		scale=1.0,
	},
	every coordinate/.style={
		fill=black, circle, inner sep=1pt,
	},
	every path/.style={
		line width=0.3pt,
	},
	every node/.style={
		font=\normalsize,
	},
	every angle/.style={
	},
	every pic/.style={
		% ---- does not worl
		%draw,
		%-{Straight Barb[length=1.2mm]},
	},
	% ------- custom
	vector/.style={
		-{Straight Barb[length=1.2mm]},
		%thick,
	},
	construction/.style={
		line width=0.1pt,
		dashed,
	},
	dimension/.style={
		line width=0.2pt,
		<->,
		goldenbrown,
	},
	dimension extension/.style={
		line width=0.2pt,
		dashed,
		goldenbrown,
	},
}

% document %{{{1

% opening %{{{2

\begin{document}
\begin{tikzpicture}[scale=1.0]

% parameters %{{{2

	\def\len{3}
	\def\ext{1.2}
	\def\ension{1.9}

	\def\ang{20}

% coordinates %{{{2

	\coordinate (X) at (0,{-\ext*\len});
	\coordinate (Y) at (0,{\ext*\len});

	\coordinate (A) at (-\len,0);
	\coordinate (B) at (\len,0);

	\coordinate (E1) at ({-\ension*\len},0);
	\coordinate (E2) at ({\ension*\len},0);

% intersections %{{{2

	\path[name path=line2] (X) -- (Y);
	\path[name path=line1] (A) -- (B);
	\path[name intersections={of=line1 and line2, by=I}];

% points, dots, vertices %{{{2

	\fill (A) circle (0.4mm);
	\fill (B) circle (0.4mm);
	\fill (I) circle (0.4mm);

% segments, sides %{{{2

	\draw (X) -- (Y);
	\draw (A) -- (B);
	\draw[dashed] (E1) -- (A);
	\draw[dashed] (B) -- (E2);

% points, dots, vertices labels %{{{2

	\node[below left] at (A) {$A$};
	\node[below right] at (B) {$B = \mathcal{R}(A)$};
	\node[below left] at (I) {$I$};

% segments, sides labels %{{{2

	\node[left] at ($ (X)!0.9!(Y) $) {$d$};
	\node[above] at ($ (E1)!0.1!(E2) $) {$p$};

% segments, sides marks %{{{2

	\tkzMarkSegments[mark=||, size=2pt](A,I)
	\tkzMarkSegments[mark=||, size=2pt](I,B)

% right angles markers %{{{3

	\pic [draw, angle radius=7pt, angle eccentricity=1]
	{right angle = B--I--Y};

% closing %{{{2

\end{tikzpicture}
\end{document}
