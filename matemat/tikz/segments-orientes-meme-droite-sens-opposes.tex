% vim: set ft=tex :

% header %{{{1

% opening %{{{2

\documentclass[tikz, border=1mm]{standalone}

% packages and libraries %{{{2

% ---- not necessary since the documentclass[tikz ...] requires it
% \usepackage{tikz}

\usepackage{amsmath}
\usepackage{mathrsfs}
\usepackage[x11names]{xcolor}

\usetikzlibrary{calc,angles,quotes,intersections,shapes.geometric}

\usepackage{tkz-euclide}

% colors %{{{2

\definecolor{goldenbrown}{HTML}{5b3c11}

%\definecolor{somebrown}{RGB}{101,67,33}

%\colorlet{somebrown}{brown!80!black}

% style %{{{2

\tikzset{
	% ------- every something
	every picture/.style={
		scale=1.0,
	},
	every coordinate/.style={
		fill=black, circle, inner sep=1pt,
	},
	every path/.style={
		line width=0.3pt,
	},
	every node/.style={
		font=\normalsize,
	},
	every angle/.style={
	},
	every pic/.style={
		% ---- does not worl
		%draw,
		%-{Straight Barb[length=1.2mm]},
	},
	% ------- custom
	construction/.style={
		line width=0.1pt,
		dashed,
	},
	vector/.style={
		-{Straight Barb[length=1.7mm]},
		%thick,
	},
	dimension/.style={
		line width=0.2pt,
		<->,
		goldenbrown,
	},
	dimension extension/.style={
		line width=0.2pt,
		dashed,
		goldenbrown,
	},
}

% document %{{{1

% opening %{{{2

\begin{document}
\begin{tikzpicture}[scale=1.0]

% parameters %{{{2

	\def\len{2.9}
	\def\ratio{1.5}
	\def\ang{5}

% coordinates %{{{2

	\coordinate (A) at (0,0);
	\coordinate (B) at (\ang:\len);

	\coordinate (t) at (\ang:\len);

	\coordinate (C) at ($ (B) + \ratio*(t) $);
	\coordinate (D) at ($ (A) + \ratio*(t) $);

	\coordinate (X) at ($ (A) - 0.5*(t) $);
	\coordinate (Y) at ($ (C) + 0.5*(t) $);

% segments %{{{2

	\draw[dashed] (X) -- (A);
	\draw[vector] (A) -- (B);
	\draw[dashed] (B) -- (C);
	\draw[vector] (C) -- (D);
	\draw[dashed] (D) -- (Y);

% points, dots, vertices labels %{{{2

	\fill (A) circle (0.4mm);
	\fill (B) circle (0.4mm);
	\fill (C) circle (0.4mm);
	\fill (D) circle (0.4mm);

% vertices labels %{{{2

	\node[above] at (A) {$A$};
	\node[above] at (B) {$B$};
	\node[above] at (C) {$C$};
	\node[above] at (D) {$D$};

% segments, sides labels %{{{2

	\node[above] at ($ (X)!0.95!(Y) $) {$d$};

% segments, sides marks %{{{2

	\tkzMarkSegments[mark=||, size=2pt](A,B)
	\tkzMarkSegments[mark=||, size=2pt](C,D)

% closing %{{{2

\end{tikzpicture}
\end{document}
