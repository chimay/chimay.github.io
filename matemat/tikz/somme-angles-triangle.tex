
% header %{{{1

\documentclass[tikz, border=1mm]{standalone}

\usepackage{amsmath}

\usetikzlibrary{calc,angles,quotes,arrows.meta}

\tikzset{
	% ---- default
	every path/.style={line width=0.3pt},
	every coordinate/.style={fill=black, circle, inner sep=1pt},
	every node/.style={font=\normalsize},
	every picture/.style={scale=1.0},
	every angle/.style={
		draw,
		line width=1.6pt,
		-{Latex[length=7mm,width=3mm]}
	},
	% ---- custom
	construction/.style={line width=0.1pt, dashed},
	dimension/.style={line width=0.2pt, <->, goldenbrown},
	dimension extension/.style={line width=0.2pt, dashed, goldenbrown},
	vector/.style={->, thick},
}

% document %{{{1

% opening %{{{2

\begin{document}

\begin{tikzpicture}[scale=1.0]

% floating vectors %{{{2

	\coordinate (v1) at (-1,-1);
	\coordinate (v2) at (2,-3);

% unit vectors %{{{3

	\coordinate (u1) at ($ (0,0)!1cm!(v1) $);
	\coordinate (u2) at ($ (0,0)!1cm!(v2) $);

% coordinates %{{{2

	\coordinate (A) at (0,0);
	\coordinate (B) at (5,0);
	\coordinate (C) at (3,3);

	\coordinate (X1) at (-2,0);
	\coordinate (X2) at (7,0);

	\coordinate (Y1) at (0,3);
	\coordinate (Y2) at (6,3);

	\coordinate (Z1) at ($ (A) + 1.5*(u1) $);
	\coordinate (Z2) at ($ (B) + 1.5*(u2) $);

% vertices, points, dots %{{{2

	\fill (A) circle (0.4mm);
	\fill (B) circle (0.4mm);
	\fill (C) circle (0.4mm);

% triangle sides %{{{2

	\draw (A) -- (C);
	\draw (B) -- (C);
	\draw (X1) -- (X2);
	\draw (Y1) -- (Y2);
	\draw (Z1) -- (A);
	\draw (Z2) -- (B);

% vertices labels %{{{2

	\node[above left] at (A) {$A$};
	\node[above right] at (B) {$B$};
	\node[above] at (C) {$C$};

% segments, sides labels %{{{2

	\node[above] at ($ (X1)!0.95!(X2) $) {$a$};
	\node[above] at ($ (Y1)!0.9!(Y2) $) {$b$};
	\node[left] at ($ (Z1)!0.4!(A) $) {$c$};
	\node[right] at ($ (Z2)!0.4!(B) $) {$d$};

% angles labels %{{{2

	\pic[draw, ->, "$\alpha$", angle eccentricity=1.3, angle radius=1.2cm]
	{angle = B--A--C};
	\pic[draw, ->, "$\alpha$", angle eccentricity=1.3, angle radius=1.2cm]
	{angle = Y1--C--A};

	\pic[draw, ->, "$\beta$", angle eccentricity=1.3, angle radius=1.2cm]
	{angle = C--B--A};
	\pic[draw, ->, "$\beta$", angle eccentricity=1.3, angle radius=1.2cm]
	{angle = B--C--Y2};

	\pic[draw, ->, "$\gamma$", angle eccentricity=1.3, angle radius=1.0cm]
	{angle = A--C--B};

% closing %{{{2

\end{tikzpicture}

\end{document}
