
\input{../../include/latex/tikz/header-light.tex}

% opening %{{{1

\begin{document}
\begin{tikzpicture}[scale=1.0]

% parameters %{{{1

	\tikzmath{
		\len = 4.0 ;
		\gth = 2 * \len ;
		\ang{1} = 50 ;
		\ang{2} = 70 ;
		\ratio = 0.7 ;
		\lenratio = \len * \ratio ;
	}

% coordinates %{{{1

	\coordinate (A) at (0,0);
	\coordinate (B) at (\len,0);

	\coordinate (X) at ($ (A) - (\lenratio,0) $);
	\coordinate (Y) at ($ (B) + (\lenratio,0) $);

% intersections %{{{1

	\begin{pgfinterruptboundingbox}
		\coordinate (ACX) at ($ (A) + (\ang{1}:\gth) $);
		\coordinate (BCY) at ($ (B) + ({ 180 - \ang{2} }:\gth) $);
		\path[name path=line1] (A) -- (ACX);
		\path[name path=line2] (B) -- (BCY);
		\path[name intersections={of=line1 and line2, by=C}];
	\end{pgfinterruptboundingbox}

	\coordinate (CX) at ($ (C) - (\lenratio,0) $);
	\coordinate (CY) at ($ (C) + (\lenratio,0) $);

% points, dots, vertices %{{{1

	\fill (A) circle (0.4mm);
	\fill (B) circle (0.4mm);
	\fill (C) circle (0.4mm);

% segments, sides, lines %{{{1

	\draw (A) -- (B) -- (C) -- cycle;

	\draw (X) -- (A) ;
	\draw (B) -- (Y) ;

	\draw (CX) -- (CY) ;

% points, dots, vertices labels %{{{1

	\node[below] at (A) {$A$};
	\node[below] at (B) {$B$};
	\node[above] at (C) {$C$};

% segments, sides, lines labels %{{{1

	\node[above] at ($ (B)!0.7!(Y) $) {$d$};
	\node[above] at ($ (C)!0.7!(CY) $) {$f$};

% angles labels %{{{1

	\pic[draw, ->, "$\alpha$", angle radius=0.8cm, angle eccentricity=1.5]
		{angle = B--A--C};

	\pic[draw, ->, "$\beta$", angle radius=0.8cm, angle eccentricity=1.5]
		{angle = C--B--A};

	\pic[draw, ->, "$\gamma$", angle radius=0.8cm, angle eccentricity=1.5]
		{angle = A--C--B};

	\pic[draw, ->, "$\alpha$", angle radius=0.7cm, angle eccentricity=1.5]
		{angle = CX--C--A};

	\pic[draw, ->, "$\beta$", angle radius=0.7cm, angle eccentricity=1.5]
		{angle = B--C--CY};

% closing %{{{1

\end{tikzpicture}
\end{document}
