% vim: set ft=tex tabstop=4 shiftwidth=4 noexpandtab:

\input{../../include/latex/tikz/header-light.tex}

% opening %{{{1

\begin{document}
\begin{tikzpicture}[scale=0.8]

% parameters %{{{1

	\def\ratio{1.7}
	\def\ension{1.2}
	\def\ext{0.4}

% coordinates %{{{2

	\coordinate (A) at (0,0);
	\coordinate (B) at (-2,-3);
	\coordinate (C) at (1,-3);

	\coordinate (u) at ($ (B) - (A) $);
	\coordinate (v) at ($ (C) - (A) $);

	\coordinate (D) at ($ (A) + \ratio*(u) $);
	\coordinate (E) at ($ (A) + \ratio*(v) $);

	\coordinate (UX) at ($ (A) - \ext*(u) $);
	\coordinate (VX) at ($ (A) - \ext*(v) $);

	\coordinate (UY) at ($ (A) + \ension*\ratio*(u) $);
	\coordinate (VY) at ($ (A) + \ension*\ratio*(v) $);

	\coordinate (BX) at (-6,-3);
	\coordinate (BY) at (4,-3);

	\coordinate (CX) at ($ (D) - \ext*(4,0) $);
	\coordinate (CY) at ($ (E) + \ext*(4,0) $);

% points, dots, vertices %{{{1

	\fill (A) circle (0.4mm);
	\fill (B) circle (0.4mm);
	\fill (C) circle (0.4mm);
	\fill (D) circle (0.4mm);
	\fill (E) circle (0.4mm);

% segments, sides, lines %{{{1

	\draw (UX) -- (UY);
	\draw (VX) -- (VY);

	\draw (BX) -- (BY);
	\draw (CX) -- (CY);

% points, dots, vertices labels %{{{1

	\node[left] at (A) {$A$};
	\node[above left] at (B) {$B$};
	\node[above right] at (C) {$C$};
	\node[above left] at (D) {$D$};
	\node[above right] at (E) {$E$};

% segments, sides, lines labels %{{{1

	%\node[above] at ($ (A)!0.5!(B) $) {$d$};

% segments, sides, lines marks %{{{1

	%\tkzMarkSegments[mark=||, size=2pt](A,B)
	%\tkzMarkSegments[mark=||, size=2pt](C,D)

% angles labels %{{{1

	%\pic[draw, ->, "$\alpha$", angle radius=0.8cm, angle eccentricity=1.5]
	%{angle = B--A--C};

% right angles markers %{{{2

	%\pic [draw, angle radius=7pt, angle eccentricity=1]
	%{right angle = C--B--A};

% closing %{{{1

\end{tikzpicture}
\end{document}
