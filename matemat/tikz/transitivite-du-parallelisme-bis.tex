% vim: set ft=tex tabstop=4 shiftwidth=4 noexpandtab:

\input{../../include/latex/tikz/header.tex}

% opening %{{{1

\begin{document}
\begin{tikzpicture}[scale=1.2]

% parameters %{{{1

	\def\len{4}
	\def\height{1}
	\def\ang{0}
	\def\gle{90}

% vectors %{{{1

	\coordinate (u) at (\ang:1);
	\coordinate (v) at (\gle:1);

% coordinates %{{{1

	\coordinate (A) at (0,0);
	\coordinate (X1) at ($ (A) - {1.2*\len}*(u) $);
	\coordinate (X2) at ($ (A) + {1.2*\len}*(u) $);

	\coordinate (B) at ($\height*(v)$);
	\coordinate (Y1) at ($ (B) - \len*(u) $);
	\coordinate (Y2) at ($ (B) + \len*(u) $);

	\coordinate (C) at (${2.25*\height}*(v)$);
	\coordinate (Z1) at ($ (C) - \len*(u) $);
	\coordinate (Z2) at ($ (C) + \len*(u) $);

	\coordinate (W1) at ($ (A) - {0.3*\len}*(v) $);
	\coordinate (W2) at ($ (C) + {0.3*\len}*(v) $);

% vertices, points, dots %{{{1

	\fill (A) circle (0.4mm);
	\fill (B) circle (0.4mm);
	\fill (C) circle (0.4mm);

% segments %{{{1

	\draw (X1) -- (X2);
	\draw (Y1) -- (Y2);
	\draw (Z1) -- (Z2);
	\draw (W1) -- (W2);

% segments, sides labels %{{{1

	\node[above] at ($ (X1)!0.9!(X2) $) {$a$};
	\node[above] at ($ (Y1)!0.9!(Y2) $) {$b$};
	\node[above] at ($ (Z1)!0.9!(Z2) $) {$c$};
	\node[left] at ($ (W1)!0.95!(W2) $) {$d$};

% angles labels %{{{1

% right angles markers %{{{2

	\pic[draw, angle radius=7pt, angle eccentricity=1.3]
	{right angle = X2--A--C};

	\pic[draw, angle radius=7pt, angle eccentricity=1.3]
	{right angle = Y2--B--C};

	%\pic[draw, angle radius=7pt, angle eccentricity=1.3]
	%{right angle = Z2--C--W2};

% closing %{{{1

\end{tikzpicture}
\end{document}
