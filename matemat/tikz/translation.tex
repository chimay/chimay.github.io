% vim: set ft=tex tabstop=4 shiftwidth=4 noexpandtab:

% header %{{{1

% opening %{{{2

\documentclass[tikz, border=1mm]{standalone}

% packages and libraries %{{{2

\usepackage{../../include/latex/custom}

\usetikzlibrary{calc,intersections,angles,quotes}
%\usetikzlibrary{calc,intersections,angles,quotes,shapes.geometric,arrows.meta}

\usepackage{tkz-euclide}

% colors %{{{2

\definecolor{goldenbrown}{HTML}{5b3c11}

% style %{{{2

\tikzset{
	% ------- every something
	every picture/.style={
		scale=1.0,
	},
	every coordinate/.style={
		fill=black, circle, inner sep=1pt,
	},
	every path/.style={
		line width=0.3pt,
	},
	every node/.style={
		font=\normalsize,
	},
	every angle/.style={
	},
	every pic/.style={
		% ---- does not worl
		%draw,
		%-{Straight Barb[length=1.2mm]},
	},
	% ------- custom
	vector/.style={
		-{Straight Barb[length=1.2mm]},
		%thick,
	},
	construction/.style={
		line width=0.1pt,
		dashed,
	},
	dimension/.style={
		line width=0.2pt,
		<->,
		goldenbrown,
	},
	dimension extension/.style={
		line width=0.2pt,
		dashed,
		goldenbrown,
	},
}

% document %{{{1

% opening %{{{2

\begin{document}
\begin{tikzpicture}[scale=1.0]

% parameters %{{{2

	\def\len{3}

	\def\ang{20}

% vectors %{{{2

	\coordinate (v) at (\ang:\len);

% unit vectors %{{{3

	\coordinate (u) at ($ (0,0)!1cm!(v) $);

% coordinates %{{{2

	\coordinate (X) at (-1,2);
	\coordinate (Y) at ($ (X) + (v) $);

	\coordinate (A) at (0,0);
	\coordinate (B) at ($ (A) + (v) $);

	\coordinate (E1) at ($ (A) - 2*(u) $);
	\coordinate (E2) at ($ (B) + 2*(u) $);

% points, dots, vertices %{{{2

	\fill (X) circle (0.4mm);
	\fill (Y) circle (0.4mm);
	\fill (A) circle (0.4mm);
	\fill (B) circle (0.4mm);

% segments, sides %{{{2

	\draw[dashed] (X) -- (A);
	\draw[dashed] (Y) -- (B);

	\draw[dashed] (E1) -- (A);
	\draw[dashed] (B) -- (E2);

% vectors %{{{3

	\draw[vector] (X) -- (Y);
	\draw[vector] (A) -- (B);

% points, dots, vertices labels %{{{2

	\node[above left] at (X) {$X$};
	\node[above right] at (Y) {$Y$};
	\node[below left] at (A) {$A$};
	\node[below right] at (B) {$B$};

% segments, sides labels %{{{2

	\node[above] at ($ (X)!0.5!(Y) $) {$\vecteur{v}$};
	\node[above] at ($ (A)!0.5!(B) $) {$\vecteur{v}$};
	\node[above] at ($ (E1)!0.9!(E2) $) {$d$};

% closing %{{{2

\end{tikzpicture}
\end{document}
