% vim: set ft=tex tabstop=4 shiftwidth=4 noexpandtab:

% standard template with header in another file

\input{../../include/latex/tikz/header.tex}

% opening %{{{1

\begin{document}
\begin{tikzpicture}[scale=1.0]

% parameters %{{{1

	%\def\len{3}
	%\def\gth{3}

% coordinates %{{{1

	\coordinate (A) at (0,0);
	\coordinate (B) at (7,0);
	\coordinate (C) at (6,3);
	\coordinate (D) at (3,3);

	\coordinate (X1) at (-2,3);
	\coordinate (X2) at (9,3);

% points, dots, vertices %{{{1

	\fill (A) circle (0.4mm);
	\fill (B) circle (0.4mm);
	\fill (C) circle (0.4mm);
	\fill (D) circle (0.4mm);

% segments, sides %{{{1

	\draw (A) -- (B) -- (C) -- (D) -- cycle;
	\draw[dashed] (X1) -- (D);
	\draw[dashed] (C) -- (X2);

% points, dots, vertices labels %{{{1

	\node[below left] at (A) {$A$};
	\node[below right] at (B) {$B$};
	\node[above] at (C) {$C$};
	\node[above] at (D) {$D$};

% angles labels %{{{1

	\pic[draw, ->, "$\alpha$", angle radius=0.8cm, angle eccentricity=1.5]
	{angle = B--A--D};

	\pic[draw, ->, "$\alpha$", angle radius=0.8cm, angle eccentricity=1.5]
	{angle = X1--D--A};

	\pic[draw, ->, "$\beta$", angle radius=0.8cm, angle eccentricity=1.5]
	{angle = C--B--A};

	\pic[draw, ->, "$\beta$", angle radius=0.8cm, angle eccentricity=1.5]
	{angle = B--C--X2};

	\pic[draw, ->, "$\gamma$", angle radius=0.6cm, angle eccentricity=1.5]
	{angle = D--C--B};

	\pic[draw, ->, "$\delta$", angle radius=0.6cm, angle eccentricity=1.5]
	{angle = A--D--C};

% closing %{{{1

\end{tikzpicture}
\end{document}
