% vim: set ft=tex tabstop=4 shiftwidth=4 noexpandtab:

\input{../../include/latex/tikz/header-light.tex}

% opening %{{{1

\begin{document}
\begin{tikzpicture}[scale=1.0]

% parameters %{{{1

	%\tikzmath{
		%\var = 2.1;
		%\numsides = int(3) ;
	%}

% coordinates %{{{1

	\coordinate (A) at (2,0);
	\coordinate (B) at (5,0);
	\coordinate (C) at (7,4);
	\coordinate (D) at (0,4);

	\coordinate (E) at (0,0);
	\coordinate (F) at (7,0);

% points, dots, vertices %{{{1

	\fill (A) circle (0.4mm);
	\fill (B) circle (0.4mm);
	\fill (C) circle (0.4mm);
	\fill (D) circle (0.4mm);

	\fill (E) circle (0.4mm);
	\fill (F) circle (0.4mm);

% segments, sides %{{{1

	\draw (A) -- (B) -- (C) -- (D) -- cycle;

	\draw[dashed] (D) -- (E);
	\draw[dashed] (C) -- (F);

	\draw[dashed] (D) -- (F);

	\draw[dashed] (E) -- (A);
	\draw[dashed] (B) -- (F);

% points, dots, vertices labels %{{{1

	\node[below left] at (A) {$A$};
	\node[below right] at (B) {$B$};
	\node[above right] at (C) {$C$};
	\node[above left] at (D) {$D$};

	\node[below] at (E) {$E$};
	\node[below] at (F) {$F$};

% segments, sides labels %{{{1

	\node[right] at ($ (D)!0.5!(E) $) {$h_1$};
	\node[left] at ($ (C)!0.5!(F) $) {$h_2$};

% segments, sides marks %{{{1

	\tkzMarkSegments[mark=||, size=2pt](A,D)
	\tkzMarkSegments[mark=||, size=2pt](C,B)

% angles labels %{{{1

	\pic[draw, ->, "$\alpha$", angle radius=0.5cm, angle eccentricity=1.5]
		{angle = B--A--D};

	\pic[draw, ->, "$\alpha$", angle radius=0.5cm, angle eccentricity=1.5]
		{angle = C--B--A};

	\pic[draw, ->, "$\beta$", angle radius=0.9cm, angle eccentricity=1.4]
		{angle = C--F--D};

	\pic[draw, ->, "$\gamma$" {xshift=7, yshift=6}, angle radius=0.9cm, angle eccentricity=1.4]
		{angle = E--D--F};

	\pic[draw, ->, "$\lambda$", angle radius=3.2cm, angle eccentricity=1.1]
		{angle = D--F--E};

	\pic[draw, ->, "$\mu$", angle radius=1.5cm, angle eccentricity=1.2]
		{angle = F--D--C};

	\pic[draw, ->, "$\theta$", angle radius=0.7cm, angle eccentricity=1.4]
		{angle = D--A--E};

	\pic[draw, ->, "$\omega$", angle radius=0.6cm, angle eccentricity=1.4]
		{angle = F--B--C};

% right angles markers %{{{2

	\pic [draw, angle radius=7pt, angle eccentricity=1]
		{right angle = D--E--A};

	\pic [draw, angle radius=7pt, angle eccentricity=1]
		{right angle = B--F--C};

% closing %{{{1

\end{tikzpicture}
\end{document}
