
\input{../../include/latex/tikz/header.tex}

% opening %{{{1

\begin{document}
\begin{tikzpicture}[scale=1.0]

% parameters %{{{1

	\tikzmath{
		\len = 4.0 ;
		\ang = 60 ;
		\gle = 90 - \ang / 2 ;
	}

% coordinates %{{{1

	\coordinate (A) at (0,0);
	\coordinate (B) at ({180+\gle}:\len);
	\coordinate (C) at (-\gle:\len);

% points, dots, vertices %{{{1

	\fill (A) circle (0.4mm);
	\fill (B) circle (0.4mm);
	\fill (C) circle (0.4mm);

% segments, sides, lines %{{{1

	\draw (A) -- (B) -- (C) -- cycle;

% points, dots, vertices labels %{{{1

	\node[above] at (A) {$A$};
	\node[below left] at (B) {$B$};
	\node[below right] at (C) {$C$};

% segment labels %{{{1

	\node[above left]  at ($(A)!0.5!(B)$) {$a$};
	\node[above right] at ($(A)!0.5!(C)$) {$a$};
	\node[below]  at ($(B)!0.5!(C)$) {$a$};

% segments, sides marks %{{{1

	\tkzMarkSegments[mark=||, size=2pt](A,B)
	\tkzMarkSegments[mark=||, size=2pt](B,C)
	\tkzMarkSegments[mark=||, size=2pt](C,A)

% closing %{{{1

\end{tikzpicture}
\end{document}
