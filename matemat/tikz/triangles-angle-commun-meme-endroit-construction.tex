% vim: set ft=tex tabstop=4 shiftwidth=4 noexpandtab:

\input{../../include/latex/tikz/header-light.tex}

% opening %{{{1

\begin{document}
\begin{tikzpicture}[scale=0.8]

% parameters %{{{1

	\def\ratio{1.7}
	\def\ext{2.3}

% coordinates %{{{1

	\coordinate (A) at (0,0);
	\coordinate (B) at (4,0);
	\coordinate (C) at (3,3);

	\coordinate (D) at ({4*\ratio},0);
	\coordinate (E) at ({3*\ratio},{3*\ratio});

	\coordinate (X) at ({4*\ext},0);
	\coordinate (Y) at ({3*\ext},{3*\ext});

% points, dots, vertices %{{{1

	\fill (A) circle (0.5mm);
	\fill (B) circle (0.5mm);
	\fill (C) circle (0.5mm);
	\fill (D) circle (0.5mm);
	\fill (E) circle (0.5mm);

% triangles %{{{1

	\draw (A) -- (D) -- (E) -- cycle;
	\draw (B) -- (C);

	\draw[dashed] (D) -- (X);
	\draw[dashed] (E) -- (Y);

% points, dots, vertices labels %{{{1

	\node[below left] at (A) {$A$};
	\node[below] at (B) {$B$};
	\node[above left] at (C) {$C$};

	\node[below] at (D) {$D$};
	\node[above left] at (E) {$E$};

% sides labels %{{{1

% angles labels %{{{1

	\pic[draw, ->, "$\alpha$", angle radius=1.1cm, angle eccentricity=1.3]
	{angle = B--A--C};

% closing %{{{1

\end{tikzpicture}
\end{document}
