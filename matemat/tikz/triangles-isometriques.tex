
\input{../../include/latex/tikz/header-light.tex}

% opening %{{{1

\begin{document}
\begin{tikzpicture}[scale=1.2]

% coordinates %{{{1

	\coordinate (A) at (0,0);
	\coordinate (B) at (5,0);
	\coordinate (C) at (2,3);

	\coordinate (D) at (7,0);
	\coordinate (E) at (12,0);
	\coordinate (F) at (9,3);

% points, dots, vertices %{{{1

	\fill (A) circle (0.4mm);
	\fill (B) circle (0.4mm);
	\fill (C) circle (0.4mm);

	\fill (D) circle (0.4mm);
	\fill (E) circle (0.4mm);
	\fill (F) circle (0.4mm);

% triangles %{{{1

	\draw (A) -- (B) -- (C) -- cycle;
	\draw (D) -- (E) -- (F) -- cycle;

% points, dots, vertices labels %{{{1

	\node[below left] at (A) {$A$};
	\node[below right] at (B) {$B$};
	\node[above] at (C) {$C$};

	\node[below left] at (D) {$D$};
	\node[below right] at (E) {$E$};
	\node[above] at (F) {$F$};

% sides labels %{{{1

	\node[above right] at ($(B)!0.5!(C)$) {$a_1$};
	\node[above left] at ($(A)!0.5!(C)$) {$b_1$};
	\node[below]  at ($(A)!0.5!(B)$) {$c_1$};

	\node[above right] at ($(E)!0.5!(F)$) {$a_2$};
	\node[above left] at ($(D)!0.5!(F)$) {$b_2$};
	\node[below]  at ($(D)!0.5!(E)$) {$c_2$};

% angles labels %{{{1

	\pic[draw, ->, "$\alpha_1$", angle eccentricity=1.3, angle radius=1cm]
	{angle = B--A--C};
	\pic[draw, ->, "$\beta_1$", angle eccentricity=1.3, angle radius=1cm]
	{angle = C--B--A};
	\pic[draw, ->, "$\gamma_1$", angle eccentricity=1.3, angle radius=1cm]
	{angle = A--C--B};

	\pic[draw, ->, "$\alpha_2$", angle eccentricity=1.3, angle radius=1cm]
	{angle = E--D--F};
	\pic[draw, ->, "$\beta_2$", angle eccentricity=1.3, angle radius=1cm]
	{angle = F--E--D};
	\pic[draw, ->, "$\gamma_2$", angle eccentricity=1.3, angle radius=1cm]
	{angle = D--F--E};

% closing %{{{1

\end{tikzpicture}
\end{document}
