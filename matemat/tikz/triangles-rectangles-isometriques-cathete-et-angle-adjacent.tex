% vim: set ft=tex tabstop=4 shiftwidth=4 noexpandtab:

\input{../../include/latex/tikz/header.tex}

% opening %{{{1

\begin{document}
\begin{tikzpicture}[scale=1.2]

% coordinates %{{{1

	\coordinate (B) at (0,0);
	\coordinate (C) at (5,0);
	\coordinate (A) at (5,3);

	\coordinate (E) at (7,0);
	\coordinate (F) at (12,0);
	\coordinate (D) at (12,3);

% points, dots, vertices %{{{1

	\fill (A) circle (0.4mm);
	\fill (B) circle (0.4mm);
	\fill (C) circle (0.4mm);

	\fill (D) circle (0.4mm);
	\fill (E) circle (0.4mm);
	\fill (F) circle (0.4mm);

% triangles %{{{1

	\draw (A) -- (B) -- (C) -- cycle;
	\draw (D) -- (E) -- (F) -- cycle;

% points, dots, vertices labels %{{{1

	\node[below] at (B) {$B$};
	\node[below] at (C) {$C$};
	\node[above] at (A) {$A$};

	\node[below] at (E) {$E$};
	\node[below] at (F) {$F$};
	\node[above] at (D) {$D$};

% sides labels %{{{1

	\node[below] at ($(B)!0.5!(C)$) {$a_1$};

	\node[below] at ($(E)!0.5!(F)$) {$a_2$};

% angles labels %{{{1

	\pic[draw, ->, "$\beta_1$", angle eccentricity=1.3, angle radius=1.4cm]
	{angle = C--B--A};

	\pic[draw, ->, "$\beta_2$", angle eccentricity=1.3, angle radius=1.4cm]
	{angle = F--E--D};

% right angles markers %{{{2

	\pic[draw, angle eccentricity=1, angle radius=7pt]
	{right angle = A--C--B};

	\pic[draw, angle eccentricity=1, angle radius=7pt]
	{right angle = D--F--E};

% closing %{{{1

\end{tikzpicture}
\end{document}
