% vim: set ft=tex tabstop=4 shiftwidth=4 noexpandtab:

\input{../../include/latex/tikz/header-light.tex}

% opening %{{{1

\begin{document}
\begin{tikzpicture}[scale=1.0]

% parameters %{{{1

	\def\len{6}
	\def\gth{5}
	\def\ratio{1.7}
	\def\ang{210}
	\def\gle{-100}

% coordinates %{{{2

	%\coordinate (u) at (\ang:1);
	%\coordinate (v) at (\gle:1);

	\coordinate (A) at (0,0);
	\coordinate (B) at (-5,-3);
	\coordinate (C) at (-2,-3);

	%\coordinate (B) at ($ (A) + \len*(u) $);
	%\coordinate (C) at ($ (A) + \gth*(v) $);

	\coordinate (u) at ($ (B) - (A) $);
	\coordinate (v) at ($ (C) - (A) $);

	\coordinate (D) at ($ (A) + \ratio*(u) $);
	\coordinate (E) at ($ (A) + \ratio*(v) $);

	\coordinate (F) at ($ (B)!(A)!(C) $);
	\coordinate (G) at ($ (D)!(A)!(E) $);

	\coordinate (H) at ($ (D)!(B)!(E) $);
	\coordinate (I) at ($ (D)!(C)!(E) $);

% points, dots, vertices %{{{1

	\fill (A) circle (0.4mm);
	\fill (B) circle (0.4mm);
	\fill (C) circle (0.4mm);
	\fill (D) circle (0.4mm);
	\fill (E) circle (0.4mm);

	\fill (F) circle (0.4mm);
	\fill (G) circle (0.4mm);

	\fill (H) circle (0.4mm);
	\fill (I) circle (0.4mm);

% segments, sides, lines %{{{1

	\draw (A) -- (B) -- (C) -- cycle;
	\draw (B) -- (D);
	\draw (C) -- (E);
	\draw (B) -- (C);
	\draw (D) -- (E);

	\draw[dashed] (A) -- (G);
	\draw[dashed] (B) -- (H);
	\draw[dashed] (C) -- (I);
	\draw[dashed] (C) -- (F);
	\draw[dashed] (E) -- (G);

% points, dots, vertices labels %{{{1

	\node[above] at (A) {$A$};
	\node[above left] at (B) {$B$};
	\node[above left] at (C) {$C$};
	\node[below left] at (D) {$D$};
	\node[below] at (E) {$E$};

	\node[right] at (F) {$F$};
	\node[below right] at (G) {$G$};

	\node[below left] at (H) {$H$};
	\node[below] at (I) {$I$};

% segments, sides, lines labels %{{{1

	\node[above left] at ($ (A)!0.5!(B) $) {$a$};
	\node[above left] at ($ (A)!0.7!(C) $) {$b$};

	%\node[left] at ($ (A)!0.7!(F) $) {$f$};

	\node[above left] at ($ (B)!0.5!(D) $) {$u$};
	\node[above left] at ($ (C)!0.5!(E) $) {$v$};

	\node[right] at ($ (B)!0.5!(H) $) {$h$};
	\node[right] at ($ (C)!0.5!(I) $) {$p$};

% segments, sides, lines marks %{{{1

	%\tkzMarkSegments[mark=||, size=2pt](A,B)
	%\tkzMarkSegments[mark=||, size=2pt](C,D)

% angles labels %{{{1

	\pic[draw, ->, "$\alpha$", angle radius=0.9cm, angle eccentricity=1.3]
	{angle = B--A--C};

	\pic[draw, ->, "$\beta$", angle radius=1.1cm, angle eccentricity=1.3]
	{angle = E--D--A};

	\pic[draw, ->, "$\gamma$", angle radius=1.1cm, angle eccentricity=1.3]
	{angle = C--B--A};

	\pic[draw, ->, "$\lambda$", angle radius=1.7cm, angle eccentricity=1.2]
	{angle = B--A--F};

	\pic[draw, ->, "$\mu$", angle radius=1.0cm, angle eccentricity=1.2]
	{angle = D--B--H};

	\pic[draw, ->, "$\eta$", angle radius=1.1cm, angle eccentricity=1.2]
	{angle = C--A--F};

	\pic[draw, ->, "$\theta$", angle radius=1.0cm, angle eccentricity=1.2]
	{angle = E--C--I};

% right angles markers %{{{2

	\pic [draw, angle radius=7pt, angle eccentricity=1]
	{right angle = E--G--A};

	\pic [draw, angle radius=7pt, angle eccentricity=1]
	{right angle = E--H--B};

	\pic [draw, angle radius=7pt, angle eccentricity=1]
	{right angle = C--I--D};

% closing %{{{1

\end{tikzpicture}
\end{document}
