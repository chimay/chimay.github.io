% vim: set ft=tex tabstop=4 shiftwidth=4 noexpandtab:

\input{../../include/latex/tikz/header-light.tex}

% opening %{{{1

\begin{document}
\begin{tikzpicture}[scale=0.8]

% parameters %{{{1

	\def\len{6}
	\def\gth{5}
	\def\ratio{1.7}
	\def\ang{210}
	\def\gle{-70}

% coordinates %{{{1

	%\coordinate (u) at (\ang:1);
	%\coordinate (v) at (\gle:1);

	\coordinate (A) at (0,0);
	\coordinate (B) at (-4,-3);
	\coordinate (C) at (2,-3);

	%\coordinate (B) at ($ (A) + \len*(u) $);
	%\coordinate (C) at ($ (A) + \gth*(v) $);

	\coordinate (u) at ($ (B) - (A) $);
	\coordinate (v) at ($ (C) - (A) $);

	\coordinate (D) at ($ (A) + \ratio*(u) $);
	\coordinate (E) at ($ (A) + \ratio*(v) $);

% points, dots, vertices %{{{1

	\fill (A) circle (0.5mm);
	\fill (B) circle (0.5mm);
	\fill (C) circle (0.5mm);
	\fill (D) circle (0.5mm);
	\fill (E) circle (0.5mm);

% segments, sides, lines %{{{1

	\draw (A) -- (B) -- (C) -- cycle;
	\draw (B) -- (D);
	\draw (C) -- (E);
	\draw (B) -- (C);
	\draw (D) -- (E);

% points, dots, vertices labels %{{{1

	\node[above] at (A) {$A$};
	\node[above left] at (B) {$B$};
	\node[above right] at (C) {$C$};
	\node[below left] at (D) {$D$};
	\node[below right] at (E) {$E$};

% segments, sides, lines labels %{{{1

	\node[above left] at ($ (A)!0.5!(B) $) {$a$};
	\node[above right] at ($ (A)!0.5!(C) $) {$b$};
	\node[above right] at ($ (B)!0.5!(C) $) {$c$};
	\node[above right] at ($ (D)!0.5!(E) $) {$f$};

% segments, sides, lines marks %{{{1

	%\tkzMarkSegments[mark=||, size=2pt](A,B)
	%\tkzMarkSegments[mark=||, size=2pt](C,D)

% angles labels %{{{1

	\pic[draw, ->, "$\alpha$", angle radius=1.0cm, angle eccentricity=1.3]
	{angle = B--A--C};

% closing %{{{1

\end{tikzpicture}
\end{document}
