
\input{../../include/latex/tikz/header-light.tex}

% opening %{{{1

\begin{document}
\begin{tikzpicture}[scale=1.2]

% coordinates %{{{1

	\coordinate (A1) at (0,0);
	\coordinate (C1) at (6,0);
	\coordinate (B1) at ({18/5},{18/5});

	\begin{scope}[shift={(7,0)}]
		\begin{scope}[scale=0.7]
			\coordinate (A2) at (0,0);
			\coordinate (C2) at (6,0);
			\coordinate (B2) at ({18/5},{18/5});
		\end{scope}
	\end{scope}

	%\coordinate (A2) at (7,0);
	%\coordinate (C2) at (12,0);
	%\coordinate (B2) at (9,3);

% lines %{{{1

	\draw (A1) -- (B1) -- (C1) -- cycle;
	\draw (A2) -- (B2) -- (C2) -- cycle;

% points, dots, vertices %{{{1

	\fill (A1) circle (0.4mm);
	\fill (B1) circle (0.4mm);
	\fill (C1) circle (0.4mm);

	\fill (A2) circle (0.4mm);
	\fill (B2) circle (0.4mm);
	\fill (C2) circle (0.4mm);

% polygon vertices %{{{2

	%\foreach \i in {1,...,\numsides} \fill (P\i) circle (0.4mm);

% grid vertices %{{{2

	%\foreach \i in {0,...,\xnum}
		%\foreach \j in {0,...,\ynum}
			%\fill (P-\i-\j) circle (0.4mm);

% side labels %{{{1

	\node[left]  at ($(A1)!0.5!(B1)$) {$a_1$};
	\node[below] at ($(A1)!0.5!(C1)$) {$b_1$};
	\node[right] at ($(B1)!0.5!(C1)$) {$c_1$};

	\node[left]  at ($(A2)!0.5!(B2)$) {$a_2$};
	\node[below] at ($(A2)!0.5!(C2)$) {$b_2$};
	\node[right] at ($(B2)!0.5!(C2)$) {$c_2$};

% angles labels %{{{1 %{{{1

	\pic[draw, ->, "$\alpha_1$", angle eccentricity=1.3, angle radius=1cm]
	{angle = B1--C1--A1};
	\pic[draw, ->, "$\beta_1$", angle eccentricity=1.3, angle radius=1cm]
	{angle = A1--B1--C1};
	\pic[draw, ->, "$\gamma_1$", angle eccentricity=1.3, angle radius=1cm]
	{angle = C1--A1--B1};

	\pic[draw, ->, "$\alpha_2$", angle eccentricity=1.3, angle radius=1cm]
	{angle = B2--C2--A2};
	\pic[draw, ->, "$\beta_2$", angle eccentricity=1.3, angle radius=1cm]
	{angle = A2--B2--C2};
	\pic[draw, ->, "$\gamma_2$", angle eccentricity=1.3, angle radius=1cm]
	{angle = C2--A2--B2};

% closing %{{{1

\end{tikzpicture}
\end{document}
