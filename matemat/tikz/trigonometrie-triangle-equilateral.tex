
\input{../../include/latex/tikz/header-light.tex}

% opening %{{{1

\begin{document}
\begin{tikzpicture}[scale=1.0]

% parameters %{{{1

	\def\len{5}
	\def\ang{60}

% coordinates %{{{1

	\coordinate (A) at (0,0);
	\coordinate (B) at (0:{2*\len*cos(\ang)});
	\coordinate (C) at (\ang:\len);

	\coordinate (D) at ({\len*cos(\ang)},0);

% points, dots, vertices %{{{1

	\fill (A) circle (0.4mm);
	\fill (B) circle (0.4mm);
	\fill (C) circle (0.4mm);
	\fill (D) circle (0.4mm);

% polygon vertices %{{{2

	%\foreach \i in {1,...,\numsides} { \fill (P\i) circle (0.3mm); }

% equilateral triangle %{{{1

	% triangle sides
	\draw (A) -- (B) -- (C) -- cycle;

	% altitude
	\draw[dashed] (C) -- (D);

% points, dots, vertices labels %{{{1

	\node[below left] at (A) {$A$};
	\node[below right] at (B) {$B$};
	\node[above] at (C) {$C$};
	\node[below] at (D) {$D$};

% segment labels %{{{1

	\node[above left]  at ($(A)!0.5!(C)$) {$a$};
	\node[above right] at ($(B)!0.5!(C)$) {$a$};

	\node[below] at ($(A)!0.5!(D)$) {$a/2$};
	\node[below] at ($(D)!0.5!(B)$) {$a/2$};

	\node[left] at ($(C)!0.6!(D)$) {$h$};

% angle labels %{{{1

	\pic["$60^\circ$", draw, ->, angle eccentricity=1.5, angle radius=0.7cm]
		{angle = B--A--C};

	\pic["$60^\circ$", draw, ->, angle eccentricity=1.5, angle radius=0.7cm]
		{angle = C--B--A};

	\pic["$30^\circ$", draw, ->, angle eccentricity=1.3, angle radius=1.4cm]
		{angle = A--C--D};

	\pic["$30^\circ$", draw, ->, angle eccentricity=1.3, angle radius=1.4cm]
		{angle = D--C--B};

% right angle markers %{{{2

	\pic [draw, angle radius=7pt, angle eccentricity=1]
		{right angle = C--D--A};

% closing %{{{1

\end{tikzpicture}
\end{document}
